% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes_meta_spot.R
\name{MetaSpot}
\alias{MetaSpot}
\title{bixverse MetaSpot class}
\usage{
MetaSpot(
  meta_cell_data,
  var_data,
  samples,
  per_sample_coords,
  per_sample_graph = list(),
  aggregation_method
)
}
\arguments{
\item{meta_cell_data}{Named list. Output of meta-spot generation Rust
functions. Must contain \code{aggregated} and \code{assignments}, same shape as
\code{\link[=MetaCells]{MetaCells()}}.}

\item{var_data}{data.table with the variable/feature information.
Must contain \code{gene_idx} and \code{gene_id}.}

\item{samples}{Named list keyed by \code{exp_id} of
\code{\link[=new_spatial_sample]{new_spatial_sample()}} objects (typically copied from the
parent SpatialSpot).}

\item{per_sample_coords}{Named list keyed by \code{exp_id} of two-column
numeric matrices with the metaspot centroids for each sample.}

\item{per_sample_graph}{Named list keyed by \code{exp_id} of \code{dgCMatrix}
(CSR-style) adjacency matrices at metaspot resolution. Optional;
defaults to an empty list.}

\item{aggregation_method}{String describing how metaspots were
generated. E.g. \code{"hex_bin_16um"}, \code{"superspot_walktrap"}.}
}
\value{
Returns the \code{MetaSpot} class for further operations.
}
\description{
Metaspot (aggregated SpatialSpot) class. Inherits from
\code{\link[=MetaCells]{MetaCells()}} - all in-memory obs/var/data primitives work
unchanged. Adds per-sample centroids and adjacency graphs at metaspot
resolution.

The \code{obs_table} must contain an \code{exp_id} column identifying which
slide each metaspot belongs to.
}
\section{Properties}{

\describe{
\item{obs_table}{The metaspot observation table. Must include
\code{exp_id}.}
\item{var_table}{The metaspot variable table.}
\item{data}{Named list with \code{raw} and \code{norm} aggregated count
matrices.}
\item{sc_cache}{Inherited \code{ScCache} for global analysis state.}
\item{original_assignment}{Inherited assignment metadata.}
\item{dims}{Two-element integer with \code{c(no_metaspots, no_genes)}.}
\item{other_data}{Inherited list for additional data.}
\item{meta_cell_method}{Inherited origin string.}
\item{samples}{Named list of \code{\link[=new_spatial_sample]{new_spatial_sample()}}
keyed by \code{exp_id}.}
\item{per_sample_coords}{Named list of n_metaspot x 2 coord
matrices keyed by \code{exp_id}.}
\item{per_sample_graph}{Named list of \code{dgCMatrix} adjacency
matrices at metaspot resolution, keyed by \code{exp_id}.}
\item{aggregation_method}{String identifying the aggregation
algorithm used.}
}
}

