% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes_single_cell_subset.R
\name{SingleCellsSubset}
\alias{SingleCellsSubset}
\title{bixverse single cell subset class}
\usage{
SingleCellsSubset(sc_object, grouping_column, group)
}
\value{
A \code{SingleCellsSubset} object.
}
\description{
Subset view onto a \code{\link[=SingleCells]{SingleCells()}} object, restricted to cells
belonging to a single level of a grouping variable. The Rust count
connection is shared with the parent (no data copy). \code{obs_table} and
\code{var_table} are held in memory; \code{sc_map} is rebuilt to point only at the
subset cells but stays in the original index space so Rust calls remain
valid without further translation.
}
\section{Properties}{

\describe{
\item{count_connection}{Shared Rust pointer to the on-disk counts.}
\item{dir_data}{Directory holding the binary count files.}
\item{obs_table}{Subset obs (rows for the chosen group only).
\code{cell_idx} keeps the original 1-indexed position in the parent.}
\item{var_table}{Variable/feature table (unchanged from parent).}
\item{grouping_column}{Column in obs used to define the subset.}
\item{group}{Value of \code{grouping_column} represented by this subset.}
\item{sc_cache}{Fresh \code{ScCache} for subset-specific PCA, kNN, sNN,
embeddings.}
\item{sc_map}{\code{ScMap} restricted to the subset cells. \code{cell_mapping}
stays 1-indexed and \code{cells_to_keep_idx} stays 0-indexed, both in the
original parent index space.}
\item{subset_to_original}{Integer vector. 1-indexed original cell
positions, in subset row order. \code{subset_to_original[i]} is the parent
position of subset row \code{i}.}
\item{dims}{\code{c(n_cells_subset, n_genes)}.}
}
}

