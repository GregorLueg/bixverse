% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes_spatial_spot.R
\name{SpatialSpot}
\alias{SpatialSpot}
\title{bixverse SpatialSpot class}
\usage{
SpatialSpot(dir_data)
}
\arguments{
\item{dir_data}{String. Path to the directory holding the SpatialSpot
on-disk files (DuckDB plus the two \code{counts_*.bin} Rust files).}
}
\value{
Returns the \code{SpatialSpot} class for further operations.
}
\description{
Spatial transcriptomics class for spot-based assays (Visium, Xenium).
Inherits from \code{\link[=SingleCells]{SingleCells()}} - the on-disk DuckDB + Rust
binary count storage and all global single-cell methods (HVG, PCA,
KNN, SNN, embeddings) work unchanged. Adds per-experiment spatial
metadata in \code{samples} and per-experiment spatial analysis results in
\code{sp_cache}.

The obs table on disk is expected to include an \code{exp_id} column to
distinguish spots from different slides/sections.
}
\section{Properties}{

\describe{
\item{db_connection}{R6 \code{\link[=SpatialSpotDuckDB]{SpatialSpotDuckDB()}} instance.}
\item{count_connection}{R6-like environment pointing to the Rust
sparse count storage.}
\item{dir_data}{Path to the on-disk data directory.}
\item{sc_cache}{Inherited \code{ScCache} for global single-cell-style
state (PCA, KNN, SNN, embeddings).}
\item{sc_map}{Inherited \code{ScMap} with gene/cell mappings and HVG
indices.}
\item{dims}{Two-element integer with \code{c(no_spots, no_genes)}.}
\item{samples}{Named list keyed by \code{exp_id} holding one
\code{\link[=new_spatial_sample]{new_spatial_sample()}} per slide/section.}
\item{sp_cache}{\code{\link[=new_sp_cache]{new_sp_cache()}} holding per-sample
spatial analysis results.}
}
}

