% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes_spatial_spot_subset.R
\name{SpatialSpotSubset}
\alias{SpatialSpotSubset}
\title{bixverse spatial spot subset class}
\usage{
SpatialSpotSubset(sp_object, exp_id)
}
\arguments{
\item{sp_object}{A \code{\link[=SpatialSpot]{SpatialSpot()}}.}

\item{exp_id}{String. The experiment identifier to subset to.}
}
\value{
A \code{SpatialSpotSubset} object.
}
\description{
Subset view onto a \code{\link[=SpatialSpot]{SpatialSpot()}} object, restricted to the spots
of a single experiment. Mirrors \code{\link[=SingleCellsSubset]{SingleCellsSubset()}} and inherits
from it, so every single-cell-style getter, setter and method works
unchanged. The Rust count connection is shared with the parent (no data
copy); \code{obs_table} and \code{var_table} are held in memory; \code{sc_map} is rebuilt to
point only at the subset spots but stays in the original index space so Rust
calls remain valid without further translation.

The grouping column is always \code{exp_id}, so the constructor takes the
experiment identifier rather than a generic column/value pair.
}
\section{Properties}{

\describe{
\item{count_connection}{Shared Rust pointer to the on-disk counts.}
\item{dir_data}{Directory holding the binary count files.}
\item{obs_table}{Subset obs, sorted by \code{cell_idx}. \code{cell_idx} keeps the
original 1-indexed position in the parent.}
\item{var_table}{Variable/feature table (unchanged from parent).}
\item{grouping_column}{Always \code{"exp_id"}.}
\item{group}{The \code{exp_id} this subset represents.}
\item{sc_cache}{Fresh \code{ScCache} for subset-specific PCA, kNN, sNN,
embeddings.}
\item{sc_map}{\code{ScMap} restricted to the subset spots. \code{cell_mapping}
stays 1-indexed and \code{cells_to_keep_idx} stays 0-indexed, both in the
original parent index space.}
\item{subset_to_original}{Integer vector. 1-indexed original spot
positions, ascending, in subset row order.}
\item{dims}{\code{c(n_spots_subset, n_genes)}.}
\item{sample}{The \code{\link[=new_spatial_sample]{new_spatial_sample()}} of this experiment.}
\item{sp_cache}{Fresh \code{\link[=new_sp_cache]{new_sp_cache()}} for the spatial results.}
\item{coords}{Numeric matrix with columns \code{x} and \code{y} in full-resolution
pixels, one row per subset spot, aligned with \code{subset_to_original}.}
}
}

\section{Row order}{

\code{obs_table}, \code{coords} and \code{subset_to_original} are all in ascending
\code{cell_idx} order, which is what \code{\link[=get_spot_indices_for_exp]{get_spot_indices_for_exp()}}
returns. Row \code{i} of \code{coords} therefore belongs to spot
\code{subset_to_original[i]} of the parent, and everything handed to Rust lines up
by position without a translation step.
}

