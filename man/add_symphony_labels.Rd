% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_sc_annotations_reference.R
\name{add_symphony_labels}
\alias{add_symphony_labels}
\title{Add labels to a Symphony reference post-hoc}
\usage{
add_symphony_labels(reference, sc_object, columns, overwrite = FALSE)
}
\arguments{
\item{reference}{\code{SymphonyReference}.}

\item{sc_object}{\code{SingleCells} to read labels from. Typically the same
object passed to \code{\link[=build_symphony_ref]{build_symphony_ref()}}.}

\item{columns}{Character vector of obs column names.}

\item{overwrite}{Boolean. If \code{TRUE}, existing label columns of the same
name are replaced; otherwise an error is raised on collision.}
}
\value{
The \code{reference} with updated \code{labels}.
}
\description{
Reads one or more obs columns from a \code{SingleCells} object and stores them
in the reference's \code{labels} slot for use by \code{\link[=transfer_labels_symphony]{transfer_labels_symphony()}}.
The provided \code{sc_object} must have \code{cells_to_keep} matching the cells used
at reference-build time; this is enforced by a length check against
\code{nrow(z_corr)}.
}
\examples{
# attach obs labels to a reference built without them
ref <- demo_single_cells(
  syn_data_params = params_sc_synthetic_data(
    n_cells = 500L,
    n_genes = 50L,
    n_batches = 2L
  )
)
symphony_ref <- build_symphony_ref(
  ref,
  batch_column = "batch_index",
  hvg = get_hvg(ref) + 1L,
  harmony_params = params_sc_harmony(k = 10L),
  no_pcs = 10L,
  .verbose = FALSE
)
symphony_ref <- add_symphony_labels(
  symphony_ref,
  sc_object = ref,
  columns = "cell_grp"
)
head(get_symphony_labels(symphony_ref))

unlink(ref@dir_data, recursive = TRUE, force = TRUE)
}
