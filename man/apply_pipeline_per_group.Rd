% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipelines_sc.R
\name{apply_pipeline_per_group}
\alias{apply_pipeline_per_group}
\title{Apply a pipeline independently to each group of a \code{SingleCells} object}
\usage{
apply_pipeline_per_group(pipeline, object, group_col, groups = NULL)
}
\arguments{
\item{pipeline}{\code{ScPipeline}.}

\item{object}{\code{SingleCells}.}

\item{group_col}{String. Column in obs used to split.}

\item{groups}{Optional character vector. Restrict to these group values; if
\code{NULL}, all unique values of \code{group_col} are used.}
}
\value{
Named list of \code{SingleCellsSubset} objects, names being the group
values.
}
\description{
Splits \code{object} by \code{group_col}, applies \code{pipeline} to each subset, and
returns a named list of processed \code{SingleCellsSubset}s. Useful for
per-sample / per-cell-type re-analysis where the same chain (HVG, PCA,
neighbours, clusters, ...) is run on each group, e.g. sample-pure metacell
generation followed by an external merge.
}
