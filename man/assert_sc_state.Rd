% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions_sc_cache_state.R
\name{assert_sc_state}
\alias{assert_sc_state}
\title{Assert that cached artefacts still match the object's state}
\usage{
assert_sc_state(
  x,
  artefacts,
  modality = "rna",
  .var.name = checkmate::vname(x),
  add = NULL
)
}
\arguments{
\item{x}{\code{SingleCells}, \code{SingleCellsSubset}, \code{MetaCells} or
\code{SingleCellsMultiModal} class.}

\item{artefacts}{Character vector. Artefacts to check, either an artefact
kind (\code{"pca"}, \code{"knn"}, \code{"snn"}, \code{"magic"}) or an embedding name
(\code{"umap"}). May be modality qualified (\code{"adt:pca"}).}

\item{modality}{String. Modality to resolve unqualified names against. One
of \code{c("rna", "adt", "atac", "wnn")}.}
}
\value{
Invisibly \code{x} when the assertion holds, otherwise an error.
}
\description{
The hard tier. Called at the entry of anything that hands cached indices to
Rust or writes a derived result back into the object, where a stale artefact
means either a crash in the binding layer or silently mis-aligned biology.
}
