% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions_single_cell.R
\name{build_hvg_table}
\alias{build_hvg_table}
\title{Assemble the HVG data.table and flag the top N}
\usage{
build_hvg_table(var_table, res, hvg_no, hvg_method)
}
\arguments{
\item{var_table}{data.table. The var table (or a subset of it) containing
at least \code{gene_idx} and \code{gene_id}. Copied internally so the caller's
table is not mutated.}

\item{res}{Named list. Per-gene statistics returned by \code{rs_sc_hvg} or
\code{rs_mc_hvg}. The names of \code{res} become new columns on the returned
data.table. Must contain the ranking column implied by \code{hvg_method}
(\code{var_std} for \code{"vst"}, \code{dispersion} for \code{"dispersion"},
\code{dispersion_scaled} for \code{"meanvarbin"}).}

\item{hvg_no}{Integer. Number of top genes to flag as HVGs.}

\item{hvg_method}{String. One of \code{c("vst", "dispersion", "meanvarbin")}.
Selects which column in \code{res} is used to rank genes.}
}
\value{
A data.table with the original \code{var_table} columns, all columns
from \code{res}, plus:
\itemize{
\item \code{is_hvg} - Boolean. \code{TRUE} for the top \code{hvg_no} genes.
\item \code{hvg_rank} - Integer. Rank from 1 (most variable) to \code{hvg_no}
for HVGs, \code{NA_integer_} otherwise.
}
}
\description{
Internal helper shared by the \code{get_hvg_data_sc} methods. Takes the
per-gene statistics returned by the Rust HVG functions, attaches them
to a copy of the var table, and flags the top \code{hvg_no} genes according
to the ranking column appropriate for the chosen HVG method.
}
\keyword{internal}
