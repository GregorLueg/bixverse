% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes_single_cell_others.R
\name{build_regulons}
\alias{build_regulons}
\alias{build_regulons.ScenicGrn}
\title{Build the final regulons}
\usage{
build_regulons(
  x,
  use_leading_edge = TRUE,
  add_tf = TRUE,
  min_genes = 10L,
  mode = c("activating", "repressing", "both"),
  .verbose = TRUE
)

\method{build_regulons}{ScenicGrn}(
  x,
  use_leading_edge = TRUE,
  add_tf = TRUE,
  min_genes = 10L,
  mode = c("activating", "repressing", "both"),
  .verbose = TRUE
)
}
\arguments{
\item{x}{\code{ScenicGrn} object. You need to have run
\code{\link[=tf_to_genes_motif_enrichment]{tf_to_genes_motif_enrichment()}} for the leading edge filter to do anything.}

\item{use_leading_edge}{Boolean. Shall only the TF to gene pairs inside the
CisTarget leading edge be kept. Defaults to \code{TRUE}.}

\item{add_tf}{Boolean. Shall the TF be added to its own regulon. Defaults to
\code{TRUE}, following SCENIC.}

\item{min_genes}{Integer. Regulons with fewer genes than this are dropped.
Defaults to \code{10L}, the SCENIC value.}

\item{mode}{String. Which links to keep, based on the \code{cor_sign} column
added by \code{\link[=tf_to_genes_correlations]{tf_to_genes_correlations()}}. One of
\code{c("activating", "repressing", "both")}. With \code{"both"} the regulon names get
a \code{"_pos"} or \code{"_neg"} suffix so the two stay distinct. Defaults to
\code{"activating"}.}

\item{.verbose}{Boolean. Controls verbosity of the function.}
}
\value{
A named list of character vectors, one per regulon.
}
\description{
Turns the TF to gene table into the gene sets you hand to \code{\link[=aucell_sc]{aucell_sc()}}.
\code{\link[=tf_to_genes_motif_enrichment]{tf_to_genes_motif_enrichment()}} only flags which pairs survived the
CisTarget leading edge, it does not remove anything, so this is the step that
applies that filter.

Following SCENIC, the TF is added back to its own target list and regulons
below \code{min_genes} are dropped. Note the CisTarget leading edge usually cuts
hard: a module of a few hundred candidate targets typically ends up as a
regulon of a few dozen.
}
\references{
Aibar, et al., Nat Methods, 2017
}
