% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/base_generics_spatial.R
\name{build_spatial_graph_sp}
\alias{build_spatial_graph_sp}
\title{Build the spatial neighbourhood graph}
\usage{
build_spatial_graph_sp(
  object,
  exp_id = NULL,
  graph_params = params_sp_graph(),
  knn_params = NULL,
  seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{object}{A \code{\link[=SpatialSpot]{SpatialSpot()}} or
\code{\link[=SpatialSpotSubset]{SpatialSpotSubset()}}.}

\item{exp_id}{Optional string. Restrict to one experiment. \code{NULL} runs over
every registered sample. A \code{SpatialSpotSubset} only ever has one.}

\item{graph_params}{List. Output of \code{\link[=params_sp_graph]{params_sp_graph()}}.}

\item{knn_params}{List or \code{NULL}. Output of \code{\link[=params_sc_knn]{params_sc_knn()}},
only used by \code{layout = "knn"}.}

\item{seed}{Integer. Seed for the ANN index.}

\item{.verbose}{Boolean or integer. Controls verbosity.}
}
\value{
The object with the graph(s) cached.
}
\description{
Turns spot coordinates into the neighbour and weight lists every other
spatial statistic in this package consumes. The result lands in the
\code{per_sample_spatial_graph} slot of the \code{SpCache} as a sparse adjacency, one
entry per sample.
}
