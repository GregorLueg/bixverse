% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions_gse.R
\name{calc_singscore}
\alias{calc_singscore}
\title{Bixverse implementation of singscore (single gene set)}
\usage{
calc_singscore(
  ranks,
  up_set,
  down_set = NULL,
  center_score = TRUE,
  known_direction = TRUE,
  n_permutations = 0L,
  seed = 42L
)
}
\arguments{
\item{ranks}{Numerical matrix. Output of \code{\link[=calc_singscore_rank]{calc_singscore_rank()}}.}

\item{up_set}{Character vector. Gene names of the up-regulated set.}

\item{down_set}{Character vector or NULL. Optional paired down-regulated set.}

\item{center_score}{Boolean. Centre scores around 0. Ignored when
\code{known_direction = FALSE}.}

\item{known_direction}{Boolean. Whether the up-set direction is known.
Becomes irrelevant when \code{down_set} is also provided.}

\item{n_permutations}{Integer. Number of permutations. \code{0} disables the
permutation test.}

\item{seed}{Integer. RNG seed for the permutation test.}
}
\value{
A data.table with one row per sample. Columns: \code{total_score},
\code{total_dispersion}, optionally \code{up_score}, \code{up_dispersion}, \code{down_score},
\code{down_dispersion}, and (when permutations are run) \code{pval}. With
permutations, the null distribution is attached as
\code{attr(., "null_distribution")}.
}
\description{
Scores all samples against one up-regulated gene set with an optional
paired down-regulated set. When \code{n_permutations > 0}, additionally runs a
permutation test and includes empirical p-values.
}
\references{
Foroutan et al., BMC Bioinformatics, 2018.
}
