% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_sc_batch_corr.R
\name{calculate_cell_type_asw_sc}
\alias{calculate_cell_type_asw_sc}
\title{Calculate cell type average silhouette width}
\usage{
calculate_cell_type_asw_sc(
  object,
  cell_type_column,
  embd_to_use = "pca",
  max_cells = 5000L,
  seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{object}{\code{SingleCells} or \code{SingleCellsSubset} class.}

\item{cell_type_column}{String. The column with the cell type labels in the
obs data of the class.}

\item{embd_to_use}{String. Which embedding to compute the ASW on. Defaults
to \code{"pca"}.}

\item{max_cells}{Integer or \code{NULL}. If not \code{NULL}, subsample to this many
cells for performance. Defaults to \code{5000L}.}

\item{seed}{Integer. Seed for subsampling reproducibility.}

\item{.verbose}{Boolean. Controls verbosity of the function.}
}
\value{
A \code{CellTypeAswScores} object with the following elements
\itemize{
\item per_cell - Per-cell rescaled silhouette scores in \verb{[0, 1]}.
\item mean_asw - Mean rescaled silhouette width.
\item median_asw - Median rescaled silhouette width.
\item n_cell_types - Number of cell types.
\item embedding_used - Which embedding the ASW was computed on.
}
}
\description{
Average silhouette width on cell type labels in the embedding, rescaled to
\verb{[0, 1]} via \code{(s + 1) / 2} as in scIB. Higher values mean cell types stay
separated after correction. Counterpart to
\code{\link[=calculate_batch_asw_sc]{calculate_batch_asw_sc()}} on the biology side.
}
\examples{
# cell type silhouette width on the PCA embedding
sc <- demo_single_cells(
  syn_data_params = params_sc_synthetic_data(
    n_cells = 600L, n_genes = 50L, n_batches = 3L
  )
)
calculate_cell_type_asw_sc(
  sc,
  cell_type_column = "cell_grp",
  .verbose = FALSE
)

unlink(sc@dir_data, recursive = TRUE, force = TRUE)
}
\references{
Luecken, et al., Nat. Methods, 2022
}
