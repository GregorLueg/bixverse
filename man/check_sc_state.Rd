% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions_sc_cache_state.R
\name{check_sc_state}
\alias{check_sc_state}
\title{Check that cached artefacts still match the object's state}
\usage{
check_sc_state(x, artefacts, modality = "rna")
}
\arguments{
\item{x}{\code{SingleCells}, \code{SingleCellsSubset}, \code{MetaCells} or
\code{SingleCellsMultiModal} class.}

\item{artefacts}{Character vector. Artefacts to check, either an artefact
kind (\code{"pca"}, \code{"knn"}, \code{"snn"}) or an embedding name (\code{"umap"}). May be
modality qualified (\code{"adt:pca"}).}

\item{modality}{String. Modality to resolve unqualified names against. One
of \code{c("rna", "adt", "atac", "wnn")}.}
}
\value{
\code{TRUE} if the check was successful, otherwise a string describing
the failure.
}
\description{
Returns \code{TRUE} when every requested artefact is fresh, otherwise a message
describing the first problem. An artefact that is absent, or that predates
provenance stamping, passes: unknown provenance is not the same as known
bad.

Named in snake case rather than the \code{checkXxx} camel case of the other
checkmate extensions in this package, because it validates object state
rather than a \verb{params_*()} list and sits alongside \code{\link[=get_sc_cache_status]{get_sc_cache_status()}}.
}
