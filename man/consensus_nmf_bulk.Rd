% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_coexp_nmf.R
\name{consensus_nmf_bulk}
\alias{consensus_nmf_bulk}
\title{Run consensus NMF on a BulkCoExp}
\usage{
consensus_nmf_bulk(
  object,
  k,
  n_runs = 30L,
  preprocessing = c("none", "sd", "sqrt_sd"),
  nmf_hals_params = params_nmf_hals(),
  nmf_consensus_params = params_nmf_consensus(),
  membership_params = params_module_membership(),
  seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{object}{The class, see \code{\link[=BulkCoExp]{BulkCoExp()}}. Ideally, you should
run \code{\link[=preprocess_bulk_coexp]{preprocess_bulk_coexp()}} before applying this function. The
NMF requires non-negative input, so the pre-processing should either skip
scaling or use a strictly non-negative representation.}

\item{k}{Integer. Number of latent factors (modules). Must be at least 2.}

\item{n_runs}{Integer. Number of random restarts. Must be at least 2.}

\item{preprocessing}{String. One of \code{c("none", "sd", "sqrt_sd")}. Applied
inside the Rust kernel to the input matrix before the HALS updates.}

\item{nmf_hals_params}{List. Output of \code{\link[=params_nmf_hals]{params_nmf_hals()}}. The
\code{nmf_init} field is ignored, restarts always use random initialisation.}

\item{nmf_consensus_params}{List. Output of
\code{\link[=params_nmf_consensus]{params_nmf_consensus()}}. Controls the clustering of the pooled
components.}

\item{membership_params}{List. Controls how the gene loadings are turned into
module membership, see \code{\link[=params_module_membership]{params_module_membership()}}.}

\item{seed}{Integer. Base random seed. Restart \code{i} uses \code{seed + i}, and the
k-means step is seeded from it too.}

\item{.verbose}{Boolean or integer \code{0L}/\code{1L}/\code{2L}. Controls verbosity.}
}
\value{
The class with \code{final_results} populated from the consensus fit,
with the clustering diagnostics under \code{diagnostics} and the fit parameters
under \code{params$nmf_fit}.
}
\description{
Runs \code{n_runs} HALS-NMF restarts, pools their components, drops unstable ones
by local density, k-means clusters the survivors into \code{k} groups and refits
the partner factor against the per-cluster median. This is cNMF: the answer
is the structure the restarts agree on, and the mean silhouette of those
clusters (\code{stability}) tells you how much they agreed.

Prefer this over \code{\link[=stabilised_nmf_bulk]{stabilised_nmf_bulk()}}, which just picks the
lowest-loss restart and tells you nothing about whether that restart is
reproducible.
}
\details{
Use \code{\link[=nmf_k_sweep_bulk]{nmf_k_sweep_bulk()}} first if you do not already know \code{k}.

The density filter is the part that bites. Components whose mean cosine
distance to their neighbours exceeds \code{density_threshold} are dropped, and if
that leaves fewer than \code{k} survivors the fit errors rather than returning
something degenerate. With few restarts the filter is jumpy, so either raise
\code{n_runs} or set \code{density_threshold = 2} to switch it off.
}
\references{
Kotliar et al., eLife, 2019
}
