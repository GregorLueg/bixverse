% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/base_generics_sc.R
\name{consensus_nmf_sc}
\alias{consensus_nmf_sc}
\title{Run consensus NMF on single cell or meta cell data}
\usage{
consensus_nmf_sc(
  object,
  k,
  cell_ids = NULL,
  gene_ids = NULL,
  preprocessing = "none",
  use_second_layer = TRUE,
  nmf_hals_params = params_nmf_hals(),
  nmf_consensus_params = params_nmf_consensus(),
  n_runs = 30L,
  seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{object}{\code{SingleCells} or \code{MetaCells} class.}

\item{k}{Integer. Number of latent factors. Must be at least 2.}

\item{cell_ids}{Optional character. Cell ids (or meta cell ids) to restrict
the NMF to. If \code{NULL}, uses \code{\link[=get_cells_to_keep]{get_cells_to_keep()}} for \code{SingleCells} and all
meta cells for \code{MetaCells}.}

\item{gene_ids}{Optional character. Gene ids to restrict the NMF to. If
\code{NULL}, uses \code{\link[=get_hvg]{get_hvg()}} on the object.}

\item{preprocessing}{String. One of \code{c("none", "sd", "sqrt_sd")}.}

\item{use_second_layer}{Boolean. If \code{TRUE}, runs NMF on the normalised
counts (recommended); if \code{FALSE}, on the raw counts.}

\item{nmf_hals_params}{List, see \code{\link[=params_nmf_hals]{params_nmf_hals()}}. The \code{nmf_init} field is
ignored, restarts always use random initialisation.}

\item{nmf_consensus_params}{List, see \code{\link[=params_nmf_consensus]{params_nmf_consensus()}}.}

\item{n_runs}{Integer. Number of random restarts. Must be at least 2.}

\item{seed}{Integer. Base random seed. Restart \code{i} uses \code{seed + i}, and the
k-means step is seeded from it too.}

\item{.verbose}{Boolean or integer. Verbosity.}
}
\value{
A \code{ConsensusNmfResult} object.
}
\description{
Runs \code{n_runs} HALS NMF restarts, pools their components, drops unstable ones
by local density, k-means clusters the survivors into \code{k} groups and refits
the partner factor against the per-cluster median. This is cNMF: the answer
is the structure the restarts agree on, and the mean silhouette of those
clusters (\code{stability}) says how much they agreed.

Prefer this over \code{\link[=stabilised_nmf_sc]{stabilised_nmf_sc()}}, which picks the lowest-loss restart
and tells you nothing about whether that restart is reproducible.
}
\details{
Use \code{\link[=nmf_k_sweep_sc]{nmf_k_sweep_sc()}} first if you do not already know \code{k}.

Memory is the thing to watch. The restarts are dense and all live at once:
roughly \code{n_cells * k * n_runs} plus \code{n_runs * k * n_genes} floats on top of
the counts. At 200k cells, \code{k = 20} and \code{n_runs = 50} that is a few hundred
megabytes before anything else, which is exactly the regime where running on
\code{MetaCells} instead is the honest answer.

The density filter is the part that bites. If it leaves fewer than \code{k}
components the fit errors rather than returning something degenerate. With
few restarts the filter is jumpy, so either raise \code{n_runs} or set
\code{density_threshold = 2} in \code{\link[=params_nmf_consensus]{params_nmf_consensus()}} to switch it off.
}
\references{
Kotliar et al., eLife, 2019
}
