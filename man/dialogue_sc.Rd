% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/base_generics_sc.R
\name{dialogue_sc}
\alias{dialogue_sc}
\title{Find multicellular programmes with DIALOGUE}
\usage{
dialogue_sc(
  object,
  cell_type_col,
  sample_col,
  features,
  quality_col = NULL,
  gene_ids = NULL,
  pmd_params = params_dialogue_pmd(),
  hlm_params = params_dialogue_hlm(),
  refine_params = params_dialogue_refine(),
  .verbose = TRUE
)
}
\arguments{
\item{object}{\code{SingleCells}, \code{SingleCellsSubset} or \code{MetaCells} class.}

\item{cell_type_col}{String. Column in the obs table holding the cell type
labels.}

\item{sample_col}{String. Column in the obs table holding the sample labels.
The random intercept in stage two is over these. For \code{MetaCells} the meta
cells must have been built \emph{within} samples, otherwise the level is not
well-defined.}

\item{features}{Named list of numeric matrices, one per cell type. Names must
match the levels in \code{cell_type_col}, row names must cover that cell type's
cells, and each needs at least two columns. Rows are matched by name, not by
position.}

\item{quality_col}{Optional string. Column in the obs table to use as the
cell quality covariate. If \code{NULL}, defaults to the z-scored log library size.}

\item{gene_ids}{Optional character. Genes to consider when building
signatures. If \code{NULL}, uses \code{\link[=get_hvg]{get_hvg()}} on the object.}

\item{pmd_params}{List, see \code{\link[=params_dialogue_pmd]{params_dialogue_pmd()}}.}

\item{hlm_params}{List, see \code{\link[=params_dialogue_hlm]{params_dialogue_hlm()}}.}

\item{refine_params}{List, see \code{\link[=params_dialogue_refine]{params_dialogue_refine()}}.}

\item{.verbose}{Boolean or integer. Verbosity.}
}
\value{
A \code{DialogueResult} object.
}
\description{
DIALOGUE looks for programmes of cell-type-specific genes whose activity
covaries across the samples that several cell types share. Where
co-expression modules ask what varies together \emph{within} a cell type, this
asks what varies together \emph{between} them, sample by sample.
}
\details{
The algorithm works in three stages. Each cell type's features are  collapsed
to one row per sample and put through a sparse multi-CCA, giving  every
programme a weight vector per cell type plus a provisional gene signature.
Then, for every ordered pair of cell types and every candidate gene, a mixed
model asks whether a cell's own programme score tracks the partner's
expression of that gene in the same sample. Finally the partners are
meta-analysed and the scores refit onto the surviving genes by non-negative
least squares.

\code{features} is mandatory and there is no default. DIALOGUE does not compute
it: it is whatever low-dimensional description of each cell type you trust,
and the method is only as good as that choice. Do \emph{not} hand it a slice of a
global PCA. Those components mostly carry between-cell-type identity, which
is near-constant inside one cell type and gets dropped by the ANOVA filter,
leaving the decomposition to work off whatever is left. Run a PCA per cell
type instead, which for \code{SingleCells} means \code{\link[=SingleCellsSubset]{SingleCellsSubset()}} followed by
\code{\link[=calculate_pca_sc]{calculate_pca_sc()}} on each subset.

The method is unforgiving about study design, and the failure modes are
errors rather than bad answers. It needs at least two cell types, at least
five samples present in \emph{every} cell type, and enough cells per sample per
cell type to clear \code{abn_c} in \code{\link[=params_dialogue_pmd]{params_dialogue_pmd()}}. A handful of samples
with thousands of cells each is the regime it was built for; many samples
with a dozen cells each is not.
}
\references{
Jerby-Arnon & Regev, Nature Biotechnology, 2022
}
