% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_bulk.R
\name{.assert_bulk_detection_method}
\alias{.assert_bulk_detection_method}
\title{Guard on detection_method for a BulkCoExp method}
\usage{
.assert_bulk_detection_method(
  object,
  allowed,
  method_label,
  allow_unset = FALSE
)
}
\arguments{
\item{object}{The class, see \code{\link[=BulkCoExp]{BulkCoExp()}}.}

\item{allowed}{Character vector. Detection-method strings the caller accepts.}

\item{method_label}{String. Human-readable label used in the warning
message (e.g. \code{"correlation"}, \code{"ICA"}, \code{"DGRDL"}, \code{"NMF"}).}

\item{allow_unset}{Boolean. If \code{TRUE}, a \code{NULL} \code{detection_method} is not
an error; returns \code{NA_character_}. Used by finalisers that set
\code{detection_method} themselves on first invocation.}
}
\value{
One of:
\itemize{
\item the resolved \code{detection_method} string (proceed)
\item \code{NA_character_} (proceed, first invocation, only when
\code{allow_unset = TRUE})
\item \code{NULL} (mismatch, caller should return object unchanged)
}
}
\description{
Reads \code{detection_method} from the class params and checks it against the
set of methods the caller is designed to handle. Returns the resolved
method on success, \code{NULL} on mismatch (with a warning). If
\code{allow_unset = TRUE}, a \code{NULL} \code{detection_method} is treated as a first
invocation and returns \code{NA_character_} silently.

Caller pattern:
\preformatted{
detection_method <- .assert_bulk_detection_method(object, "correlation-based", "correlation")
if (is.null(detection_method)) return(object)
}
}
\keyword{internal}
