% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions_dge.R
\name{.nebula_design}
\alias{.nebula_design}
\title{Build the NEBULA design from an obs table}
\usage{
.nebula_design(obs, design, subject_col)
}
\arguments{
\item{obs}{data.table. The obs table of the selected cells, holding at least
the subject column and every variable in \code{design}.}

\item{design}{Formula. The experimental design, evaluated against \code{obs}.}

\item{subject_col}{String. The column of \code{obs} holding the subject
identifier, i.e. what the random effect is over.}
}
\value{
A list with \code{obs} (rows actually used), \code{design_mat} and
\code{subject_fct}.
}
\description{
Evaluates the design formula against the obs table of the selected cells (or
meta cells) and drops anything with a missing design or subject value. The
subject labels come back as a factor so the caller can map them onto the
full store.
}
\keyword{internal}
