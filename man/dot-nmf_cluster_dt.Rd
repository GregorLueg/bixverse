% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes_single_cell_others.R
\name{.nmf_cluster_dt}
\alias{.nmf_cluster_dt}
\title{Assemble the per-component consensus diagnostics}
\usage{
.nmf_cluster_dt(nmf_res, k, n_runs)
}
\arguments{
\item{nmf_res}{Named list. Raw consensus output, needs \code{labels},
\code{local_density}, \code{kept} and \code{silhouette}.}

\item{k}{Integer. Components per restart.}

\item{n_runs}{Integer. Number of restarts.}
}
\value{
A data.table with one row per pooled component and the columns
\code{component_id}, \code{run}, \code{component}, \code{pooled_idx}, \code{cluster},
\code{local_density}, \code{silhouette} and \code{kept}. Dropped components carry \code{NA} in
\code{cluster} and \code{silhouette}.
}
\description{
Turns the flat vectors the Rust side returns into one row per pooled
component. Rust pools component \code{j} of restart \code{r} at position \code{k * r + j},
which is the order \code{\link[=new_stabilised_nmf_result]{new_stabilised_nmf_result()}} builds its
\code{w_all} column names in, so \code{component_id} joins straight onto the columns
of \code{get_w()} on a stabilised fit.
}
\keyword{internal}
