% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_sc_dialogue.R
\name{.prep_dialogue_inputs}
\alias{.prep_dialogue_inputs}
\title{Resolve the per-cell-type inputs DIALOGUE needs}
\usage{
.prep_dialogue_inputs(
  object,
  obs,
  cell_type_col,
  sample_col,
  features,
  quality_col,
  cell_id_col,
  default_quality
)
}
\arguments{
\item{object}{\code{SingleCells}, \code{SingleCellsSubset} or \code{MetaCells} class.}

\item{obs}{data.table. The observation table to read the labels off.}

\item{cell_type_col}{String. Column holding the cell type labels.}

\item{sample_col}{String. Column holding the sample labels.}

\item{features}{Named list of numeric matrices, one per cell type.}

\item{quality_col}{Optional string. Column to use as the quality covariate.}

\item{cell_id_col}{String. Column holding the cell identifiers.}

\item{default_quality}{Numeric vector aligned to \code{obs} rows. Used when
\code{quality_col} is \code{NULL}, after a \code{log1p} and a z-score. Typically the library
size.}
}
\value{
A list with the following items
\itemize{
\item cell_type_indices - List of integer vectors. 0-indexed global cell
positions per cell type.
\item features - List of numeric matrices, rows reordered to match.
\item sample_ids - Integer vector. 0-indexed sample code per global cell.
\item cell_quality - Numeric vector. Quality covariate per global cell.
\item cell_types - Character vector. The analysed cell types, in order.
\item cell_ids - List of character vectors. Cell ids per cell type.
\item sample_levels - Character vector. Sample labels, position \code{i} being
sample code \code{i - 1}.
}
}
\description{
Turns the obs table plus the user's feature matrices into the flat, 0-indexed
vectors the Rust side takes. The cell types analysed are \code{names(features)},
in that order: anything else in \code{cell_type_col} is ignored, and that order is
what the pair columns of \code{emp_p} follow.
}
\details{
\code{sample_ids} and \code{cell_quality} are indexed by \emph{global} cell position, not by
position within a cell type, so both are scattered into vectors long enough
to cover the largest index in use. Cells belonging to no analysed cell type
leave holes, which Rust never reads.
}
\keyword{internal}
