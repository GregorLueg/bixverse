% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions_sc_cache_state.R
\name{.reset_sc_cells}
\alias{.reset_sc_cells}
\title{Restore every cell and clear the gene selection}
\usage{
.reset_sc_cells(x)
}
\arguments{
\item{x}{\code{SingleCells} or \code{SingleCellsMultiModal} class.}
}
\value{
The object with its map and DuckDB reset.
}
\description{
Sets \code{cells_to_keep_idx} back to \code{NULL} rather than materialising the full
index vector: \code{get_cells_to_keep()} already falls back to all cells on an
empty slot, so this is the genuine pristine state and costs nothing at a
million cells. The HVG selection goes too, since \code{\link[=find_hvg_sc]{find_hvg_sc()}} reads the
cell set and a selection made on a subset does not describe the whole.
}
\keyword{internal}
