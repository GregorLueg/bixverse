% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions_sc_residuals.R
\name{.residual_obs}
\alias{.residual_obs}
\title{Observation rows for the selected cells, in the selected order}
\usage{
.residual_obs(object, cell_indices)
}
\arguments{
\item{object}{\code{SingleCells} or \code{SingleCellsSubset} class.}

\item{cell_indices}{Integer. The 0-based cells, in the order Rust gets them.}
}
\value{
The observation table for those cells, row \code{i} being
\code{cell_indices[i]}.
}
\description{
The group labels and the covariates are matched to cells by position, so the
observation rows have to arrive in the same order as \code{cell_indices}.
\code{get_sc_obs()} reads from DuckDB without an \verb{ORDER BY}, so the order it
returns is not something to rely on. Reordering here is cheap and removes a
failure that would otherwise be silent: a model fitted against shuffled
covariates gives plausible residuals and a wrong embedding.
}
\keyword{internal}
