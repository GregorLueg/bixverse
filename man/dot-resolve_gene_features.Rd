% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_sc_plots_helpers.R
\name{.resolve_gene_features}
\alias{.resolve_gene_features}
\title{Resolve feature names to gene indices, keeping the two in step}
\usage{
.resolve_gene_features(object, features)
}
\arguments{
\item{object}{\code{SingleCells} or \code{SingleCellsSubset} class.}

\item{features}{Character vector. The requested gene ids.}
}
\value{
A list with \code{features}, the surviving gene ids in request order,
and \code{indices}, their 0-indexed positions for Rust.
}
\description{
\code{\link[=get_gene_indices]{get_gene_indices()}} warns and silently drops anything it cannot match, so
the caller is left holding a \code{features} vector longer than the indices it
got back. Anything that labels a Rust result by position has to use the
surviving names rather than the requested ones, otherwise the labels slide
off the values. This is the in-memory \code{.match_features()} for the streaming
paths.
}
\keyword{internal}
