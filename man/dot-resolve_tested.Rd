% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions_dge.R
\name{.resolve_tested}
\alias{.resolve_tested}
\title{Resolve what an edgeR-style test is testing}
\usage{
.resolve_tested(design, coef = NULL, contrast = NULL)
}
\arguments{
\item{design}{Numeric matrix. The design matrix of samples x coefficients.}

\item{coef}{Optional integer or character. The coefficient(s) to drop from
the null model.}

\item{contrast}{Optional numeric vector or matrix. Weights over the
coefficients, one entry (or row) per column of \code{design}.}
}
\value{
A named list holding either \code{coef} or \code{contrast} plus
\code{n_contrasts}.
}
\description{
Turns the user-facing \code{coef} / \code{contrast} pair into the elements the Rust
side reads off the parameter list. \code{coef} accepts 1-based column positions
or column names of the design and is shifted to the 0-based indices Rust
wants. \code{contrast} is flattened column-major with its column count alongside.

Supplying neither tests the last column of the design, which is what edgeR
and limma default to.
}
\keyword{internal}
