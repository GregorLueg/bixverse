% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions_sc_cache_state.R
\name{.sc_payload}
\alias{.sc_payload}
\title{Read an artefact payload straight out of a cache}
\usage{
.sc_payload(cache, artefact, name = NULL, wnn = FALSE)
}
\arguments{
\item{cache}{The cache list.}

\item{artefact}{String. One of \code{c("pca", "embedding", "knn", "snn", "magic")}.}

\item{name}{String. Embedding name, only used when \code{artefact = "embedding"}.}

\item{wnn}{Boolean. Whether \code{cache} is the \code{wnn} slot rather than an
\code{ScCache}.}
}
\value{
The payload, or \code{NULL}.
}
\description{
Raw slot access with no freshness checking and no stamp stripping. This is
what the provenance machinery reads; user facing code goes through the
getters.

The \code{wnn} pseudo modality stores its graph under \code{snn} and its embeddings
under \code{embeddings}, where a real \code{ScCache} uses \code{snn_graph} and
\code{other_embeddings}, hence the flag.
}
\keyword{internal}
