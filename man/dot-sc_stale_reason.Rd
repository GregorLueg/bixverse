% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions_sc_cache_state.R
\name{.sc_stale_reason}
\alias{.sc_stale_reason}
\title{Why an artefact is stale, if it is}
\usage{
.sc_stale_reason(label, artefacts, cells_hash, seen = character())
}
\arguments{
\item{label}{String. The artefact label to evaluate.}

\item{artefacts}{List as returned by \code{\link[=.sc_artefacts]{.sc_artefacts()}}.}

\item{cells_hash}{String. The object's current cell state hash.}

\item{seen}{Character vector. Labels already on the recursion stack.}
}
\value{
\code{NA_character_} when fresh (or unstamped), otherwise the reason.
}
\description{
Walks the provenance chain. An artefact is stale when the cell set moved
under it, when a parent it was built on no longer exists (i.e. was
recomputed and minted a new id), or when a parent is itself stale. The last
case is why this recurses: without it, re-running the PCA marks the kNN
stale but the sNN still resolves its untouched parent and passes.
}
\keyword{internal}
