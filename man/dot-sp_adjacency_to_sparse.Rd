% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_sp_analysis.R
\name{.sp_adjacency_to_sparse}
\alias{.sp_adjacency_to_sparse}
\title{Pack a spatial adjacency pair into a sparse matrix}
\usage{
.sp_adjacency_to_sparse(indices, weights, n_spots)
}
\arguments{
\item{indices}{List of integer vectors. Local 0-based neighbour indices.}

\item{weights}{List of numeric vectors. Aligned with \code{indices}.}

\item{n_spots}{Integer. Number of spots.}
}
\value{
A \code{dgCMatrix} of dimension \verb{n_spots x n_spots}.
}
\description{
The \code{SpCache} stores the spatial graph as a sparse matrix because that is
what a user wants to poke at. Entry \verb{(i, j)} is the weight of the directed
edge from spot \code{i} to spot \code{j}, exactly as \code{\link[=rs_sp_build_graph]{rs_sp_build_graph()}}
returned it, i.e. before any symmetrisation. Every downstream statistic
symmetrises internally.

Weights of exactly zero are not representable this way. No layout produces
them (binary weights are one, distance-based weights are positive), but a
gaussian bandwidth small enough to underflow would silently lose edges.
}
\keyword{internal}
