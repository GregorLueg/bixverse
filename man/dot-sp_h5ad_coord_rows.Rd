% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_sp_io_h5ad.R
\name{.sp_h5ad_coord_rows}
\alias{.sp_h5ad_coord_rows}
\title{Map the loaded obs rows back onto the rows of \code{obsm/spatial}}
\usage{
.sp_h5ad_coord_rows(obs_ids, all_ids, n_coords)
}
\arguments{
\item{obs_ids}{Character vector. \code{cell_id} of the loaded rows, in ascending
\code{cell_idx} order.}

\item{all_ids}{Character vector. Identifiers of every row on disk.}

\item{n_coords}{Integer. Rows in \code{obsm/spatial}.}
}
\value{
Integer vector of 1-based positions into \code{obsm/spatial}, one per
loaded obs row.
}
\description{
This is the load-bearing join of the h5ad reader, and it is the same trap
\code{\link[=load_visium]{load_visium()}} has with \code{tissue_positions.csv}.
\code{\link[=load_h5ad]{load_h5ad()}} drops cells that fail QC, so the obs table is a
subset of the file in file order while \code{obsm/spatial} still has every row.
Zipping the two by position silently shifts every coordinate from the first
dropped cell onwards and nothing downstream notices.
}
\keyword{internal}
