% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_sp_io_h5ad.R
\name{.sp_h5ad_obs_ids}
\alias{.sp_h5ad_obs_ids}
\title{Read the full obs identifier vector of an h5ad}
\usage{
.sp_h5ad_obs_ids(h5_path, cell_id_col = NULL)
}
\arguments{
\item{h5_path}{String. Path to the h5ad file.}

\item{cell_id_col}{Optional string. Obs column holding the identifiers, for
files that carry no \verb{_index}.}
}
\value{
Character vector, one entry per row of the on-disk obs table.
}
\description{
The identifiers of \strong{every} row on disk, before any QC filtering. Needed to
line \code{obsm/spatial} up with the obs table \code{\link[=load_h5ad]{load_h5ad()}} leaves
behind, which holds only the rows that survived.

Mirrors the precedence \code{\link[=SingleCellDuckDB]{SingleCellDuckDB()}} uses when it populates
\code{cell_id}: the \verb{_index} first, then a caller-named column.
}
\keyword{internal}
