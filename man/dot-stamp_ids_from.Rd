% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions_sc_cache_state.R
\name{.stamp_ids_from}
\alias{.stamp_ids_from}
\title{Resolve parent artefact names to their current stamp ids}
\usage{
.stamp_ids_from(x, from, modality = "rna")
}
\arguments{
\item{x}{The object owning the cache(s).}

\item{from}{Character vector of parent artefact names.}

\item{modality}{String. Modality to resolve unqualified names against.}
}
\value{
Character vector of stamp ids.
}
\description{
Producers pass parent \emph{names} (\code{"pca"}, \code{"umap"}) rather than ids, so they
never have to reach into the stamps themselves. A name may be modality
qualified (\code{"adt:pca"}); otherwise \code{modality} is assumed. Names that do not
resolve to a stamped artefact are dropped, which is what keeps a chain built
on top of a legacy artefact from claiming a provenance it does not have.
}
\keyword{internal}
