% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_sp_io.R
\name{.visium_in_tissue_allowlist}
\alias{.visium_in_tissue_allowlist}
\title{Resolve the in-tissue spots to mtx cell indices}
\usage{
.visium_in_tissue_allowlist(barcodes, positions, exp_id)
}
\arguments{
\item{barcodes}{Character vector of barcodes, in matrix column order.}

\item{positions}{\code{data.table} from \code{\link[=.read_visium_positions]{.read_visium_positions()}}.}

\item{exp_id}{String. The experiment identifier, for the error message.}
}
\value{
Integer vector of 0-based mtx cell indices with \code{in_tissue == 1}.
}
\description{
The allowlist the Rust mtx reader takes indexes the mtx file's own cell
axis, which for 10x output is the order of \code{barcodes.tsv.gz} and nothing
else. \code{tissue_positions.csv} is in array raster order, so this joins on the
barcode string and returns positions in the barcode vector, never positions
in the positions table.
}
\keyword{internal}
