% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_sp_io.R
\name{.visium_obs_dt}
\alias{.visium_obs_dt}
\title{Join a Visium positions table onto the barcode list}
\usage{
.visium_obs_dt(barcodes, positions, exp_id)
}
\arguments{
\item{barcodes}{Character vector of barcodes, in matrix column order.}

\item{positions}{\code{data.table} from \code{\link[=.read_visium_positions]{.read_visium_positions()}}.}

\item{exp_id}{String. The experiment identifier.}
}
\value{
A \code{data.table} with one row per barcode, in barcode order, carrying
the spatial columns and \code{exp_id}.
}
\description{
This is the load-bearing join of the whole reader. \code{barcodes.tsv.gz} is
lexicographically sorted, \code{tissue_positions.csv} is in array raster order.
They hold the same barcodes in different orders, so anything that zips the
two by row index scrambles every coordinate in the dataset and nothing
downstream notices. Match on the barcode string.
}
\keyword{internal}
