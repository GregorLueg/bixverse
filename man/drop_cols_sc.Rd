% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/base_generics_sc.R
\name{drop_cols_sc}
\alias{drop_cols_sc}
\title{Drop columns from the obs or var table}
\usage{
drop_cols_sc(object, table = c("obs", "var"), cols)
}
\arguments{
\item{object}{\code{SingleCells} (or other compatible) class.}

\item{table}{String. One of \code{c("obs", "var")}.}

\item{cols}{Character vector. Column names to drop.}
}
\value{
Invisible self.
}
\description{
Drops the named columns from the obs or var table of single cell-related
classes. Protected identifier and bookkeeping columns (\code{cell_idx},
\code{cell_id}, \code{to_keep} for obs; \code{gene_idx}, \code{gene_id} for var) are refused
with a warning. Columns that do not exist trigger a warning and are
skipped.
}
\examples{
# drop a column that is no longer needed
sc <- demo_single_cells(prepped = FALSE)
sc <- drop_cols_sc(sc, table = "obs", cols = "batch_index")
names(get_sc_obs(sc))

unlink(sc@dir_data, recursive = TRUE, force = TRUE)
}
