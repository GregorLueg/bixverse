% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions_single_cell.R
\name{extract_feature_pair}
\alias{extract_feature_pair}
\title{Extract a pair of features for scatter / hex plots}
\usage{
extract_feature_pair(
  object,
  feature_1,
  feature_2,
  obs_cols = NULL,
  scale = FALSE,
  clip = NULL,
  modality = c("rna", "adt")
)
}
\arguments{
\item{object}{A single cell class.}

\item{feature_1}{String. First feature, optionally \verb{_rna} / \verb{_adt} suffixed.}

\item{feature_2}{String. Second feature, optionally \verb{_rna} / \verb{_adt} suffixed.}

\item{obs_cols}{Optional character vector. Obs columns to attach (e.g. to
colour the scatter).}

\item{scale}{Boolean. Whether to z-score the expression values per feature.}

\item{clip}{Optional numeric. Clip z-scores if \code{scale = TRUE}.}

\item{modality}{String. Fallback modality for unsuffixed features. One of
\code{c("rna", "adt")}.}
}
\value{
A data.table with \code{cell_id}, \code{feature_1}, \code{feature_2} and any
requested obs columns. The original feature labels are stored in a
\code{features} attribute as \code{c(feature_1, feature_2)}.
}
\description{
Extracts two features into a wide data.table with \code{feature_1} and
\code{feature_2} value columns, ready for a scatter or hex plot. Each feature may
carry a \verb{_rna} or \verb{_adt} suffix to choose its modality independently (e.g.
\code{"ENSG00000167286_rna"} against \code{"CD3_adt"}); features without a suffix fall
back to \code{modality}. For \code{SingleCells} / \code{MetaCells} only RNA exists, so an
\verb{_adt} feature there errors via \code{\link[=extract_gene_expression]{extract_gene_expression()}}.
}
