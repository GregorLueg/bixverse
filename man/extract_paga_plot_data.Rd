% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions_single_cell.R
\name{extract_paga_plot_data}
\alias{extract_paga_plot_data}
\title{Extract the PAGA graph positioned on an embedding}
\usage{
extract_paga_plot_data(
  object,
  paga_res,
  embedding = "umap",
  cluster_col = NULL,
  threshold = 0.01,
  tree_only = FALSE,
  centroid = c("median", "mean"),
  node_stat_col = NULL,
  ...
)
}
\arguments{
\item{object}{A single cell class.}

\item{paga_res}{\code{PagaRes} class. The output of \code{\link[=run_paga_sc]{run_paga_sc()}}, run on this
object.}

\item{embedding}{String. Name of the embedding to position the nodes in.}

\item{cluster_col}{Optional string. The obs column holding the clustering.
Defaults to the one the PAGA run used, and errors if a different one is
given: nodes from one clustering with edges from another produce a plausible
looking graph that is wrong.}

\item{threshold}{Numeric. Edges below this connectivity are dropped.
Defaults to \code{0.01}.}

\item{tree_only}{Boolean. Use the maximum spanning forest rather than the
full abstracted graph. Defaults to \code{FALSE}.}

\item{centroid}{String. One of \code{c("median", "mean")}. How a cluster's
position is summarised. Median by default, since embeddings throw stragglers
that drag a mean off its cluster.}

\item{node_stat_col}{Optional string. A numeric obs column to summarise per
cluster with the same statistic, e.g. \code{"palantir_pseudotime"}, giving a
\code{stat} column to colour the nodes by.}

\item{...}{Additional arguments forwarded to \code{\link[=extract_embedding_data]{extract_embedding_data()}} and
onward to \code{\link[=get_embedding]{get_embedding()}} (e.g. \code{modality}).}
}
\value{
A list with the embedding stored as an \code{embedding} attribute and
\itemize{
\item nodes - data.table with \code{cluster} (a factor in graph order),
\code{dim_1}, \code{dim_2}, \code{n_cells} and, when \code{node_stat_col} is given, \code{stat}.
Clusters holding no cells are dropped, as they have no position.
\item edges - data.table with \code{from}, \code{to}, \code{weight} and the coordinates
\code{x}, \code{y}, \code{xend}, \code{yend} of both ends, ready for a segment layer. Each
edge appears once, despite the graph being stored symmetrically.
}
}
\description{
Puts every cluster of a \code{\link[=run_paga_sc]{run_paga_sc()}} result at the centroid of its cells
in an embedding and returns the abstracted graph as node and edge tables. A
node-link plot drawn from these sits where the reader already knows the
biology is, which a free layout of the abstracted graph does not.

The abstracted graph is close to complete on real data, so \code{threshold} is
doing real work. Drop it to zero and you get a hairball. \code{tree_only} is the
shortcut to the backbone.
}
\references{
Wolf, et al., Genome Biol., 2019.
}
