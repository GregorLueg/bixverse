% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_ontology.R
\name{filter_similarities}
\alias{filter_similarities}
\title{Filter the calculated similarities}
\usage{
filter_similarities(
  object,
  alpha,
  permutations = 100000L,
  seed = 10101L,
  .verbose = TRUE
)
}
\arguments{
\item{object}{\code{OntologySim} class. See \code{\link[=OntologySim]{OntologySim()}}.}

\item{alpha}{Float. The alpha value. For example, 0.001 would mean that the
critical value is smaller than 0.1 percentile of the random permutations.}

\item{permutations}{Number of random permutations.}

\item{seed}{Integer. For reproducibility purposes.}

\item{.verbose}{Boolean. Controls verbosity of the function.}
}
\value{
The class with filtered results added to the respective slot.
}
\description{
This function calculates the critical value, see
\code{\link[=calculate_critical_value]{calculate_critical_value()}} and filters subsequently all the
term pairs to the ones with a value ≥ critical value.
}
\examples{
# keep only term pairs above the permutation-derived critical value
onto <- data.table::data.table(
  parent = c("a", "b", "b", "b", "c"),
  child = c("b", "c", "d", "e", "f"),
  type = c("part_of", "part_of", "part_of", "is_a", "is_a")
)
onto_obj <- OntologySim(onto, .verbose = FALSE)
onto_obj <- calculate_wang_sim_onto(
  onto_obj,
  weights = c(part_of = 0.8, is_a = 0.6),
  .verbose = FALSE
)
onto_obj <- filter_similarities(onto_obj, alpha = 0.1, .verbose = FALSE)
head(get_results(onto_obj))
}
