% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_sc_dge_diffabund.R
\name{find_specific_markers_sc}
\alias{find_specific_markers_sc}
\title{Find markers that are specific to a cell group}
\usage{
find_specific_markers_sc(
  object,
  column_of_interest,
  reference_group = NULL,
  method = "wilcox",
  alternative = c("greater", "less", "twosided"),
  min_prop = 0.05,
  downsampling = TRUE,
  seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{object}{\code{SingleCells} or \code{SingleCellsSubset} class.}

\item{column_of_interest}{String. The column in the obs table holding the
groups, e.g. a cell type annotation or a clustering.}

\item{reference_group}{Optional string. The group to use as the reference. If
\code{NULL}, every group of \code{column_of_interest} is used as the reference in turn.}

\item{method}{String. Which method to use for the calculations of the DGE.
At the moment the only option is \code{"wilcox"}, but the parameter is reserved
for future features.}

\item{alternative}{String. Test alternative. One of
\code{c("twosided", "greater", "less")}. This function will default to
\code{"greater"}, i.e., genes upregulated in the reference group.}

\item{min_prop}{Numeric. The minimum proportion of cells that need to express
the gene in at least one of the groups. Applied once, globally, so every
comparison's FDR is calculated over the same gene set.}

\item{downsampling}{Boolean. If any group exceeds 100,000 cells, a random
subsample of 100,000 cells is used for it. The subsample is drawn once per
group, so a group is represented by the same cells in every arm.}

\item{seed}{Integer. Seed that is used for the downsampling.}

\item{.verbose}{Boolean or integer. Controls verbosity and returns run times.
\code{FALSE} -> quiet, \code{TRUE} or \code{1L} -> normal verbosity, \code{2L} -> detailed
verbosity.}
}
\value{
A \code{ScSpecificMarkers} class with the following elements
\itemize{
\item summary - data.table. Per gene and reference group, the summaries
across all rivals: \code{prop_ref}, \code{median_auroc}, \code{min_auroc}, \code{mean_auroc},
\code{max_auroc}, \code{worst_rival} (the rival achieving \code{min_auroc}), \code{min_rank}
(best AUROC rank the gene reaches against any single rival), the
Simes-combined p-value with its FDR, and the maximum p-value with its FDR.
\item per_comparison - data.table. Per gene, reference group and rival, the
underlying \code{auroc}, \code{lfc}, \code{prop_ref}, \code{prop_rival}, \code{z_scores}, \code{p_values}
and \code{fdr}.
\item params - List. The parameters the run used.
}
}
\description{
This function scores one group of cells against every other group of a column
separately, instead of against all of them pooled together. That difference
matters for markers. A pooled test is dominated by whichever rival
contributes the most cells, so a gene that is high in the reference and just
as high in one small rival still comes out looking like a clean marker. Here
the gene has to hold up against every rival, and the per-gene summaries
(\code{min_auroc}, \code{median_auroc}, \code{min_rank}) tell you whether it does.

Leave \code{reference_group} as \code{NULL} to run every group of the column as the
reference in turn, or name one group to only get that arm.

The summaries rank on AUROC rather than the p-value on purpose. Group sizes
vary a lot in practice and p-values scale with the group sizes, so a large
rival would otherwise crowd out a small one regardless of effect size.
}
\references{
Soneson and Robinson, Nat Methods, 2018; Lun, et al.,
F1000Research, 2016
}
