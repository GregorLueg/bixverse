% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/base_generics_sc.R
\name{fit_residuals_sc}
\alias{fit_residuals_sc}
\title{Fit a residual model for single cell data}
\usage{
fit_residuals_sc(
  object,
  method = c("sctransform", "analytic_pearson"),
  group_column = NULL,
  covariate_columns = NULL,
  residual_params = NULL,
  gene_batch_size = NULL,
  seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{object}{\code{SingleCells}, \code{SingleCellsSubset} or \code{MetaCells} class.}

\item{method}{String. One of \code{c("sctransform", "analytic_pearson")}.
scTransform fits a negative binomial per gene and regularises the
parameters; analytic Pearson is the closed-form alternative with one shared
dispersion, which is much cheaper.}

\item{group_column}{String or \code{NULL}. Column in the observation table to fit
separate models over, usually the sample. \code{NULL} fits one model.}

\item{covariate_columns}{Character vector or \code{NULL}. Numeric columns in the
observation table to add to the design. scTransform only. The library size
is never a covariate, it enters as a fixed offset.}

\item{residual_params}{List or \code{NULL}. Parameters, see
\code{\link[=params_sc_sctransform]{params_sc_sctransform()}} or \code{\link[=params_sc_apr]{params_sc_apr()}}. \code{NULL}
takes the defaults for \code{method}.}

\item{gene_batch_size}{Integer or \code{NULL}. Genes held in memory per batch.}

\item{seed}{Integer. Random seed for the step-1 subsample.}

\item{.verbose}{Boolean or Integer. Controls verbosity.}
}
\value{
The object with the fitted model attached.
}
\description{
Fits scTransform (v2) or the analytic Pearson residual model over the cells
currently kept. The fit lands in the cache, and
\code{\link[=find_hvg_sc]{find_hvg_sc()}} with \code{params_sc_hvg(method = "residual")},
\code{\link[=calculate_pca_sc]{calculate_pca_sc()}} with \code{residuals = TRUE} and
\code{\link[=sct_corrected_counts_sc]{sct_corrected_counts_sc()}} all read it from there.

Unlike the log-normalised layer, which is computed once at ingestion and
written to disk, nothing is precomputed here: the residual rows are
regenerated on demand by whatever consumes the fit.

With \code{group_column} one model is fitted per group. That is what a
multi-sample experiment wants, since each sample keeps its own depth and
composition, and it changes how the residual HVG selection behaves, see
\code{\link[=params_sc_hvg]{params_sc_hvg()}}.

For \code{MetaCells} the counts are summed UMIs, so the negative binomial still
applies, but at a much greater depth than a single cell. Prefer
\code{method = "analytic_pearson"} there, and revisit the defaults of
\code{\link[=params_sc_sctransform]{params_sc_sctransform()}}: \code{n_genes} and \code{n_cells} are sized for
raw cells, of which there are usually far more than meta cells.
}
\examples{
# analytic Pearson residuals over every kept cell
sc <- demo_single_cells(prepped = FALSE)
sc <- fit_residuals_sc(sc, method = "analytic_pearson", .verbose = FALSE)
get_residual_fit(sc)$n_groups

unlink(sc@dir_data, recursive = TRUE, force = TRUE)
}
\references{
Choudhary and Satija, Genome Biology, 2022; Lause, Berens and
Kobak, Genome Biology, 2021.
}
