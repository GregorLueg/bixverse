% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions_sc_lr.R
\name{generate_ligand_target_influence}
\alias{generate_ligand_target_influence}
\title{Generate the ligand to target influence matrix}
\usage{
generate_ligand_target_influence(
  ligand_seeds,
  ppi_network,
  grn_network,
  params = params_ligand_target()
)
}
\arguments{
\item{ligand_seeds}{List of character vectors. Each element is one
ligand symbol or a group treated as one complex (e.g.
\code{list(c("TGFB1", "TGFB2"))}). Optionally named; if unnamed, row names
default to the symbols joined with \code{+}.}

\item{ppi_network}{data.table with columns \code{from}, \code{to}, \code{weight}
(character, character, numeric). Protein-protein / signalling layer.}

\item{grn_network}{data.table with columns \code{from}, \code{to}, \code{weight}
(character, character, numeric). Gene regulatory layer.}

\item{params}{List. As returned by \code{\link[=params_ligand_target]{params_ligand_target()}}.}
}
\value{
A \code{LigandTargetInfluence} object.
}
\description{
Computes the NicheNet-style ligand to target gene influence matrix. Builds
the gene universe as the union of all symbols across both networks, remaps to
0-indexed integer node IDs for the Rust side, and wraps the resulting matrix
in a \code{LigandTargetInfluence} object.
}
