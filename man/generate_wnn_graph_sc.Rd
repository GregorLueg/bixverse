% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_sc_multimodal.R
\name{generate_wnn_graph_sc}
\alias{generate_wnn_graph_sc}
\title{Generate a weighted nearest neighbour (WNN) graph}
\usage{
generate_wnn_graph_sc(
  object,
  modality_1 = "rna",
  modality_2 = "adt",
  embd_to_use_1 = "pca",
  embd_to_use_2 = "pca",
  no_embd_to_use_1 = NULL,
  no_embd_to_use_2 = NULL,
  wnn_params = params_sc_wnn(),
  full_snn = TRUE,
  pruning = 1/15,
  snn_similarity = "jaccard",
  seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{object}{\code{SingleCellsMultiModal} class to which to add the WNN.}

\item{modality_1}{String. First modality. Defaults to \code{"rna"}.}

\item{modality_2}{String. Second modality. Defaults to \code{"adt"}.}

\item{embd_to_use_1}{String. The embedding to use for the first modality.
Must be available in the object for \code{modality_1}. Defaults to \code{"pca"}.}

\item{embd_to_use_2}{String. The embedding to use for the second modality.
Must be available in the object for \code{modality_2}. Defaults to \code{"pca"}.}

\item{no_embd_to_use_1}{Optional integer. Number of embedding dimensions to
use for \code{embd_to_use_1}. If \code{NULL}, all will be used.}

\item{no_embd_to_use_2}{Optional integer. Number of embedding dimensions to
use for \code{embd_to_use_2}. If \code{NULL}, all will be used.}

\item{wnn_params}{Named list. Controls the parameters for the WNN generation,
see \code{\link[=params_sc_wnn]{params_sc_wnn()}}.}

\item{full_snn}{Boolean. Shall the full shared nearest neighbour graph be
generated that generates edges between all cells instead of between only
neighbours.}

\item{pruning}{Numeric. Weights below this threshold will be set to 0 in the
generation of the sNN graph. Seurat uses for example 1/15 with k = 20.}

\item{snn_similarity}{String. One of \code{c("rank", "jaccard")}. The Jaccard
similarity calculates the Jaccard between the neighbours, whereas the rank
method calculates edge weights based on the ranking of shared neighbours. For
the rank method, the weight is determined by finding the shared neighbour
with the lowest combined rank across both cells, where lower-ranked (closer)
shared neighbours result in higher edge weights Both methods produce weights
normalised to the range \verb{⁠[0, 1]}⁠.}

\item{seed}{Integer. For reproducibility.}

\item{.verbose}{Boolean or integer. Controls verbosity and returns run times.
\code{FALSE} -> quiet, \code{TRUE} or \code{1L} -> normal verbosity, \code{2L} -> detailed
verbosity.}
}
\value{
The \code{SingleCellsMultiModal} object with the WNN graph and per-cell
modality weights added to \code{other_data[["wnn"]]}.
}
\description{
This function implements the approach from Hao et al., to generate a weighted
nearest neighbour graph given two 'omics modalities (for example RNA and
ADT). Per-cell modality weights are computed by comparing within- and
cross-modality neighbourhood distances, and these weights are used to fuse
the two modalities into a single multimodal kNN graph.

The per-modality kNN graphs are always recomputed internally from the chosen
embeddings at \code{knn_range} neighbours (see \code{\link[=params_sc_wnn]{params_sc_wnn()}}); the kNN graphs
previously stored on the object via \code{\link[=find_neighbours_sc]{find_neighbours_sc()}} are not
reused, as WNN requires a larger candidate pool than the default neighbour
search.

The result is stored in the object's \code{other_data} under \code{"wnn"}, containing
the fused kNN graph as a \code{SingleCellNearestNeighbour} (with a kernelised
pseudo-distance metric), the sNN graph as an igraph and a table of per-cell
modality weights. This graph can subsequently be used for clustering and 2D
embedding via the \code{"wnn"} modality.
}
\references{
Hao et al., Cell, 2021
}
