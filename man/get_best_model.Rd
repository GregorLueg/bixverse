% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes_lda.R
\name{get_best_model}
\alias{get_best_model}
\alias{get_best_model.LdaKSweepResult}
\title{Get the selected model from an LDA topic count sweep}
\usage{
get_best_model(x, k = NULL)

\method{get_best_model}{LdaKSweepResult}(x, k = NULL)
}
\arguments{
\item{x}{\code{LdaKSweepResult} object.}

\item{k}{Optional integer. The topic count to extract. If \code{NULL}, uses the
\code{best_k} the sweep selected.}
}
\value{
An \code{LdaResult}.
}
\description{
Returns the fit at \code{best_k}, or at a \code{k} you name, without refitting. The
sweep keeps every model it fitted.
}
\details{
\code{best_k} is never below five, see \code{\link[=lda_k_sweep]{lda_k_sweep()}}. Pass \code{k}
explicitly if the raw metrics point somewhere the selection could not go.
}
\examples{
# pull the selected fit out of a sweep without refitting
set.seed(42L)
corpus <- matrix(rbinom(200L * 40L, 1L, 0.05), nrow = 200L, ncol = 40L)
corpus[1:100, 1:10] <- rbinom(1000L, 1L, 0.6)
corpus[101:200, 11:20] <- rbinom(1000L, 1L, 0.6)
colnames(corpus) <- sprintf("term_\%02d", 1:40)
sweep_res <- lda_k_sweep(corpus > 0, k_range = 5:7, .verbose = FALSE)
dim(get_best_model(sweep_res))
}
