% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes_bulk.R
\name{get_factors}
\alias{get_factors}
\title{Get factor matrices from a BulkModuleResult}
\usage{
get_factors(object, which = NULL)
}
\arguments{
\item{object}{A \code{BulkModuleResult}.}

\item{which}{String or \code{NULL}. Name of the factor matrix to return. If
\code{NULL}, returns the whole list.}
}
\value{
A matrix, the named list of matrices, or \code{NULL} (with warning)
if \code{which} is not among the stored factor keys.
}
\description{
Returns one factor matrix by key, or the whole named list of factor
matrices if \code{which} is \code{NULL}. Keys are method-specific; see the
\code{factors} argument of \code{new_bulk_module_result()} for the shared vocabulary.
}
\examples{
# gene loadings and sample activities of an NMF fit
syn <- synthetic_bulk_cor_matrix()
mat <- log1p(t(syn$counts))
meta <- data.table::data.table(sample_id = rownames(mat))
object <- BulkCoExp(raw_data = mat, meta_data = meta)
object <- preprocess_bulk_coexp(object, hvg = 500L, .verbose = FALSE)
object <- nmf_bulk(object, k = 3L, .verbose = FALSE)
res <- S7::prop(object, "final_results")
names(get_factors(res))
dim(get_factors(res, "gene_loadings"))
}
