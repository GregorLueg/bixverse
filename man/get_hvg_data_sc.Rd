% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/base_generics_sc.R
\name{get_hvg_data_sc}
\alias{get_hvg_data_sc}
\title{Identify HVGs without mutating object state}
\usage{
get_hvg_data_sc(
  object,
  cell_ids = NULL,
  hvg_no = 3000L,
  hvg_params = params_sc_hvg(),
  streaming = NULL,
  .verbose = TRUE
)
}
\arguments{
\item{object}{\code{SingleCells} or \code{MetaCells} class.}

\item{cell_ids}{Optional character. Cell ids (or meta cell ids) to restrict
the HVG calculation to. If \code{NULL}, uses \code{\link[=get_cells_to_keep]{get_cells_to_keep()}} for
\code{SingleCells} and all meta cells for \code{MetaCells}.}

\item{hvg_no}{Integer. Number of top HVGs to flag. Defaults to \code{3000L}.}

\item{hvg_params}{List, see \code{\link[=params_sc_hvg]{params_sc_hvg()}}.}

\item{streaming}{Optional Boolean. Stream the data. Ignored for \code{MetaCells}.}

\item{.verbose}{Boolean or integer. Verbosity.}
}
\value{
data.table with \code{gene_idx}, \code{gene_id}, the HVG statistics returned
by the Rust HVG function, an \code{is_hvg} boolean and an \code{hvg_rank} integer
(\code{NA} for non-HVGs).
}
\description{
Like \code{\link[=find_hvg_sc]{find_hvg_sc()}} but does not mutate \code{object}. Returns a data.table
with per-gene HVG statistics plus \code{is_hvg}/\code{hvg_rank} for the top \code{hvg_no}
genes. Useful for computing HVGs on a subset of cells (e.g. a specific
cell type) for downstream methods like NMF, without overwriting the HVGs
stored on the object.
}
