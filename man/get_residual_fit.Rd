% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/base_generics_sc.R, R/classes_meta_cell.R,
%   R/classes_single_cell_.R
\name{get_residual_fit}
\alias{get_residual_fit}
\alias{get_residual_fit.MetaCells}
\alias{get_residual_fit.ScCache}
\alias{get_residual_fit.SingleCells}
\title{Get the fitted residual model}
\usage{
get_residual_fit(x, ...)

## S7 method for class <bixverse::MetaCells>
get_residual_fit(x, ...)

\method{get_residual_fit}{ScCache}(x, ...)

## S7 method for class <bixverse::SingleCells>
get_residual_fit(x, ...)
}
\arguments{
\item{x}{An object to get the fitted model from.}

\item{...}{Other parameters.}
}
\value{
The \code{ScResidualFit} object, or \code{NULL} when nothing was fitted.
}
\description{
Returns the \code{ScResidualFit} written by \code{\link[=fit_residuals_sc]{fit_residuals_sc()}}. This
function is used for the single cell-related classes and methods.

Warns and returns \code{NULL} when nothing was fitted, so it is safe to use as a
presence probe. The functions that compute with the fit assert instead.
}
\examples{
# the fitted model and the genes it covers
sc <- demo_single_cells()
sc <- fit_residuals_sc(sc, method = "analytic_pearson", .verbose = FALSE)
length(get_residual_fit(sc)$genes)

unlink(sc@dir_data, recursive = TRUE, force = TRUE)
}
