% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes_single_cell_others.R
\name{identify_tf_to_genes}
\alias{identify_tf_to_genes}
\alias{identify_tf_to_genes.ScenicGrn}
\title{Identify the TF to gene regulation}
\usage{
identify_tf_to_genes(
  x,
  method = c("threshold", "top_k"),
  k_tfs = NULL,
  k_genes = NULL,
  n_sd = 2,
  min_importance = NULL,
  .verbose = TRUE
)

\method{identify_tf_to_genes}{ScenicGrn}(
  x,
  method = c("threshold", "top_k"),
  k_tfs = NULL,
  k_genes = NULL,
  n_sd = 2,
  min_importance = NULL,
  .verbose = TRUE
)
}
\arguments{
\item{x}{\code{ScenicGrn} object.}

\item{method}{Character. Either \code{"top_k"} or \code{"threshold"}.}

\item{k_tfs}{Optional integer. Top TFs per gene (only used when
\code{method = "top_k"}).}

\item{k_genes}{Optional integer. Top genes per TF (only used when
\code{method = "top_k"}).}

\item{n_sd}{Numeric. Number of standard deviations above the per-gene mean
to use as the threshold (only used when \code{method = "threshold"}). Default
is \code{2}.}

\item{min_importance}{Optional numeric in [0, 1]. Absolute minimum
importance score for inclusion.}

\item{.verbose}{Boolean. Controls verbosity of the function.}
}
\value{
The \code{ScenicGrn} object with a \code{tf_to_gene_results} data.table
added.
}
\description{
Generates the TF to gene associations from the importance matrix produced
by the tree-based regression models. Two filtering strategies are available:

\itemize{
\item \strong{Threshold} (\code{method = "threshold"}): For each gene, computes
mean + \code{n_sd} * SD of the importance scores across all TFs and retains
only pairs exceeding that threshold. This adapts to the per-gene
importance distribution and is less sensitive to differences between
learners.
\item \strong{Top-k} (\code{method = "top_k"}): Selects the top \code{k_tfs} TFs per
gene and/or the top \code{k_genes} genes per TF. Both margins can be combined
(union). At least one of \code{k_tfs} or \code{k_genes} must be provided.
}
Both methods accept an optional \code{min_importance} floor.
}
