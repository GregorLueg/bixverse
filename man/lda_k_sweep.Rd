% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions_lda.R
\name{lda_k_sweep}
\alias{lda_k_sweep}
\title{Sweep the topic count for LDA}
\usage{
lda_k_sweep(
  x,
  k_range,
  lda_params = params_lda(),
  top_topics_coh = 5L,
  seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{x}{Documents x terms matrix. Either a \code{dgCMatrix}, a \code{dgRMatrix}, or
a dense numeric or logical matrix. Column names are required, row names are
generated if absent.}

\item{k_range}{Integer vector. The topic counts to evaluate. Every entry
must be at least 2.}

\item{lda_params}{List, see \code{\link[=params_lda]{params_lda()}}.}

\item{top_topics_coh}{Integer. Number of top-scoring topics averaged into
the reported coherence.}

\item{seed}{Integer. Seed for the variational initialisation.}

\item{.verbose}{Boolean or integer. Verbosity.}
}
\value{
An \code{LdaKSweepResult}, which is a data.table with one row per \code{k}.
}
\description{
Fits one model per requested topic count and scores each with three model
selection metrics, then combines them into a single rescaled score. Use it
to pick \code{k}, then pull the winning model out with
\code{\link[=get_best_model]{get_best_model()}}.
}
\details{
The three metrics disagree by design and are meant to be looked at together.
Arun is a symmetric KL between the singular values of the topic-term matrix
and the document length distribution (lower is better). Cao Juan is the mean
pairwise cosine similarity between topics, so it punishes a \code{k} that has
started splitting one signal in two (lower is better). Mimno is UMass
coherence (higher is better), averaged over the top-scoring topics only,
because coherence over all topics is dragged down by the ones that never
specialised. \code{combined_score} is the min-max rescaled compromise, and
\code{best_k} is its argmax.

One thing to know before reading \code{best_k}: any \code{k} below five is struck out
of the selection entirely, because coherence saturates on small topic counts
and would otherwise always win. That is upstream behaviour, inherited from
pycisTopic. So \code{best_k} can never come back below five, however good the raw
metrics of a smaller \code{k} look, and a sweep over \code{2:6} is really a choice
between five and six. The excluded rows carry \code{NA} in \code{combined_score}, keep
their metrics, and trigger a warning. If you suspect a handful of topics,
read \code{arun_2010} and \code{cao_juan_2009} off the table yourself and pass the \code{k}
you want to \code{\link[=get_best_model]{get_best_model()}}.

Cost is one full fit per \code{k}. The corpus is built once and shared, and each
fit is already parallel over documents, so the topic counts run sequentially
rather than nesting thread pools.
}
\references{
Arun, et al., PAKDD, 2010; Cao, et al., Neurocomputing, 2009;
Mimno, et al., EMNLP, 2011
}
