% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_sc_io.R
\name{load_h5ad}
\alias{load_h5ad}
\title{Load in h5ad to \code{SingleCells}}
\usage{
load_h5ad(
  object,
  h5_path,
  sc_qc_param = params_sc_min_quality(),
  streaming = 1L,
  raw_count_slot = c("X", "raw.X"),
  cell_id_col = NULL,
  batch_size = 1000L,
  max_genes_in_memory = 2000L,
  cell_batch_size = 100000L,
  .verbose = TRUE
)
}
\arguments{
\item{object}{\code{SingleCells} class.}

\item{h5_path}{File path to the h5ad object you wish to load in.}

\item{sc_qc_param}{List. Output of \code{\link[=params_sc_min_quality]{params_sc_min_quality()}}. A
list with the following elements:
\itemize{
\item min_unique_genes - Integer. Minimum number of genes to be detected
in the cell to be included.
\item min_lib_size - Integer. Minimum library size in the cell to be
included.
\item min_cells - Integer. Minimum number of cells a gene needs to be
detected to be included.
\item target_size - Float. Target size to normalise to. Defaults to \code{1e5}.
}}

\item{streaming}{Integer. \code{0L} -> all cells loaded in memory then transposed
(fastest, highest memory), \code{1L} -> light streaming with cell batching, \code{2L}
-> heavy streaming with memory upper boundaries on the gene side. Controls
memory pressure during the CSR-to-CSC conversion. Defaults to \code{1L}.}

\item{raw_count_slot}{String. One of \code{c("X", "raw.X")}. Where to find the
raw counts.}

\item{cell_id_col}{Optional string. If a specific column in the h5ad obs
data is representing the cell identifiers, you can specify it here.}

\item{batch_size}{Integer. Cell batch size when \code{streaming = 1L}. Defaults
to \code{1000L}.}

\item{max_genes_in_memory}{Integer. Maximum genes held in memory at once
when \code{streaming = 2L}. Defaults to \code{2000L}.}

\item{cell_batch_size}{Integer. Cell batch size when \code{streaming = 2L}.
Defaults to \code{100000L}.}

\item{.verbose}{Boolean. Controls the verbosity of the function.}
}
\value{
It will populate the files on disk and return the class with updated
shape information.
}
\description{
This function takes an h5ad file and loads the obs and var data into the
DuckDB of the \code{SingleCells} class and the counts into a Rust-binarised
format for rapid access. During the reading in of the counts, the log CPM
transformation will occur automatically.
}
