% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_sc_io.R
\name{load_multi_h5ad}
\alias{load_multi_h5ad}
\title{Load multiple h5ad files into a single \code{SingleCells}}
\usage{
load_multi_h5ad(
  object,
  prescan_result,
  sc_qc_param = params_sc_min_quality(),
  cell_id_col = NULL,
  streaming = 1L,
  batch_size = 1000L,
  max_genes_in_memory = 2000L,
  cell_batch_size = 100000L,
  .verbose = TRUE
)
}
\arguments{
\item{object}{\code{SingleCells} class.}

\item{prescan_result}{Output of \code{\link[=prescan_h5ad_files]{prescan_h5ad_files()}}.}

\item{sc_qc_param}{List. Output of \code{\link[=params_sc_min_quality]{params_sc_min_quality()}}.}

\item{cell_id_col}{Optional string. Column name for cell identifiers in obs.}

\item{streaming}{Integer. \code{0L} -> in-memory, \code{1L} -> light streaming, \code{2L}
-> heavy streaming with memory upper boundaries. Defaults to \code{1L}.}

\item{batch_size}{Integer. Cell batch size when \code{streaming = 1L}. Defaults
to \code{1000L}.}

\item{max_genes_in_memory}{Integer. Maximum genes held in memory at once
when \code{streaming = 2L}. Defaults to \code{2000L}.}

\item{cell_batch_size}{Integer. Cell batch size when \code{streaming = 2L}.
Defaults to \code{100000L}.}

\item{.verbose}{Boolean.}
}
\value{
The class with updated shape and populated DuckDB.
}
\description{
Takes a pre-scan result from \code{\link[=prescan_h5ad_files]{prescan_h5ad_files()}} and loads
all files into a single experiment with global gene QC and sequential
cell indexing.
}
