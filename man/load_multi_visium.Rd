% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_sp_io.R
\name{load_multi_visium}
\alias{load_multi_visium}
\title{Load multiple Visium directories into a single \code{SpatialSpot}}
\usage{
load_multi_visium(
  object,
  prescan_result,
  sp_io_param = params_sp_visium_io(),
  sc_qc_param = params_sc_min_quality(),
  copy_images = TRUE,
  streaming = 1L,
  .verbose = TRUE
)
}
\arguments{
\item{object}{\code{SpatialSpot} class.}

\item{prescan_result}{Output of \code{\link[=prescan_visium_dirs]{prescan_visium_dirs()}}.}

\item{sp_io_param}{List. Output of \code{\link[=params_sp_visium_io]{params_sp_visium_io()}}.
\code{matrix_type} was already resolved during the prescan and is ignored here.
\code{slide_file} has no meaning across several inputs and must be \code{NULL}.}

\item{sc_qc_param}{List. Output of \code{\link[=params_sc_min_quality]{params_sc_min_quality()}}.}

\item{copy_images}{Boolean. Copy the images of every input into
\verb{<dir_data>/images/<exp_id>/}. Defaults to \code{TRUE}.}

\item{streaming}{Integer. CSR-to-CSC conversion mode. \code{0L} -> in-memory,
\code{1L} -> light streaming, \code{2L} -> heavy streaming. Defaults to \code{1L}.}

\item{.verbose}{Boolean. Controls the verbosity of the function.}
}
\value{
The class with updated shape, a populated DuckDB and one
\code{SpatialSample} per input.
}
\description{
Takes the result of \code{\link[=prescan_visium_dirs]{prescan_visium_dirs()}} and loads all inputs
into one experiment with global gene QC and sequential spot indexing. The
feature space is the \strong{intersection} of input gene IDs. Each input is
registered as its own \code{SpatialSample}.
}
\section{In-tissue filtering}{

Unlike \code{\link[=load_visium]{load_visium()}}, this path still applies \code{in_tissue_only}
through the \code{to_keep} flag after the counts are written. The multi-file
reader has no cell allowlist, so global gene QC across inputs sees the
off-tissue counts.
}

