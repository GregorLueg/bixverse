% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_sc_io.R
\name{load_seurat}
\alias{load_seurat}
\title{Load in Seurat to \code{SingleCells}}
\usage{
load_seurat(
  object,
  seurat,
  sc_qc_param = params_sc_min_quality(),
  streaming = 1L,
  batch_size = 1000L,
  max_genes_in_memory = 2000L,
  cell_batch_size = 100000L,
  .verbose = TRUE
)
}
\arguments{
\item{object}{\code{SingleCells} class.}

\item{seurat}{\code{Seurat} class you want to transform.}

\item{sc_qc_param}{List. Output of \code{\link[=params_sc_min_quality]{params_sc_min_quality()}}. A
list with the following elements:
\itemize{
\item min_unique_genes - Integer. Minimum number of genes to be detected
in the cell to be included.
\item min_lib_size - Integer. Minimum library size in the cell to be
included.
\item min_cells - Integer. Minimum number of cells a gene needs to be
detected to be included.
\item target_size - Float. Target size to normalise to. Defaults to \code{1e5}.
}}

\item{streaming}{Integer. CSR-to-CSC conversion mode. \code{0L} -> in-memory
(fastest, highest memory), \code{1L} -> light streaming with cell batching,
\code{2L} -> heavy streaming with memory upper boundaries. Defaults to \code{1L}.}

\item{batch_size}{Integer. Cell batch size when \code{streaming = 1L}. Defaults
to \code{1000L}.}

\item{max_genes_in_memory}{Integer. Maximum genes held in memory at once
when \code{streaming = 2L}. Defaults to \code{2000L}.}

\item{cell_batch_size}{Integer. Cell batch size when \code{streaming = 2L}.
Defaults to \code{100000L}.}

\item{.verbose}{Boolean. Controls the verbosity of the function.}
}
\value{
It will populate the files on disk and return the class with updated
shape information.
}
\description{
This function takes a Seurat object and generates a \code{SingleCells} class
from it. The raw counts are extracted, written to the Rust binary format,
and the metadata is loaded into the DuckDB.
}
\examples{
\dontshow{if (requireNamespace("Seurat", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
# a Seurat object holds genes x cells, the loader transposes it
data <- generate_single_cell_test_data(
  syn_data_params = params_sc_synthetic_data(n_cells = 200L, n_genes = 40L)
)
seurat_obj <- Seurat::CreateSeuratObject(
  counts = Matrix::t(data$counts),
  meta.data = data.frame(data$obs, row.names = data$obs$cell_id)
)
dir_data <- tempfile("sc_seurat")
dir.create(dir_data, recursive = TRUE)
sc <- load_seurat(
  object = SingleCells(dir_data = dir_data),
  seurat = seurat_obj,
  sc_qc_param = params_sc_min_quality(
    min_unique_genes = 5L,
    min_lib_size = 25L,
    min_cells = 5L
  ),
  streaming = 0L,
  .verbose = FALSE
)
dim(sc)

unlink(dir_data, recursive = TRUE, force = TRUE)
}
\dontshow{\}) # examplesIf}
}
