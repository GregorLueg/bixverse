% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions_meta_cell.R
\name{merge_meta_cells}
\alias{merge_meta_cells}
\title{Merge meta cell objects into one}
\usage{
merge_meta_cells(
  inputs,
  source_ids = NULL,
  feature_space = c("intersect", "union"),
  prefix_ids = TRUE,
  .verbose = TRUE
)
}
\arguments{
\item{inputs}{List of \code{MetaCells} objects.}

\item{source_ids}{Optional character vector of the same length as \code{inputs}
with the source (e.g. patient) identifiers. Defaults to \code{names(inputs)} and
falls back to \code{source_01}, \code{source_02}, ... Needs to be unique.}

\item{feature_space}{String. One of \code{c("intersect", "union")}. Controls how
differing gene spaces are resolved. With \code{"union"} genes missing from an
input become structural zeros for its meta cells. Irrelevant when all inputs
came from the same source object, as their gene spaces are then identical.}

\item{prefix_ids}{Boolean. Prefix the meta cell identifiers with the source
identifier. If \code{FALSE}, duplicated identifiers across inputs are an error.}

\item{.verbose}{Boolean. Controls verbosity of the function.}
}
\value{
A single \code{MetaCells} object with all meta cells of the inputs. The
observation table gains a \code{source_id} column; \code{original_cell_idx} stays in
the index space of its own source.
}
\description{
Row-binds several \code{\link[=MetaCells]{MetaCells()}} objects into a single one. The
typical use case is sample-pure meta cells: generate meta cells per patient
(see \code{\link[=meta_cells_per_group]{meta_cells_per_group()}}), then merge them so that methods like SCENIC,
AUCell or NMF can run across the full set.

The normalised counts are carried over as generated. They are normalised
per meta cell, so row-binding leaves them valid. Caches (PCA, kNN, sNN,
embeddings) are per source and are dropped; recompute them on the merged
object.
}
