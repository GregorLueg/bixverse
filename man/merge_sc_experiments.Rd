% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_sc_obj_manipulation.R
\name{merge_sc_experiments}
\alias{merge_sc_experiments}
\title{Merge multiple \code{SingleCells} experiments into one}
\usage{
merge_sc_experiments(
  target,
  inputs,
  exp_ids,
  renormalise = FALSE,
  sc_qc_param = params_sc_min_quality(),
  streaming = 1L,
  batch_size = 1000L,
  max_genes_in_memory = 2000L,
  cell_batch_size = 100000L,
  .verbose = TRUE
)
}
\arguments{
\item{target}{A freshly constructed \code{SingleCells} pointing at the output
directory.}

\item{inputs}{List of \code{SingleCells} objects to merge. Length >= 2.}

\item{exp_ids}{Character vector of experiment identifiers, one per input.
Must be unique.}

\item{renormalise}{Boolean. Whether to recompute \code{data_norm} against
\code{sc_qc_param$target_size}. Defaults to \code{FALSE}.}

\item{sc_qc_param}{List. Output of \code{\link[=params_sc_min_quality]{params_sc_min_quality()}}.
Only \code{target_size} is consulted here; no QC filtering is applied during
merge.}

\item{streaming}{Integer. \code{0} -> no streaming, \code{1} -> light streaming, \code{2} ->
heavy streaming with memory upper boundaries. This enables you to control
the memory pressure during ingestion.}

\item{batch_size}{Integer. Batch size when \code{streaming = 1L}.}

\item{max_genes_in_memory}{Integer. How many genes shall be held in memory
at a given point. Defaults to \code{2000L}. Only relevant if streaming is set to
\code{2}.}

\item{cell_batch_size}{Integer. How big are the batch sizes for the cells
in the transformation from the cell-based to gene-based format. Defaults to
\code{100000L}. Only relevant if streaming is set to \code{2}.}

\item{.verbose}{Boolean.}
}
\value{
The populated target \code{SingleCells}.
}
\description{
Merges N existing \code{SingleCells} objects into a freshly constructed target
object. The feature space of the result is the \strong{intersection} of the input
gene sets. Each input's \code{cells_to_keep} filter is honoured (i.e. only cells
with \code{to_keep = TRUE} in the input's obs are carried over).

If \code{renormalise = FALSE}, the stored \code{data_norm} values are copied through
unchanged. This is valid only when all inputs were normalised against the
same \code{target_size}. If the gene intersection is much smaller than the
individual input gene sets, the inherited \code{data_norm} becomes a lossy
approximation (it was computed against the pre-intersection library size). In
that case set \code{renormalise = TRUE} to recompute \code{data_norm} against the
surviving raw counts using \code{sc_qc_param$target_size}.

Obs columns are intersected across inputs. The result obs gains an \code{exp_id}
column. Inputs that already have an \code{exp_id} column are rejected. The
\code{sc_cache} and \code{sc_map} of the target are populated fresh; any PCA, kNN, sNN
or HVG state on the inputs is not carried over and must be re-run.
}
