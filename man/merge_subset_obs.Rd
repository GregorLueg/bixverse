% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes_single_cell_subset.R
\name{merge_subset_obs}
\alias{merge_subset_obs}
\title{Merge obs columns from subsets back into the parent object}
\usage{
merge_subset_obs(
  object,
  subsets,
  cols = NULL,
  new_names = NULL,
  prefix_values = FALSE,
  overwrite = FALSE,
  .verbose = TRUE
)
}
\arguments{
\item{object}{\code{SingleCells} class. The parent the subsets were derived from.}

\item{subsets}{A \code{SingleCellsSubset} or a list of them. Every element must
originate from \code{object}; this is verified against the cell names.}

\item{cols}{Optional character vector. Columns in the subset obs tables to
merge. If \code{NULL}, every column that is present in all subsets but absent
from the parent obs table is taken, which after a pipeline run is exactly
the set of newly generated columns.}

\item{new_names}{Optional character vector. Names to give the merged columns
in the parent obs table. Same length as \code{cols}, which must be given
explicitly if this is used.}

\item{prefix_values}{Boolean. Prefix every merged value with the subset's
group, i.e. \code{"<group>_<value>"}. Coerces the columns to character, so this
only makes sense for discrete labels. Needed when merging a list of subsets
into a shared column, since sub-cluster \code{1} of one group and sub-cluster \code{1}
of another are otherwise indistinguishable.}

\item{overwrite}{Boolean. Allow merged columns to replace existing columns
of the same name in the parent obs table. Defaults to \code{FALSE}, in which case
a name clash is an error.}

\item{.verbose}{Boolean. Controls verbosity.}
}
\value{
The parent \code{SingleCells} object with the merged columns added to
the obs table in the DuckDB.
}
\description{
Takes columns computed on one or more \code{\link[=SingleCellsSubset]{SingleCellsSubset()}}
objects and writes them into the parent's obs table in the DuckDB, joining
on \code{cell_idx}. The join is a left join, so every parent cell that is not
part of any of the provided subsets ends up as \code{NA}.

Pass a single subset or a list of them, e.g. the output of
\code{\link[=apply_pipeline_per_group]{apply_pipeline_per_group()}}. All subsets are written in a single join, and
the function refuses to run if two subsets claim the same cell.
}
\seealso{
\code{\link[=add_sc_new_obs]{add_sc_new_obs()}} for the equivalent on result objects that carry
their own \code{cell_idx}, such as \code{\link[=fast_cluster_sc]{fast_cluster_sc()}}.
}
