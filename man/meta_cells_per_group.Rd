% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipelines_sc.R
\name{meta_cells_per_group}
\alias{meta_cells_per_group}
\title{Generate source-pure meta cells and merge them}
\usage{
meta_cells_per_group(
  object,
  group_col,
  method = c("bootstrapped", "seacells", "supercells"),
  mc_params = list(),
  pipeline = NULL,
  groups = NULL,
  feature_space = c("intersect", "union"),
  .verbose = TRUE
)
}
\arguments{
\item{object}{\code{SingleCells}.}

\item{group_col}{String. Column in obs used to split.}

\item{method}{String. One of \code{c("bootstrapped", "seacells", "supercells")}.
Picks the meta cell generator.}

\item{mc_params}{Named list. Arguments passed on to the generator, e.g.
\code{list(sc_meta_cell_params = params_sc_bt_metacells(), target_size = 1e5)}.}

\item{pipeline}{Optional \code{ScPipeline} applied to each subset before the meta
cells are generated.}

\item{groups}{Optional character vector. Restrict to these group values; if
\code{NULL}, all unique values of \code{group_col} are used.}

\item{feature_space}{String. One of \code{c("intersect", "union")}. Passed to
\code{\link[=merge_meta_cells]{merge_meta_cells()}}. Irrelevant here as all groups share the gene space of
the parent object.}

\item{.verbose}{Boolean. Controls verbosity of the function.}
}
\value{
A merged \code{\link[=MetaCells]{MetaCells()}} object with a \code{source_id} column
in its observation table.
}
\description{
Splits \code{object} by \code{group_col}, optionally runs \code{pipeline} on each subset,
generates meta cells per group and merges the results into a single
\code{\link[=MetaCells]{MetaCells()}} object. This gives you meta cells that never mix
cells from two patients/samples, while still being one object you can run
SCENIC, AUCell or NMF over.

The meta cell generators need an embedding, so \code{pipeline} will normally be
\code{step_hvg_sc() \%>>\% step_pca_sc() \%>>\% step_neighbours_sc()} unless every
subset already carries one.
}
\examples{
\dontrun{
prep <- step_hvg_sc() \%>>\% step_pca_sc(no_pcs = 20L) \%>>\% step_neighbours_sc()
mc <- meta_cells_per_group(
  object = sc_obj,
  group_col = "patient_id",
  method = "bootstrapped",
  pipeline = prep
)
}
}
