% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_modules.R
\name{modules_from_loadings}
\alias{modules_from_loadings}
\title{Derive sparse module membership from a loading matrix}
\usage{
modules_from_loadings(loadings, membership_params = params_module_membership())
}
\arguments{
\item{loadings}{Numeric matrix. \verb{gene x k}, with row and column names.}

\item{membership_params}{List. See
\code{\link[=params_module_membership]{params_module_membership()}}.}
}
\value{
A data.table with columns \code{gene}, \code{module_id}, \code{loading}, \code{sign} and
the per-component score (\code{z} or \code{padj} depending on the method). One row per
surviving (gene, component) pair, ordered by component then by descending
absolute loading.
}
\description{
Turns a \verb{gene x k} loading matrix from a matrix factorisation (ICA, NMF,
DGRDL) into a membership table by keeping the tails of each component's
loading distribution.

Two thresholding rules, selected via \code{membership_params}:
\itemize{
\item \code{"zscore"} - standardisation per component, keeping \code{abs(z) > cutoff}. No distributional assumption beyond rough symmetry.
\item \code{"fdr"} - two-sided p-values against a Normal null fitted per
component, Benjamini-Hochberg adjusted, keeping \code{padj < fdr}.
}

The standardisation itself is controlled by \code{membership_params$scaling}:
\code{"robust"} centres and scales by the median and MAD, \code{"standard"} by the
mean and standard deviation. The latter is stricter and keeps fewer genes
on skewed loadings, which is common for NMF.
}
\examples{
# turn NMF gene loadings into module membership
syn <- generate_gene_module_data(n_samples = 24L, n_genes = 60L)
# NMF needs a non-negative matrix
mat <- syn$data - min(syn$data)
obj <- BulkCoExp(mat, syn$meta_data)
obj <- preprocess_bulk_coexp(
  obj, hvg = NULL, scaling = FALSE, .verbose = FALSE
)
obj <- nmf_bulk(obj, k = 4L, .verbose = FALSE)
head(modules_from_loadings(get_nmf_gene_loadings(obj)))
}
