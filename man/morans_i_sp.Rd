% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/base_generics_spatial.R
\name{morans_i_sp}
\alias{morans_i_sp}
\title{Detect spatially variable genes with Moran's I}
\usage{
morans_i_sp(
  object,
  exp_id = NULL,
  genes = NULL,
  svg_params = params_sp_svg(),
  streaming = TRUE,
  .verbose = TRUE
)
}
\arguments{
\item{object}{A \code{\link[=SpatialSpot]{SpatialSpot()}} or
\code{\link[=SpatialSpotSubset]{SpatialSpotSubset()}}.}

\item{exp_id}{Optional string. Restrict to one experiment. \code{NULL} runs over
every registered sample.}

\item{genes}{Optional character vector or integer vector. Genes to test.
Characters are matched against the var table, integers are taken as
1-indexed positions in it. \code{NULL} tests every gene.}

\item{svg_params}{List. Output of \code{\link[=params_sp_svg]{params_sp_svg()}}.}

\item{streaming}{Boolean. Load the gene chunks in batches of 1000 rather
than all at once.}

\item{.verbose}{Boolean or integer. Controls verbosity.}
}
\value{
The object with the Moran's I results cached.
}
\description{
Per-gene Moran's I against the cached spatial graph, tested under the
Cliff-Ord normality null. Needs \code{\link[=build_spatial_graph_sp]{build_spatial_graph_sp()}} to have
run first. Results land in the \code{per_sample_morans_i} slot of the \code{SpCache}.
}
\references{
Cliff & Ord, Spatial Autocorrelation, 1973
}
