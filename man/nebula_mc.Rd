% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_mc_analysis.R
\name{nebula_mc}
\alias{nebula_mc}
\title{Run NEBULA on meta cells}
\usage{
nebula_mc(
  object,
  subject_col,
  design,
  coef = NULL,
  contrast = NULL,
  genes_to_use = NULL,
  offset = NULL,
  nebula_params = params_nebula(),
  .verbose = TRUE
)
}
\arguments{
\item{object}{\code{MetaCells} class.}

\item{subject_col}{String. The column in the obs table holding the subject
(donor) identifier. This is what the random effect is over.}

\item{design}{Formula. The experimental design, evaluated against the obs
table, e.g. \code{~ condition} or \code{~ condition + age}. Include the intercept.}

\item{coef}{Optional integer or character. Which coefficient of the design
the Wald test reports, as a 1-based column position or a column name.
Defaults to the last column.}

\item{contrast}{Optional numeric vector. One weight per design column.
Mutually exclusive with \code{coef}.}

\item{genes_to_use}{Optional character vector. The genes to fit. Defaults to
every gene in the object.}

\item{offset}{Optional numeric vector. Strictly positive scaling factor per
meta cell, aligned to the meta cells that survive the design. Defaults to
\code{NULL}, which uses the aggregated library sizes.}

\item{nebula_params}{A list, see \code{\link[=params_nebula]{params_nebula()}}. See
\code{\link[=nebula_sc]{nebula_sc()}} for the individual elements.}

\item{.verbose}{Boolean or integer. Controls verbosity and returns run
times. \code{FALSE} -> quiet, \code{TRUE} or \code{1L} -> normal verbosity, \code{2L} ->
detailed verbosity.}
}
\value{
A \code{ScNebula} class, see \code{\link[=new_sc_nebula_res]{new_sc_nebula_res()}}.
}
\description{
Fits NEBULA's negative binomial gamma mixed model over aggregated counts.
The arithmetic is \code{\link[=nebula_sc]{nebula_sc()}} verbatim, only the counts come out
of memory rather than the streamed store, and the fit is much cheaper
because there are far fewer rows.

What changes is the interpretation, not the numerics. The cell-level
overdispersion becomes the spread between aggregates within a subject rather
than between cells, so it is smaller and it absorbs whatever the aggregation
smoothed away. The subject-level term keeps its meaning. Read the two as a
variance decomposition over meta cells and do not compare them against a
single cell run.
}
\references{
He, et al., Commun Biol, 2021
}
