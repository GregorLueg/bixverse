% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes_single_cell_multimodal.R
\name{new_adt_counts_dsb}
\alias{new_adt_counts_dsb}
\title{Generates a new \code{ADTCounts} class via DSB normalisation}
\usage{
new_adt_counts_dsb(
  raw_counts,
  cell_info,
  empty_drops = NULL,
  isotype_names = NULL,
  dsb_params = params_sc_dsb(),
  scale_factor = c("standardise", "mean_subtract"),
  seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{raw_counts}{Numeric matrix. Cells x proteins matrix of raw ADT counts
with cell barcodes as row names and protein names as column names.}

\item{cell_info}{Named integer vector. Output of \code{\link[=get_cell_info]{get_cell_info()}}. Defines
as elements the cell indices (R-based) and as names the barcodes.}

\item{empty_drops}{Optional numeric matrix. Cells x proteins matrix of
empty-droplet ADT counts. If provided, used to estimate per-protein ambient
background.}

\item{isotype_names}{Optional string vector. Column names in \code{raw_counts}
identifying isotype control proteins. Required when
\code{dsb_params$use_isotype_controls = TRUE}.}

\item{dsb_params}{List. Output of \code{\link[=params_sc_dsb]{params_sc_dsb()}} with DSB parameters.}

\item{scale_factor}{String. One of \code{c("standardise", "mean_subtract")}. Only
used when \code{empty_drops} is provided.}

\item{seed}{Integer. Random seed for k-means initialisation.}

\item{.verbose}{Boolean or integer. Controls verbosity and returns run times.
\code{FALSE} -> quiet, \code{TRUE} or \code{1L} -> normal verbosity, \code{2L} -> detailed
verbosity.}
}
\value{
\code{ADTCounts} containing the raw counts and the DSB-normalised
counts.
}
\description{
This function generates a new \code{ADTCounts} class using DSB normalisation from
Mulè et al., instead of CLR. When \code{empty_drops} is provided, the per-protein
background is estimated from empty droplets. Without \code{empty_drops}, the
function falls back to a 2-component k-means on the log-transformed cell
counts ("ModelNegativeADTnorm").
}
\references{
Mulè et al., Nat Commun, 2022
}
