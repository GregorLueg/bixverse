% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes_spatial_sample.R
\name{new_spatial_sample}
\alias{new_spatial_sample}
\title{Construct a SpatialSample}
\usage{
new_spatial_sample(
  exp_id,
  technology = c("visium", "xenium"),
  n_spots,
  coord_cols = c(x = "pxl_col_in_fullres", y = "pxl_row_in_fullres"),
  array_cols = c(row = "array_row", col = "array_col"),
  scale_factors = list(),
  image_paths = list()
)
}
\arguments{
\item{exp_id}{String. The experiment identifier (matches \code{exp_id} in
the obs table of the parent \code{\link[=SpatialSpot]{SpatialSpot()}}).}

\item{technology}{String. One of \code{c("visium", "xenium")}. Visium HD,
MERFISH and Stereo-seq will be added in later milestones.}

\item{n_spots}{Integer. Number of spots in this experiment.}

\item{coord_cols}{Named character vector of length 2 with names
\code{c("x", "y")} giving the obs column names holding the spatial
coordinates in physical units. Defaults to the Visium full-resolution
columns.}

\item{array_cols}{Named character vector of length 2 with names
\code{c("row", "col")} giving the obs column names with array (grid)
coordinates. Visium specific; can be empty for Xenium.}

\item{scale_factors}{Named list. Parsed \code{scalefactors_json.json}
contents for Visium; named list of relevant scaling info for Xenium.
Validated via \code{\link[=validate_scale_factors]{validate_scale_factors()}}.}

\item{image_paths}{Named list of file paths RELATIVE to the parent
\code{dir_data}. Valid names are a subset of \code{c("lowres", "hires", "fullres")}. Empty list for Xenium.}
}
\value{
A \code{SpatialSample} S3 object.
}
\description{
Helper to build the \code{SpatialSample} S3 class. Stores per-experiment
spatial metadata: technology, number of spots, the obs column names
holding coordinates, the parsed scale factors and (relative) paths to
the images on disk. Image paths are expected to be RELATIVE to the
SpatialSpot \code{dir_data} so the directory is portable.
}
