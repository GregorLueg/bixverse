% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/base_generics_spatial.R
\name{nhood_enrichment_sp}
\alias{nhood_enrichment_sp}
\title{Neighbourhood enrichment between spot labels}
\usage{
nhood_enrichment_sp(
  object,
  label_col,
  exp_id = NULL,
  nhood_params = params_sp_nhood(),
  seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{object}{A \code{\link[=SpatialSpot]{SpatialSpot()}} or
\code{\link[=SpatialSpotSubset]{SpatialSpotSubset()}}.}

\item{label_col}{String. Column in the obs table holding the spot labels.}

\item{exp_id}{Optional string. Restrict to one experiment. \code{NULL} runs over
every registered sample.}

\item{nhood_params}{List. Output of \code{\link[=params_sp_nhood]{params_sp_nhood()}}.}

\item{seed}{Integer. Seed for the permutations.}

\item{.verbose}{Boolean or integer. Controls verbosity.}
}
\value{
The object with the enrichment results cached.
}
\description{
Asks whether each pair of labels shows up as spatial neighbours more or less
often than chance. Only the labels are permuted, so the null keeps the
spatial structure of the slide. Needs \code{\link[=build_spatial_graph_sp]{build_spatial_graph_sp()}} to
have run first. Results land in the \code{per_sample_nhood_enrichment} slot of the
\code{SpCache}, keyed by \code{exp_id} and then by \code{label_col}.
}
