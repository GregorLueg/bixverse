% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_coexp_nmf.R
\name{nmf_bulk}
\alias{nmf_bulk}
\title{Run non-negative matrix factorisation on a BulkCoExp}
\usage{
nmf_bulk(
  object,
  k,
  preprocessing = c("none", "sd", "sqrt_sd"),
  nmf_hals_params = params_nmf_hals(),
  min_loading = 0,
  seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{object}{The class, see \code{\link[=BulkCoExp]{BulkCoExp()}}. Ideally, you should
run \code{\link[=preprocess_bulk_coexp]{preprocess_bulk_coexp()}} before applying this function. The
NMF requires non-negative input, so the pre-processing should either skip
scaling or use a strictly non-negative representation.}

\item{k}{Integer. Number of latent factors (modules).}

\item{preprocessing}{String. One of \code{c("none", "sd", "sqrt_sd")}. Applied
inside the Rust kernel to the input matrix before the HALS updates.}

\item{nmf_hals_params}{List. Output of \code{\link[=params_nmf_hals]{params_nmf_hals()}}:
\itemize{
\item max_iter - Integer. Maximum number of HALS iterations.
\item tol - Float. Convergence tolerance.
\item eps - Float. Numerical floor.
\item check_every - Integer. Convergence check interval.
\item nmf_init - String. One of \code{c("nndsvd", "svd", "random")}.
}}

\item{min_loading}{Float in \verb{[0, 1]}. Minimum fraction of the top loading a
gene must reach to be assigned to any module. Genes falling below the
threshold are labelled \code{"unassigned"}. Defaults to \code{0}.}

\item{seed}{Integer. Random seed for the NMF initialisation. Defaults to
\code{42L}.}

\item{.verbose}{Boolean or integer \code{0L}/\code{1L}/\code{2L}. Controls verbosity.}
}
\value{
The class with \code{final_results} populated (see description) and the
fit parameters stored under \code{params$nmf_fit}, plus
\code{params$detection_method = "nmf-based"}.
}
\description{
Fits a single HALS-NMF \verb{V ~ W H} to the bulk expression matrix with a fixed
number of components \code{k}. \code{V} is samples x features (matches the layout of
\code{raw_data} / \code{processed_data} on \code{\link[=BulkCoExp]{BulkCoExp()}}). The result is
rearranged so that:
\itemize{
\item \code{gene_loadings} (features x k) captures per-module gene contributions.
\item \code{sample_activity} (samples x k) captures per-module sample activity.
}
Module membership is derived per gene by \code{which.max} over \code{abs(gene_loadings)}
with a minimum-loading threshold.
}
\references{
Cichocki & Phan, IEICE Trans., 2009.
}
