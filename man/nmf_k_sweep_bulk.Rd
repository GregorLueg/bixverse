% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_coexp_nmf.R
\name{nmf_k_sweep_bulk}
\alias{nmf_k_sweep_bulk}
\title{Sweep k for consensus NMF on a BulkCoExp}
\usage{
nmf_k_sweep_bulk(
  object,
  k_range,
  n_runs = 30L,
  preprocessing = c("none", "sd", "sqrt_sd"),
  nmf_hals_params = params_nmf_hals(),
  nmf_consensus_params = params_nmf_consensus(),
  seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{object}{The class, see \code{\link[=BulkCoExp]{BulkCoExp()}}.}

\item{k_range}{Integer vector. The ranks to evaluate. Every entry must be at
least 2.}

\item{n_runs}{Integer. Number of random restarts per \code{k}. Must be at least 2.}

\item{preprocessing}{String. One of \code{c("none", "sd", "sqrt_sd")}.}

\item{nmf_hals_params}{List. Output of \code{\link[=params_nmf_hals]{params_nmf_hals()}}.}

\item{nmf_consensus_params}{List. Output of
\code{\link[=params_nmf_consensus]{params_nmf_consensus()}}.}

\item{seed}{Integer. Base random seed.}

\item{.verbose}{Boolean or integer \code{0L}/\code{1L}/\code{2L}. Controls verbosity.}
}
\value{
An \code{NmfKSweepResult}, which is a data.table with one row per \code{k}.
}
\description{
Runs the consensus clustering step across a range of \code{k} and reports
stability against reconstruction error, without keeping any factors. Pick
the \code{k} where stability is still high and the error curve has not yet
flattened out, then run \code{\link[=consensus_nmf_bulk]{consensus_nmf_bulk()}} there.
}
\details{
This is a diagnostic, so it leaves the object alone and hands you the result
back directly. \code{plot()} on the returned object gives you the two curves.

Cost is \code{length(k_range) * n_runs} full NMF fits, so keep both modest on a
first pass.
}
\references{
Kotliar et al., eLife, 2019
}
