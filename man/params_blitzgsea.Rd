% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_wrappers.R
\name{params_blitzgsea}
\alias{params_blitzgsea}
\title{Wrapper function to generate blitzGSEA parameters}
\usage{
params_blitzgsea(
  min_size = 5L,
  max_size = 500L,
  permutations = 2000L,
  anchors = 40L,
  symmetric = FALSE,
  centre = TRUE,
  ks_test = TRUE,
  seed = 42
)
}
\arguments{
\item{min_size}{Integer. Minimum number of genes per gene set.}

\item{max_size}{Integer. Maximum number of genes per gene set.}

\item{permutations}{Integer. Random gene sets drawn per anchor size during
calibration. Defaults to \code{2000L}. Below \code{1000L} the two tails are pooled
into a single gamma regardless of \code{symmetric}.}

\item{anchors}{Integer. Number of log-spaced anchor sizes requested. Sizes
that collide after rounding are collapsed, so the realised grid is usually a
little smaller. Defaults to \code{40L}.}

\item{symmetric}{Boolean. Pool both tails into one gamma instead of fitting
them separately. Defaults to \code{FALSE}.}

\item{centre}{Boolean. Centre the signature on its mean before scoring. The
enrichment score is not invariant to an offset, so the calibration and the
scoring have to agree on this. Defaults to \code{TRUE}.}

\item{ks_test}{Boolean. Run the Kolmogorov-Smirnov goodness-of-fit
diagnostic at every anchor. Costs a sort per anchor. Defaults to \code{TRUE}.}

\item{seed}{Float. Random seed for the calibration. Defaults to \code{42}.}
}
\value{
List with parameters for usage in subsequent function.
}
\description{
Wrapper function to generate blitzGSEA parameters
}
\references{
Lachmann, et al., Bioinformatics, 2022
}
