% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-generated.R
\name{params_bulk_sparsity}
\alias{params_bulk_sparsity}
\title{Wrapper function to generate bulk sparsification parameters}
\usage{
params_bulk_sparsity(
  strategy = "seq_depth",
  target_library_size = 20000,
  capture_efficiency_sigma = 0.5,
  seed = 123L
)
}
\arguments{
\item{strategy}{String. Which dropout strategy to apply. Currently only
\code{"seq_depth"}. One of \code{"seq_depth"}. Defaults to \code{"seq_depth"}.}

\item{target_library_size}{Numeric. Reference library size per sample.
Defaults to \code{20000.0}.}

\item{capture_efficiency_sigma}{Numeric. Standard deviation of the LogNormal
size-factor distribution. Larger values spread the library sizes further
apart. Defaults to \code{0.5}.}

\item{seed}{Integer. Seed for reproducibility purposes. Defaults to \code{123L}.}
}
\value{
A named list with the following elements:
\itemize{
\item strategy - String. Which dropout strategy to apply. Currently only
\code{"seq_depth"}. One of \code{"seq_depth"}. Defaults to \code{"seq_depth"}.
\item target_library_size - Numeric. Reference library size per sample.
Defaults to \code{20000.0}.
\item capture_efficiency_sigma - Numeric. Standard deviation of the
LogNormal size-factor distribution. Larger values spread the library sizes
further apart. Defaults to \code{0.5}.
\item seed - Integer. Seed for reproducibility purposes. Defaults to \code{123L}.
}
}
\description{
Parameters for \code{\link[=simulate_dropouts]{simulate_dropouts()}}. Dropout falls
out of the library size rather than an explicit per-gene dropout curve: a
size factor \code{s_j ~ LogNormal(0, capture_efficiency_sigma)} is drawn per
sample, giving a target library size of \code{target_library_size * s_j}, and each
gene is binomially thinned towards that target.
}
\references{
Zappia, et al., Genome Biol, 2017
}
