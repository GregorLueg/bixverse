% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_constructors.R
\name{params_dialogue_hlm}
\alias{params_dialogue_hlm}
\title{Wrapper function for the DIALOGUE mixed model parameters}
\usage{
params_dialogue_hlm(
  min_cells_per_sample = 2L,
  use_tme_qc = TRUE,
  use_cell_quality = TRUE,
  satterthwaite = TRUE
)
}
\arguments{
\item{min_cells_per_sample}{Integer. Minimum cells a sample must contribute,
in \emph{both} cell types of a pair, before it takes part in that pair's models.}

\item{use_tme_qc}{Boolean. Include the partner cell type's mean quality in
that sample as a fixed effect. Upstream's \code{tme.qc}.}

\item{use_cell_quality}{Boolean. Include the responding cell's own quality
as a fixed effect. Upstream's \code{cellQ}.}

\item{satterthwaite}{Boolean. Compute Satterthwaite denominator degrees of
freedom, as \code{lmerTest} does.}
}
\value{
A list with the stage two DIALOGUE parameters.
}
\description{
Stage two of DIALOGUE: for every ordered pair of cell types and every
candidate gene, a random-intercept mixed model over samples asking whether a
cell's own programme score tracks the partner cell type's expression of that
gene in the same sample.
}
\details{
This stage dominates the runtime, and \code{satterthwaite} is the knob that
decides how badly. Turning it off falls back to the residual count for the
denominator degrees of freedom, which is far cheaper and barely differs once
a cell type has thousands of cells.

\code{use_cell_quality} conditions on the cell's own quality covariate, which
stage one has already regressed out of the scores by ordinary least squares.
The default conditions on it twice, because upstream does.
}
\references{
Jerby-Arnon & Regev, Nature Biotechnology, 2022
}
