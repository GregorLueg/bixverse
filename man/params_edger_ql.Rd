% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_constructors.R
\name{params_edger_ql}
\alias{params_edger_ql}
\title{Wrapper function for parameters for the edgeR quasi-likelihood workflow}
\usage{
params_edger_ql(
  norm_method = c("TMM", "TMMwsp", "RLE", "upperquartile", "none"),
  filter = TRUE,
  min_mean = 0,
  robust = FALSE,
  legacy = FALSE
)
}
\arguments{
\item{norm_method}{String. Library size normalisation. One of
\code{c("TMM", "TMMwsp", "RLE", "upperquartile", "none")}. Defaults to \code{"TMM"}.
\code{"none"} leaves every factor at one, which is what Milo's \code{logMS} amounts
to.}

\item{filter}{Boolean. Run \code{filterByExpr()} before fitting. Defaults to
\code{TRUE}. Turn this off for anything that is not gene expression, e.g. Milo
neighbourhood counts, where the heuristic means nothing.}

\item{min_mean}{Numeric. Drop features whose mean count across samples is
below this. Applied on top of \code{filter}. Defaults to \code{0} (off).}

\item{robust}{Boolean. Robust empirical Bayes squeezing, giving outlier
features their own smaller prior degrees of freedom. Defaults to \code{FALSE}.}

\item{legacy}{Boolean. Take edgeR's pre-4.0 quasi-likelihood pipeline.
Defaults to \code{FALSE}.}
}
\value{
A list with the edgeR quasi-likelihood parameters.
}
\description{
Parameters for the edgeR quasi-likelihood chain, implemented in Rust via the
\code{edge-rs} crate and gated against edgeR 4.8.2. Defaults are edgeR's own.

The \code{legacy} switch picks between two genuinely different pipelines. The
current route estimates its own dispersion from the most abundant genes and
skips \code{estimateDisp()}, which is where most of the runtime went and is
edgeR 4's own recommendation. The legacy route shrinks the raw residual
deviance, needs a dispersion handed to it, and is the only one where the
Poisson bound bites.
}
\references{
Chen, Lun and Smyth, F1000Research, 2016
}
