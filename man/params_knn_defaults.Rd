% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-generated.R
\name{params_knn_defaults}
\alias{params_knn_defaults}
\title{Helper function to generate kNN defaults}
\usage{
params_knn_defaults()
}
\value{
A named list with the following elements:
\itemize{
\item k - Integer. Number of neighbours. Defaults to \code{15L}.
\item knn_method - String. Which method to use for the approximate nearest
neighbour search. One of \code{c("kmknn", "hnsw", "annoy", "nndescent", "ivf", "exhaustive")}. Defaults to \code{"kmknn"}.
\item ann_dist - String. Which distance metric to use for the approximate
nearest neighbour search. One of \code{c("euclidean", "cosine")}. Defaults to
\code{"euclidean"}.
\item n_trees - Integer. Annoy param: number of trees to generate for Annoy.
Defaults to \code{50L}.
\item search_budget - Integer or \code{NULL}. Annoy param: optional search budget
per tree for Annoy. If not provided, it will default to \code{n_tree * k * 20L}.
Defaults to \code{NULL}.
\item delta - Numeric. NNDescent param: early termination criterium for
NNDescent. Defaults to \code{0.001}.
\item diversify_prob - Numeric. NNDescent param: diversification probability
for the NNDescent index. This will diversify the index at the end and
identify potentially better edges. Defaults to \code{0.0}.
\item ef_budget - Integer or \code{NULL}. NNDescent param: optional query budget
parameter. Can accelerate querying, but at the cost of Recall. Defaults to
\code{NULL}.
\item extract_knn - Boolean. NNDescent param: hand back the graph the
descent already built instead of beam searching it. Skips the query pass
entirely, so it is much faster, at the cost of some recall. Rows the descent
never filled come back padded with duplicate edges. Ignored by every other
method. Defaults to \code{FALSE}.
\item m - Integer. HNSW param: number of connections between layers for
HNSW. Defaults to \code{16L}.
\item ef_construction - Integer. HNSW param: size of dynamic candidate list
during construction. Defaults to \code{200L}.
\item ef_search - Integer. HNSW param: size of candidate list (higher =
better recall, slower). Defaults to \code{100L}.
\item n_list - Integer or \code{NULL}. IVF param: number of clusters/centroids to
generate. \code{NULL} generates \code{sqrt(n)} lists. Defaults to \code{NULL}.
\item n_probe - Integer or \code{NULL}. IVF param: number of clusters/centroids
to query. \code{NULL} queries \code{sqrt(n_lists)} clusters. Defaults to \code{NULL}.
}
}
\description{
This function generates various sensible default parameters for
all of the different approximate nearest neighbours that are available within
this package.
}
