% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-generated.R
\name{params_label_propagation}
\alias{params_label_propagation}
\title{Wrapper function to generate label propagation parameters}
\usage{
params_label_propagation(
  alpha = 0.9,
  iter = 100L,
  tolerance = 1e-06,
  symmetrise = TRUE,
  symmetry_strategy = c("average", "avg", "min", "max"),
  max_hops = NULL
)
}
\arguments{
\item{alpha}{Numeric. Controls the spreading strength. Higher values anchor
labelled nodes more strongly to their original label. Defaults to \code{0.9}.}

\item{iter}{Integer. Maximum number of iterations to run. Defaults to \code{100L}.}

\item{tolerance}{Numeric. Convergence threshold. Stops early if the maximum
change across all nodes falls below this value. Defaults to \code{1e-06}.}

\item{symmetrise}{Boolean. Whether to symmetrise the graph. Defaults to
\code{TRUE}.}

\item{symmetry_strategy}{String. Strategy to resolve weight conflicts when
symmetrising. Only relevant when \code{symmetrise = TRUE} and edge weights are
provided. One of \code{c("average", "avg", "min", "max")}. Defaults to
\code{"average"}.}

\item{max_hops}{Integer or \code{NULL}. If provided, restricts label spreading to
nodes within this many hops of any labelled node. Nodes beyond this limit
remain all-zeroes. Defaults to \code{NULL}.}
}
\value{
A named list with the following elements:
\itemize{
\item alpha - Numeric. Controls the spreading strength. Higher values anchor
labelled nodes more strongly to their original label. Defaults to \code{0.9}.
\item iter - Integer. Maximum number of iterations to run. Defaults to
\code{100L}.
\item tolerance - Numeric. Convergence threshold. Stops early if the maximum
change across all nodes falls below this value. Defaults to \code{1e-06}.
\item symmetrise - Boolean. Whether to symmetrise the graph. Defaults to
\code{TRUE}.
\item symmetry_strategy - String. Strategy to resolve weight conflicts when
symmetrising. Only relevant when \code{symmetrise = TRUE} and edge weights are
provided. One of \code{c("average", "avg", "min", "max")}. Defaults to
\code{"average"}.
\item max_hops - Integer or \code{NULL}. If provided, restricts label spreading
to nodes within this many hops of any labelled node. Nodes beyond this limit
remain all-zeroes. Defaults to \code{NULL}.
}
}
\description{
Wrapper function to generate label propagation parameters
}
