% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_wrappers.R
\name{params_label_propagation}
\alias{params_label_propagation}
\title{Wrapper function to generate label propagation parameters}
\usage{
params_label_propagation(
  alpha = 0.9,
  iter = 100L,
  tolerance = 1e-06,
  symmetrise = TRUE,
  symmetry_strategy = "average",
  max_hops = NULL
)
}
\arguments{
\item{alpha}{Numeric. Controls the spreading strength. Higher values anchor
labelled nodes more strongly to their original label. Defaults to \code{0.9}.}

\item{iter}{Integer. Maximum number of iterations to run. Defaults to \code{100L}.}

\item{tolerance}{Numeric. Convergence threshold. Stops early if the maximum
change across all nodes falls below this value. Defaults to \code{1e-6}.}

\item{symmetrise}{Logical. Whether to symmetrise the graph. Defaults to
\code{TRUE}.}

\item{symmetry_strategy}{Character. Strategy to resolve weight conflicts when
symmetrising. One of \code{"average"}, \code{"min"}, or \code{"max"}. Only relevant when
\code{symmetrise = TRUE} and edge weights are provided. Defaults to \code{"average"}.}

\item{max_hops}{Integer or NULL. If provided, restricts label spreading to
nodes within this many hops of any labelled node. Nodes beyond this limit
remain all-zeroes. Defaults to \code{NULL}.}
}
\value{
A named list of label propagation parameters.
}
\description{
Wrapper function to generate label propagation parameters
}
