% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_constructors.R
\name{params_lda}
\alias{params_lda}
\title{Wrapper function for the LDA parameters}
\usage{
params_lda(
  alpha = 50,
  alpha_by_topic = TRUE,
  eta = 0.1,
  eta_by_topic = FALSE,
  max_iter = 150L,
  tol = 0.001,
  inner_max_iter = 100L,
  inner_tol = 0.001,
  check_every = 10L,
  learning = c("batch", "online"),
  batch_size = 1024L,
  n_epochs = 10L
)
}
\arguments{
\item{alpha}{Float. Dirichlet prior on the document-topic distributions.}

\item{alpha_by_topic}{Boolean. Shall \code{alpha} be divided by the topic count.}

\item{eta}{Float. Dirichlet prior on the topic-term distributions.}

\item{eta_by_topic}{Boolean. Shall \code{eta} be divided by the topic count.}

\item{max_iter}{Integer. Maximum outer iterations. Ignored by the online
variant, which counts epochs instead.}

\item{tol}{Float. Relative change in the bound below which the solver stops.}

\item{inner_max_iter}{Integer. Maximum fixed-point iterations of the
per-document E-step.}

\item{inner_tol}{Float. Relative L1 change in the variational parameters
below which the per-document E-step stops.}

\item{check_every}{Integer. Iterations between bound evaluations.}

\item{learning}{String. One of \code{c("batch", "online")}.}

\item{batch_size}{Integer. Documents per mini-batch. Online only.}

\item{n_epochs}{Integer. Passes over the corpus. Online only.}
}
\value{
A list with the LDA parameters.
}
\description{
Solver options for the variational Bayes latent Dirichlet allocation, see
\code{\link[=run_lda]{run_lda()}}.
}
\details{
The defaults follow pycisTopic, so the knobs mean the same thing on both
sides. \code{alpha_by_topic = TRUE} turns \code{alpha} into the Griffiths and
Steyvers \code{50 / k} heuristic that cisTopic defaults to; set it to \code{FALSE} if
you want \code{alpha} taken literally.

\code{learning = "batch"} sweeps every document once per iteration and is
monotone in the bound. \code{"online"} takes decaying steps from shuffled
mini-batches, which reaches a usable fit in far fewer passes on a large
corpus at the cost of that guarantee. \code{batch_size} and \code{n_epochs} are only
read by the online variant.
}
\references{
Hoffman, Blei and Bach, NIPS, 2010; Bravo Gonzalez-Blas, et al.,
Nat Methods, 2019
}
