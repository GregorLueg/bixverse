% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-generated.R
\name{params_ligand_target}
\alias{params_ligand_target}
\title{Parameters for ligand to target influence computation}
\usage{
params_ligand_target(
  lr_sig_hub = 0,
  gr_hub = 0,
  ltf_cutoff = 0.99,
  damping_factor = 0.5,
  tol = 1e-06,
  max_iter = 1000L,
  topology_correction = FALSE,
  secondary_targets = FALSE
)
}
\arguments{
\item{lr_sig_hub}{Numeric. Numeric in \verb{[0, 1]}. Hub correction strength for
the ligand-receptor / signalling layer. 0 disables correction. Defaults to
\code{0.0}.}

\item{gr_hub}{Numeric. Numeric in \verb{[0, 1]}. Hub correction strength for the
gene regulatory layer. 0 disables correction. Defaults to \code{0.0}.}

\item{ltf_cutoff}{Numeric. Numeric in \verb{[0, 1]}. Quantile cutoff applied to
the intermediate ligand-to-TF matrix. Defaults to \code{0.99}.}

\item{damping_factor}{Numeric. Numeric in \verb{[0, 1]}. PageRank-style damping
factor. Defaults to \code{0.5}.}

\item{tol}{Numeric. Numeric > 0. Convergence tolerance for the propagation
step. Defaults to \code{1e-06}.}

\item{max_iter}{Integer. Integer >= 1. Maximum iterations for the propagation
step. Defaults to \code{1000L}.}

\item{topology_correction}{Boolean. Apply topology correction. Defaults to
\code{FALSE}.}

\item{secondary_targets}{Boolean. Run a second round through targets.
Defaults to \code{FALSE}.}
}
\value{
A named list with the following elements:
\itemize{
\item lr_sig_hub - Numeric. Numeric in \verb{[0, 1]}. Hub correction strength for
the ligand-receptor / signalling layer. 0 disables correction. Defaults to
\code{0.0}.
\item gr_hub - Numeric. Numeric in \verb{[0, 1]}. Hub correction strength for the
gene regulatory layer. 0 disables correction. Defaults to \code{0.0}.
\item ltf_cutoff - Numeric. Numeric in \verb{[0, 1]}. Quantile cutoff applied to
the intermediate ligand-to-TF matrix. Defaults to \code{0.99}.
\item damping_factor - Numeric. Numeric in \verb{[0, 1]}. PageRank-style damping
factor. Defaults to \code{0.5}.
\item tol - Numeric. Numeric > 0. Convergence tolerance for the propagation
step. Defaults to \code{1e-06}.
\item max_iter - Integer. Integer >= 1. Maximum iterations for the
propagation step. Defaults to \code{1000L}.
\item topology_correction - Boolean. Apply topology correction. Defaults to
\code{FALSE}.
\item secondary_targets - Boolean. Run a second round through targets.
Defaults to \code{FALSE}.
}
}
\description{
Parameters for ligand to target influence computation
}
