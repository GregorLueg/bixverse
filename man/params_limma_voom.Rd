% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_constructors.R
\name{params_limma_voom}
\alias{params_limma_voom}
\title{Wrapper function for parameters for the limma-voom workflow}
\usage{
params_limma_voom(
  route = c("voom", "trend"),
  norm_method = c("TMM", "TMMwsp", "RLE", "upperquartile", "none"),
  filter = FALSE,
  min_mean = 0,
  robust = FALSE,
  prior_count = NULL,
  adaptive_span = TRUE,
  span = 0.5,
  proportion = 0.01
)
}
\arguments{
\item{route}{String. One of \code{c("voom", "trend")}. Defaults to \code{"voom"}.}

\item{norm_method}{String. Library size normalisation. One of
\code{c("TMM", "TMMwsp", "RLE", "upperquartile", "none")}. Defaults to \code{"TMM"}.}

\item{filter}{Boolean. Run \code{filterByExpr()} before fitting. Defaults to
\code{FALSE}.}

\item{min_mean}{Numeric. Drop genes whose mean count across samples is below
this. Applied on top of \code{filter}. Defaults to \code{0} (off).}

\item{robust}{Boolean. Robust empirical Bayes, \code{eBayes(robust = TRUE)}.
Defaults to \code{FALSE}.}

\item{prior_count}{Optional numeric. Count added before the log. \code{NULL}
takes the route's own default, \code{0.5} for voom and \code{2} for trend.}

\item{adaptive_span}{Boolean. Derive the lowess span from the number of
genes, as limma does since 3.56. Only used by voom. Defaults to \code{TRUE}.}

\item{span}{Numeric. Lowess span for the voom trend, only read if
\code{adaptive_span = FALSE}. Defaults to \code{0.5}.}

\item{proportion}{Numeric. Assumed proportion of differentially expressed
genes, only used for the B-statistic. Defaults to \code{0.01}.}
}
\value{
A list with the limma-voom parameters.
}
\description{
Parameters for the limma linear model chain, implemented in Rust via the
\code{edge-rs} crate and gated against limma 3.66.0. Defaults are limma's own,
except for \code{filter}: inside \code{\link[=BulkDge]{BulkDge()}} the genes were already
filtered by \code{\link[=qc_bulk_dge]{qc_bulk_dge()}}.

\code{route = "voom"} is \code{voomLmFit()}: precision weights from the mean-variance
trend, then weighted least squares. \code{route = "trend"} is limma-trend:
log-CPM straight into \code{lmFit()}, with the trend absorbed by
\code{eBayes(trend = TRUE)}. The empirical Bayes trend follows the route.
}
\references{
Law, et al., Genome Biol, 2014; Smyth, Stat Appl Genet Mol Biol,
2004
}
