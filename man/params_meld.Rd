% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-generated.R
\name{params_meld}
\alias{params_meld}
\title{Constructor for MELD parameters}
\usage{
params_meld(
  beta = 60,
  offset = 0,
  order = 1,
  filter = c("heat", "laplacian"),
  chebyshev_order = 50L,
  lap_type = c("combinatorial", "normalised"),
  normalise_indicators = TRUE,
  knn = list()
)
}
\arguments{
\item{beta}{Numeric. Smoothing strength; larger values produce smoother
densities. Must be strictly positive. Defaults to \code{60.0}.}

\item{offset}{Numeric. Shift of the filter centre in the rescaled spectrum.
Must be in \verb{[0, 1]}. Defaults to \code{0.0}.}

\item{order}{Numeric. Filter falloff sharpness; larger values approach a
square low-pass. Must be strictly positive. Defaults to \code{1.0}.}

\item{filter}{String. Filter family to use. One of \code{c("heat", "laplacian")}.
Defaults to \code{"heat"}.}

\item{chebyshev_order}{Integer. Number of Chebyshev coefficients (polynomial
terms). Must be >= 2. Defaults to \code{50L}.}

\item{lap_type}{String. Type of Laplacian to use for spectral filtering. One
of \code{c("combinatorial", "normalised")}. Defaults to \code{"combinatorial"}.}

\item{normalise_indicators}{Boolean. If \code{TRUE}, each column of the indicator
matrix is divided by its column sum before filtering, making cross-condition
densities comparable regardless of cells-per-condition. Defaults to \code{TRUE}.}

\item{knn}{List. Optional overrides for kNN parameters. See
\code{\link[=params_knn_defaults]{params_knn_defaults()}} for available parameters: \code{k},
\code{knn_method}, \code{ann_dist}, \code{search_budget}, \code{n_trees}, \code{delta},
\code{diversify_prob}, \code{ef_budget}, \code{m}, \code{ef_construction}, \code{ef_search}, \code{n_list}
and \code{n_probe}. See \code{\link[=params_knn_defaults]{params_knn_defaults()}} for the available elements.
Defaults to \code{list()}.}
}
\value{
A named list with the following elements:
\itemize{
\item The elements of \code{\link[=params_knn_defaults]{params_knn_defaults()}}, overridden by \code{knn}, spliced
in at this position.
\item beta - Numeric. Smoothing strength; larger values produce smoother
densities. Must be strictly positive. Defaults to \code{60.0}.
\item offset - Numeric. Shift of the filter centre in the rescaled spectrum.
Must be in \verb{[0, 1]}. Defaults to \code{0.0}.
\item order - Numeric. Filter falloff sharpness; larger values approach a
square low-pass. Must be strictly positive. Defaults to \code{1.0}.
\item filter - String. Filter family to use. One of \code{c("heat", "laplacian")}. Defaults to \code{"heat"}.
\item chebyshev_order - Integer. Number of Chebyshev coefficients
(polynomial terms). Must be >= 2. Defaults to \code{50L}.
\item lap_type - String. Type of Laplacian to use for spectral filtering.
One of \code{c("combinatorial", "normalised")}. Defaults to \code{"combinatorial"}.
\item normalise_indicators - Boolean. If \code{TRUE}, each column of the
indicator matrix is divided by its column sum before filtering, making
cross-condition densities comparable regardless of cells-per-condition.
Defaults to \code{TRUE}.
}
}
\description{
Constructor for MELD parameters
}
