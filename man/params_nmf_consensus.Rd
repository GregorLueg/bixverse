% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-generated.R
\name{params_nmf_consensus}
\alias{params_nmf_consensus}
\title{Wrapper function for consensus NMF parameters}
\usage{
params_nmf_consensus(
  consensus_target = c("h", "w"),
  n_neighbours = 0L,
  density_threshold = 0.5,
  kmeans_iters = 100L,
  kmeans_n_init = 3L
)
}
\arguments{
\item{consensus_target}{String. \code{"h"} clusters the gene programmes (the
spectra), which is what cNMF does and what you almost always want. \code{"w"}
clusters in sample space instead, which on single cell data means cell space:
it pools a dense \verb{(k * n_runs) x n_cells} matrix and runs an exhaustive
cosine search over it, so it gets expensive fast. One of \code{c("h", "w")}.
Defaults to \code{"h"}.}

\item{n_neighbours}{Integer. Neighbours used for the local density estimate.
\code{0L} picks \code{ceiling(0.3 * n_runs)} for you. Defaults to \code{0L}.}

\item{density_threshold}{Numeric. Components whose mean cosine distance to
their neighbours exceeds this are dropped as unstable. Cosine distance cannot
exceed 2, so any value \verb{>= 2} disables the filter entirely. Defaults to
\code{0.5}.}

\item{kmeans_iters}{Integer. Maximum k-means iterations. Defaults to \code{100L}.}

\item{kmeans_n_init}{Integer. Number of k-means restarts. Defaults to \code{3L}.}
}
\value{
A named list with the following elements:
\itemize{
\item consensus_target - String. \code{"h"} clusters the gene programmes (the
spectra), which is what cNMF does and what you almost always want. \code{"w"}
clusters in sample space instead, which on single cell data means cell
space: it pools a dense \verb{(k * n_runs) x n_cells} matrix and runs an
exhaustive cosine search over it, so it gets expensive fast. One of \code{c("h", "w")}. Defaults to \code{"h"}.
\item n_neighbours - Integer. Neighbours used for the local density
estimate. \code{0L} picks \code{ceiling(0.3 * n_runs)} for you. Defaults to \code{0L}.
\item density_threshold - Numeric. Components whose mean cosine distance to
their neighbours exceeds this are dropped as unstable. Cosine distance
cannot exceed 2, so any value \verb{>= 2} disables the filter entirely. Defaults
to \code{0.5}.
\item kmeans_iters - Integer. Maximum k-means iterations. Defaults to
\code{100L}.
\item kmeans_n_init - Integer. Number of k-means restarts. Defaults to \code{3L}.
}
}
\description{
Controls the clustering step of consensus NMF: the components of
every restart are pooled, outliers are dropped by local density, and the
survivors are k-means clustered into \code{k} groups whose median becomes the
consensus factor.
}
\references{
Kotliar et al., eLife, 2019
}
