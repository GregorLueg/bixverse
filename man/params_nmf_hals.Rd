% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-generated.R
\name{params_nmf_hals}
\alias{params_nmf_hals}
\title{Wrapper function for NMF (HALS) parameters}
\usage{
params_nmf_hals(
  max_iter = 250L,
  tol = 1e-04,
  eps = 1e-10,
  check_every = 10L,
  nmf_init = c("nndsvd", "svd", "random")
)
}
\arguments{
\item{max_iter}{Integer. Maximum number of HALS iterations. Defaults to
\code{250L}.}

\item{tol}{Numeric. Convergence tolerance on the relative change in
reconstruction loss. Defaults to \code{1e-04}.}

\item{eps}{Numeric. Numerical floor for non-negativity / division safety.
Defaults to \code{1e-10}.}

\item{check_every}{Integer. Convergence check interval in iterations.
Defaults to \code{10L}.}

\item{nmf_init}{String. \code{"nndsvd"} and \code{"svd"} both map to deterministic
NNDSVD initialisation; \code{"random"} uses random non-negative draws. For
stabilised (multi-run) NMF this field is ignored and random init is always
used. One of \code{c("nndsvd", "svd", "random")}. Defaults to \code{"nndsvd"}.}
}
\value{
A named list with the following elements:
\itemize{
\item max_iter - Integer. Maximum number of HALS iterations. Defaults to
\code{250L}.
\item tol - Numeric. Convergence tolerance on the relative change in
reconstruction loss. Defaults to \code{1e-04}.
\item eps - Numeric. Numerical floor for non-negativity / division safety.
Defaults to \code{1e-10}.
\item check_every - Integer. Convergence check interval in iterations.
Defaults to \code{10L}.
\item nmf_init - String. \code{"nndsvd"} and \code{"svd"} both map to deterministic
NNDSVD initialisation; \code{"random"} uses random non-negative draws. For
stabilised (multi-run) NMF this field is ignored and random init is always
used. One of \code{c("nndsvd", "svd", "random")}. Defaults to \code{"nndsvd"}.
}
}
\description{
Wrapper function for NMF (HALS) parameters
}
