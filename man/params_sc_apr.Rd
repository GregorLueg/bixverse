% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-generated.R
\name{params_sc_apr}
\alias{params_sc_apr}
\title{Wrapper function for analytic Pearson residual parameters}
\usage{
params_sc_apr(theta = 100, min_cells = 5L, clip_min = NULL, clip_max = NULL)
}
\arguments{
\item{theta}{Numeric. The shared negative binomial dispersion. \code{Inf} gives
the Poisson limit. Defaults to \code{100.0}.}

\item{min_cells}{Integer. Minimum number of cells a gene must be detected in
to be retained. \code{0L} keeps everything. Defaults to \code{5L}.}

\item{clip_min}{Numeric or \code{NULL}. Lower residual clipping bound. \code{NULL} uses
\code{-sqrt(n_cells)}. Must be given together with \code{clip_max}. Defaults to \code{NULL}.}

\item{clip_max}{Numeric or \code{NULL}. Upper residual clipping bound. \code{NULL} uses
\code{sqrt(n_cells)}. Must be given together with \code{clip_min}. Defaults to \code{NULL}.}
}
\value{
A named list with the following elements:
\itemize{
\item theta - Numeric. The shared negative binomial dispersion. \code{Inf} gives
the Poisson limit. Defaults to \code{100.0}.
\item min_cells - Integer. Minimum number of cells a gene must be detected
in to be retained. \code{0L} keeps everything. Defaults to \code{5L}.
\item clip_min - Numeric or \code{NULL}. Lower residual clipping bound. \code{NULL}
uses \code{-sqrt(n_cells)}. Must be given together with \code{clip_max}. Defaults to
\code{NULL}.
\item clip_max - Numeric or \code{NULL}. Upper residual clipping bound. \code{NULL}
uses \code{sqrt(n_cells)}. Must be given together with \code{clip_min}. Defaults to
\code{NULL}.
}
}
\description{
The closed-form alternative to scTransform: one shared
dispersion instead of a fitted model per gene. Much cheaper, and on most data
sets it ranks genes about as well.
}
\references{
Lause, Berens and Kobak, Genome Biology, 2021.
}
