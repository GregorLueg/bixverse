% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_constructors.R
\name{params_sc_branch_selection}
\alias{params_sc_branch_selection}
\title{Wrapper function for the branch cell selection parameters}
\usage{
params_sc_branch_selection(q = 0.01, eps = 0.01, resolution = 500L)
}
\arguments{
\item{q}{Numeric. Upper-tail quantile of the fate probability used as the
threshold. Defaults to \code{0.01}.}

\item{eps}{Numeric. Slack subtracted from the threshold before the
comparison. Defaults to \code{0.01}.}

\item{resolution}{Integer. Number of pseudotime buckets, capped at the cell
count. Defaults to \code{500L}.}
}
\value{
A named flat list with all branch selection parameters.
}
\description{
Parameters controlling which cells \code{\link[=run_gene_trends_sc]{run_gene_trends_sc()}} assigns
to each branch. The threshold on a fate's probability is an expanding
quantile over the pseudotime-sorted cells, made monotone with a cumulative
maximum, so a fate's bar can only rise as differentiation proceeds. The
defaults are the reference ones.
}
\references{
Setty, et al., Nat. Biotechnol., 2019.
}
