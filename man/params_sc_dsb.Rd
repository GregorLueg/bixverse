% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-generated.R
\name{params_sc_dsb}
\alias{params_sc_dsb}
\title{Default parameters for DSB ADT normalisation}
\usage{
params_sc_dsb(
  denoise_counts = TRUE,
  use_isotype_controls = TRUE,
  pseudocount = 10,
  quantile_low = NULL,
  quantile_high = NULL
)
}
\arguments{
\item{denoise_counts}{Boolean. Run Step II (cell-to-cell technical noise
removal). Defaults to \code{TRUE}.}

\item{use_isotype_controls}{Boolean. Include isotype controls in the noise
matrix in Step II. Requires \code{isotype_indices} to be passed at call time.
Defaults to \code{TRUE}.}

\item{pseudocount}{Numeric. Pseudocount added before the log transform. The
DSB paper recommends \code{10} with empty droplets and \code{1} without. Defaults to
\code{10.0}.}

\item{quantile_low}{Numeric or \code{NULL}. Optional numeric in \verb{[0, 1)}. Lower
quantile for per-protein output clipping. If \code{NULL} (and \code{quantile_high} is
also \code{NULL}), no clipping is applied. Defaults to \code{NULL}.}

\item{quantile_high}{Numeric or \code{NULL}. Optional numeric in \verb{(0, 1]}. Upper
quantile for per-protein output clipping. If \code{NULL} (and \code{quantile_low} is
also \code{NULL}), no clipping is applied. Defaults to \code{NULL}.}
}
\value{
A named list with the following elements:
\itemize{
\item denoise_counts - Boolean. Run Step II (cell-to-cell technical noise
removal). Defaults to \code{TRUE}.
\item use_isotype_controls - Boolean. Include isotype controls in the noise
matrix in Step II. Requires \code{isotype_indices} to be passed at call time.
Defaults to \code{TRUE}.
\item pseudocount - Numeric. Pseudocount added before the log transform. The
DSB paper recommends \code{10} with empty droplets and \code{1} without. Defaults to
\code{10.0}.
\item quantile_low - Numeric or \code{NULL}. Optional numeric in \verb{[0, 1)}. Lower
quantile for per-protein output clipping. If \code{NULL} (and \code{quantile_high} is
also \code{NULL}), no clipping is applied. Defaults to \code{NULL}.
\item quantile_high - Numeric or \code{NULL}. Optional numeric in \verb{(0, 1]}. Upper
quantile for per-protein output clipping. If \code{NULL} (and \code{quantile_low} is
also \code{NULL}), no clipping is applied. Defaults to \code{NULL}.
}

[0, 1)\verb{. Lower quantile for per-protein output clipping. If }NULL\verb{(and}quantile_high\verb{is also}NULL\verb{), no clipping is applied. Defaults to }NULL\verb{. \\item quantile_high - Numeric or }NULL\verb{. Optional numeric in }(0, 1]: R:0,\%201)\%60.\%20Lower\%0A\%20quantile\%20for\%20per-protein\%20output\%20clipping.\%20If\%20\%60NULL\%60\%20(and\%20\%60quantile_high\%60\%20is\%0A\%20also\%20\%60NULL\%60),\%20no\%20clipping\%20is\%20applied.\%20Defaults\%20to\%20\%60NULL\%60.\%0A\%20\%5C\%5Citem\%20quantile_high\%20-\%20Numeric\%20or\%20\%60NULL\%60.\%20Optional\%20numeric\%20in\%20\%60(0,\%201
}
\description{
Default parameters for DSB ADT normalisation
}
