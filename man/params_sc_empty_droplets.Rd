% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_constructors.R
\name{params_sc_empty_droplets}
\alias{params_sc_empty_droplets}
\title{Parameters for identifying empty droplets}
\usage{
params_sc_empty_droplets(
  method = c("supplied", "umi_cutoff", "expected_cells", "knee"),
  is_empty_column = NULL,
  umi_cutoff = NULL,
  expected_cells = NULL
)
}
\arguments{
\item{method}{String. One of \code{"supplied"}, \code{"umi_cutoff"},
\code{"expected_cells"} or \code{"knee"}.}

\item{is_empty_column}{String. Name of the logical obs column holding the
mask. Required for \code{method = "supplied"}, ignored otherwise.}

\item{umi_cutoff}{Integer. Barcodes with a library size strictly below this
are empty. Required for \code{method = "umi_cutoff"}, ignored otherwise.}

\item{expected_cells}{Integer. Number of real cells expected in the run.
Required for \code{method = "expected_cells"}, ignored otherwise.}
}
\value{
A list with the parameters.
}
\description{
CellSweep trains its ambient profile on the empty droplets, so it needs them
called before it runs. Four ways to do that, in descending order of how much
you should trust them.

\code{"supplied"} takes an existing logical column from the obs table. This is the
common case: if you have CellRanger's filtered barcode list you already know
which barcodes are empty, and nothing here will beat that.

\code{"umi_cutoff"} calls everything below an absolute library size empty.
\code{"expected_cells"} turns a cell count into that cutoff via the sorted library
sizes. \code{"knee"} finds the cutoff from the curvature of the rank / log-count
curve; it is experimental in the reference too, and smoothing puts the
curvature minimum a few ranks ahead of the actual cliff, so it sweeps up the
real barcodes nearest the transition.
}
