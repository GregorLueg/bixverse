% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_constructors.R
\name{params_sc_gene_trends}
\alias{params_sc_gene_trends}
\title{Wrapper function for gene trend parameters}
\usage{
params_sc_gene_trends(
  resolution = 500L,
  weighting = c("hard_mask", "fate_probability"),
  length_scale = 1,
  sigma = 1,
  jitter = 1e-06,
  max_jitter_retries = 3L,
  chunk_size = 2048L
)
}
\arguments{
\item{resolution}{Integer. Grid points per branch. Kept at the default even
when a branch holds fewer cells, as the reference does. Defaults to \code{500L}.}

\item{weighting}{String. One of \code{c("hard_mask", "fate_probability")}. With
\code{"hard_mask"} every selected cell enters its branch's fit with equal weight,
which is what the reference does. With \code{"fate_probability"} every cell
enters every fit weighted by its fate probability, which is more defensible:
a cell at 0.6 is not a member. Defaults to \code{"hard_mask"}.}

\item{length_scale}{Numeric. Matern-5/2 length scale. Defaults to \code{1.0}.}

\item{sigma}{Numeric. Noise standard deviation. Defaults to \code{1.0}.}

\item{jitter}{Numeric. Added to the landmark covariance diagonal before the
Cholesky. Defaults to \code{1e-6}.}

\item{max_jitter_retries}{Integer. Times the jitter is raised and the
Cholesky retried before giving up. Defaults to \code{3L}.}

\item{chunk_size}{Integer. Training points held at once when accumulating
the cross-covariance. Defaults to \code{2048L}.}
}
\value{
A named flat list with all gene trend parameters. The Gaussian
process hyperparameters sit at the same level as the trend ones, not in a
nested block.
}
\description{
Parameters controlling the landmark Gaussian process that
\code{\link[=run_gene_trends_sc]{run_gene_trends_sc()}} fits per branch. The kernel is a
Matern-5/2 one and the prediction grid doubles as the landmark set.

The defaults come from the reference and are prior-dominated. Palantir's
pseudotime is min-max scaled to \verb{[0, 1]}, so a \code{length_scale} of \code{1.0} spans
the entire domain and a \code{sigma} of \code{1.0} sits at roughly the signal scale of
log-normalised expression. The posterior will flatten genuine transient
structure and resolve almost any gene into a smooth monotone or
single-peaked curve. That is a presentation choice, not inference. Shorten
\code{length_scale} before believing a bump.
}
\references{
Setty, et al., Nat. Biotechnol., 2019.
}
