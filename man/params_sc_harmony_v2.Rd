% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-generated.R
\name{params_sc_harmony_v2}
\alias{params_sc_harmony_v2}
\title{Default parameters for Harmony v2 batch correction}
\usage{
params_sc_harmony_v2(
  k = NULL,
  sigma = 0.1,
  theta = 2,
  lambda = 1,
  block_size = 0.2,
  max_iter_kmeans = 4L,
  max_iter_harmony = 10L,
  epsilon_kmeans = 0.001,
  epsilon_harmony = 0.01,
  window_size = 3L,
  alpha = 0.2,
  tau = 0,
  batch_proportion_cutoff = 1e-05,
  use_dynamic_lambda = FALSE,
  kmeans = list()
)
}
\arguments{
\item{k}{Integer or \code{NULL}. Number of clusters for k-means clustering. If not
provided, it will be automatically determined as \code{min(round(N / 30), 100)}.
Defaults to \code{NULL}.}

\item{sigma}{Numeric vector. Per-cluster diversity weights. Either a single
value (broadcast to all clusters) or a vector of length k. Defaults to \code{0.1}.}

\item{theta}{Numeric vector. Per-variable diversity penalties. Either a
single value (broadcast to all variables) or a vector of length equal to the
number of batch variables. Defaults to \code{2.0}.}

\item{lambda}{Numeric vector. Ridge regression penalty for the linear model.
Typically a single value that is broadcast to all design matrix columns.
Ignored when \code{use_dynamic_lambda = TRUE}. Defaults to \code{1.0}.}

\item{block_size}{Numeric. Fraction of cells to update per block during
optimisation (0.0-1.0). Lower values reduce memory usage but increase
computation time. Defaults to \code{0.2}.}

\item{max_iter_kmeans}{Integer. Maximum number of k-means iterations per
Harmony round. Defaults to \code{4L}.}

\item{max_iter_harmony}{Integer. Maximum number of Harmony outer iterations.
Defaults to \code{10L}.}

\item{epsilon_kmeans}{Numeric. Convergence threshold for k-means clustering.
Stops when the relative change in cluster assignments falls below this value.
Defaults to \code{0.001}.}

\item{epsilon_harmony}{Numeric. Convergence threshold for Harmony. Stops when
the relative change in the objective function falls below this value.
Defaults to \code{0.01}.}

\item{window_size}{Integer. Number of previous iterations to consider when
checking convergence. Defaults to \code{3L}.}

\item{alpha}{Numeric. Scaling factor for dynamic lambda estimation. Must be
in (0, 1). Only relevant when \code{use_dynamic_lambda = TRUE}. Defaults to \code{0.2}.}

\item{tau}{Numeric. Scaling factor for theta based on batch size. A value of
0 disables batch-size scaling of theta. Defaults to \code{0.0}.}

\item{batch_proportion_cutoff}{Numeric. Cutoff for pruning batches with small
proportions during ridge regression. Defaults to \code{1e-05}.}

\item{use_dynamic_lambda}{Boolean. If \code{TRUE}, lambda is estimated dynamically
per cluster instead of using the fixed \code{lambda} value. Defaults to \code{FALSE}.}

\item{kmeans}{List. Optional overrides for the k-means clustering algorithm
Possible parameters are \code{"k_means_iter"}, \code{"k_means_init"}, \code{"gemm"} and
\code{"hamerly"}, see \code{\link[=params_kmeans_defaults]{params_kmeans_defaults()}}. See \code{\link[=params_kmeans_defaults]{params_kmeans_defaults()}}
for the available elements. Defaults to \code{list()}.}
}
\value{
A named list with the following elements:
\itemize{
\item k - Integer or \code{NULL}. Number of clusters for k-means clustering. If
not provided, it will be automatically determined as \code{min(round(N / 30), 100)}. Defaults to \code{NULL}.
\item sigma - Numeric vector. Per-cluster diversity weights. Either a single
value (broadcast to all clusters) or a vector of length k. Defaults to
\code{0.1}.
\item theta - Numeric vector. Per-variable diversity penalties. Either a
single value (broadcast to all variables) or a vector of length equal to the
number of batch variables. Defaults to \code{2.0}.
\item lambda - Numeric vector. Ridge regression penalty for the linear
model. Typically a single value that is broadcast to all design matrix
columns. Ignored when \code{use_dynamic_lambda = TRUE}. Defaults to \code{1.0}.
\item block_size - Numeric. Fraction of cells to update per block during
optimisation (0.0-1.0). Lower values reduce memory usage but increase
computation time. Defaults to \code{0.2}.
\item max_iter_kmeans - Integer. Maximum number of k-means iterations per
Harmony round. Defaults to \code{4L}.
\item max_iter_harmony - Integer. Maximum number of Harmony outer
iterations. Defaults to \code{10L}.
\item epsilon_kmeans - Numeric. Convergence threshold for k-means
clustering. Stops when the relative change in cluster assignments falls
below this value. Defaults to \code{0.001}.
\item epsilon_harmony - Numeric. Convergence threshold for Harmony. Stops
when the relative change in the objective function falls below this value.
Defaults to \code{0.01}.
\item window_size - Integer. Number of previous iterations to consider when
checking convergence. Defaults to \code{3L}.
\item alpha - Numeric. Scaling factor for dynamic lambda estimation. Must be
in (0, 1). Only relevant when \code{use_dynamic_lambda = TRUE}. Defaults to
\code{0.2}.
\item tau - Numeric. Scaling factor for theta based on batch size. A value
of 0 disables batch-size scaling of theta. Defaults to \code{0.0}.
\item batch_proportion_cutoff - Numeric. Cutoff for pruning batches with
small proportions during ridge regression. Defaults to \code{1e-05}.
\item use_dynamic_lambda - Boolean. If \code{TRUE}, lambda is estimated
dynamically per cluster instead of using the fixed \code{lambda} value. Defaults
to \code{FALSE}.
\item The elements of \code{\link[=params_kmeans_defaults]{params_kmeans_defaults()}}, overridden by \code{kmeans},
spliced in at this position.
}
}
\description{
Default parameters for Harmony v2 batch correction
}
