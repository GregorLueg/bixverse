% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_wrappers.R
\name{params_sc_knn}
\alias{params_sc_knn}
\title{Parameters for single cell kNN searches}
\usage{
params_sc_knn(
  k = 15L,
  knn_method = "kmknn",
  ann_dist = "euclidean",
  n_trees = 50L,
  search_budget = NULL,
  delta = 0.001,
  diversify_prob = 0,
  ef_budget = NULL,
  m = 16L,
  ef_construction = 200L,
  ef_search = 100L,
  n_list = NULL,
  n_probe = NULL
)
}
\arguments{
\item{k}{Integer. Number of neighbours. Defaults to \code{15L}.}

\item{knn_method}{String. Which method to use for the approximate nearest
neighbour search. Defaults to \code{"kmknn"}. One of
\code{c("kmknn", "hnsw", "annoy", "nndescent", "ivf", "exhaustive")}.}

\item{ann_dist}{String. Distance metric to use. Defaults to \code{"euclidean"}.
One of \code{c("cosine", "euclidean")}.}

\item{n_trees}{Integer. Annoy param: number of trees. Defaults to \code{50L}.}

\item{search_budget}{Integer or \code{NULL}. Annoy param: optional search budget
per tree. If \code{NULL}, defaults to \code{n_trees * k * 20L} internally.}

\item{delta}{Numeric. NNDescent param: early termination criterion.
Defaults to \code{0.001}.}

\item{diversify_prob}{Numeric. NNDescent param: diversification probability
applied at the end of index construction. Defaults to \code{0.0}.}

\item{ef_budget}{Integer or \code{NULL}. NNDescent param: optional query budget.
Higher values improve recall at the cost of speed.}

\item{m}{Integer. HNSW param: number of connections between layers.
Defaults to \code{16L}.}

\item{ef_construction}{Integer. HNSW param: size of the dynamic candidate
list during construction. Defaults to \code{200L}.}

\item{ef_search}{Integer. HNSW param: size of the candidate list at query
time. Higher values improve recall at the cost of speed. Defaults to \code{100L}.}

\item{n_list}{Integer or \code{NULL}. IVF param: number of clusters to generate.
If \code{NULL}, defaults to \code{sqrt(n)} internally.}

\item{n_probe}{Integer or \code{NULL}. IVF param: number of clusters to query.
If \code{NULL}, defaults to \code{sqrt(n_list)} internally.}
}
\value{
A list with the kNN parameters.
}
\description{
Parameters for single cell kNN searches
}
