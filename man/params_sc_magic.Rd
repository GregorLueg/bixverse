% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_constructors.R
\name{params_sc_magic}
\alias{params_sc_magic}
\title{Wrapper function for MAGIC imputation parameters}
\usage{
params_sc_magic(
  n_steps = 3L,
  clip_threshold = 0.01,
  gene_batch_size = 1000L,
  layer = c("norm", "raw"),
  allow_large = FALSE
)
}
\arguments{
\item{n_steps}{Integer. Diffusion steps applied to the counts. Defaults to
\code{3L}. Zero is legal and hands back the un-imputed values, which is a cheap
way to compare the two.}

\item{clip_threshold}{Numeric. Imputed values below this are zeroed after
the last step. Defaults to \code{0.01}.}

\item{gene_batch_size}{Integer. Genes streamed off the binary store per
block. Bounds the scratch memory and is clamped to the number of requested
genes. Defaults to \code{1000L}.}

\item{layer}{String. One of \code{c("norm", "raw")}. Which stored layer to
impute. The operator preserves per-cell mass, so imputed values sit on the
scale of whatever went in: imputing raw counts and imputing log-normalised
counts are different operations rather than the same one rescaled. Defaults
to \code{"norm"}.}

\item{allow_large}{Boolean. Skip the output size guard. The dense output is
capped at 1e9 elements, i.e. 4 GB of \code{f32}. Defaults to \code{FALSE}.}
}
\value{
A named flat list with all MAGIC parameters.
}
\description{
Parameters controlling the MAGIC imputation run by
\code{\link[=run_magic_sc]{run_magic_sc()}}. The defaults mirror the reference
implementation.
}
\references{
van Dijk, et al., Cell, 2018.
}
