% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_constructors.R
\name{params_sc_palantir}
\alias{params_sc_palantir}
\title{Wrapper function for Palantir parameters}
\usage{
params_sc_palantir(
  n_dcs = 10L,
  n_eigs = NULL,
  knn = 30L,
  num_waypoints = 1200L,
  scale_components = TRUE,
  use_early_cell_as_start = FALSE,
  max_iterations = 25L,
  branch_prob_threshold = 0.01,
  lanczos_basis_size = NULL,
  lanczos_max_restarts = 16L,
  lanczos_tol = 1e-08,
  knn_params = list()
)
}
\arguments{
\item{n_dcs}{Integer. Diffusion components to extract before the multiscale
scaling. Defaults to \code{10L}.}

\item{n_eigs}{Optional integer. Multiscale components to retain. If \code{NULL},
the count is picked from the largest eigengap, as the reference does.}

\item{knn}{Integer. Neighbours for the geodesic graph over the multiscale
space, in the reference's self-inclusive convention. Defaults to \code{30L}.}

\item{num_waypoints}{Integer. Target waypoint count for the max-min sampler.
Defaults to \code{1200L}.}

\item{scale_components}{Boolean. Min-max scale each multiscale component to
\verb{[0, 1]} before any distance is taken. Defaults to \code{TRUE}.}

\item{use_early_cell_as_start}{Boolean. Use the provided early cell directly
rather than snapping it to the nearest diffusion-map boundary cell. Defaults
to \code{FALSE}.}

\item{max_iterations}{Integer. Iteration cap for the pseudotime refinement.
Defaults to \code{25L}.}

\item{branch_prob_threshold}{Numeric. Fate probabilities below this are
zeroed. Defaults to \code{0.01}.}

\item{lanczos_basis_size}{Optional integer. Krylov basis vectors held at once
during the diffusion-map eigendecomposition. If \code{NULL}, derived from the
requested component count.}

\item{lanczos_max_restarts}{Integer. Maximum restart cycles for the Lanczos
solver. Defaults to \code{16L}.}

\item{lanczos_tol}{Numeric. Relative residual tolerance for the Lanczos
solver. Defaults to \code{1e-8}.}

\item{knn_params}{List. Optional overrides for the kNN parameters of the
internal multiscale search. See \code{\link[=params_knn_defaults]{params_knn_defaults()}} for
available parameters: \code{k}, \code{knn_method}, \code{ann_dist}, \code{search_budget},
\code{n_trees}, \code{delta}, \code{diversify_prob}, \code{ef_budget}, \code{m}, \code{ef_construction},
\code{ef_search}, \code{n_list} and \code{n_probe}.}
}
\value{
A named flat list with all Palantir parameters.
}
\description{
Parameters controlling the Palantir trajectory inference. The kNN graph you
hand to \code{\link[=run_palantir_sc]{run_palantir_sc()}} feeds the diffusion kernel. The
geodesics are measured over a second kNN graph that Palantir builds
internally on the multiscale space, and \code{knn} is what controls that one. It
is a different knob from \code{k} in the kNN parameter block, which sizes the
backend index. Palantir overrides \code{k} and \code{ann_dist} for its internal search,
so only \code{knn_method} and the backend tuning parameters have an effect.
}
\references{
Setty, et al., Nat. Biotechnol., 2019.
}
