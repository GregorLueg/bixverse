% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-generated.R
\name{params_sc_sctransform}
\alias{params_sc_sctransform}
\title{Wrapper function for scTransform (v2) parameters}
\usage{
params_sc_sctransform(
  n_genes = 2000L,
  n_cells = 2000L,
  min_cells = 5L,
  bw_adjust = 3,
  gmean_eps = 1,
  outlier_th = 10,
  poisson_diff_theta = 0.001,
  clip_min = NULL,
  clip_max = NULL
)
}
\arguments{
\item{n_genes}{Integer. Genes in the step-1 subsample. Defaults to \code{2000L}.}

\item{n_cells}{Integer. Cells in the step-1 subsample. Defaults to \code{2000L}.}

\item{min_cells}{Integer. Minimum number of cells a gene must be detected in
to be modelled. Defaults to \code{5L}.}

\item{bw_adjust}{Numeric. Bandwidth multiplier for the kernel regression that
regularises the parameters. Defaults to \code{3.0}.}

\item{gmean_eps}{Numeric. Offset in the geometric mean. Defaults to \code{1.0}.}

\item{outlier_th}{Numeric. Threshold, in median absolute deviations, past
which a step-1 fit is treated as an outlier. Defaults to \code{10.0}.}

\item{poisson_diff_theta}{Numeric. Below this, the fitted dispersion is taken
as the Poisson limit. Defaults to \code{0.001}.}

\item{clip_min}{Numeric or \code{NULL}. Lower residual clipping bound. \code{NULL} uses
\code{-sqrt(n_cells)}. Must be given together with \code{clip_max}. Defaults to \code{NULL}.}

\item{clip_max}{Numeric or \code{NULL}. Upper residual clipping bound. \code{NULL} uses
\code{sqrt(n_cells)}. Must be given together with \code{clip_min}. Defaults to \code{NULL}.}
}
\value{
A named list with the following elements:
\itemize{
\item n_genes - Integer. Genes in the step-1 subsample. Defaults to \code{2000L}.
\item n_cells - Integer. Cells in the step-1 subsample. Defaults to \code{2000L}.
\item min_cells - Integer. Minimum number of cells a gene must be detected
in to be modelled. Defaults to \code{5L}.
\item bw_adjust - Numeric. Bandwidth multiplier for the kernel regression
that regularises the parameters. Defaults to \code{3.0}.
\item gmean_eps - Numeric. Offset in the geometric mean. Defaults to \code{1.0}.
\item outlier_th - Numeric. Threshold, in median absolute deviations, past
which a step-1 fit is treated as an outlier. Defaults to \code{10.0}.
\item poisson_diff_theta - Numeric. Below this, the fitted dispersion is
taken as the Poisson limit. Defaults to \code{0.001}.
\item clip_min - Numeric or \code{NULL}. Lower residual clipping bound. \code{NULL}
uses \code{-sqrt(n_cells)}. Must be given together with \code{clip_max}. Defaults to
\code{NULL}.
\item clip_max - Numeric or \code{NULL}. Upper residual clipping bound. \code{NULL}
uses \code{sqrt(n_cells)}. Must be given together with \code{clip_min}. Defaults to
\code{NULL}.
}
}
\description{
Defaults are sctransform's own with \code{vst.flavor = "v2"} applied.
Only the step-1 fit scales with \code{n_genes} and \code{n_cells}: those bound the
subsample the negative binomial models are fitted on, and every later pass
streams gene by gene. Raising them costs fitting time, not memory.
}
\references{
Choudhary and Satija, Genome Biology, 2022.
}
