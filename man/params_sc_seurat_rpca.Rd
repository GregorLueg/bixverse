% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-generated.R
\name{params_sc_seurat_rpca}
\alias{params_sc_seurat_rpca}
\title{Wrapper function for the Seurat rPCA parameters}
\usage{
params_sc_seurat_rpca(
  dims = 30L,
  k_anchor = 5L,
  k_score = 30L,
  k_weight = 100L,
  l2_norm = TRUE,
  sd = 1,
  knn = list(),
  pca = params_sc_pca()
)
}
\arguments{
\item{dims}{Integer. Number of dimensions used for the per-batch PCA
projections, the anchor kNN queries and the size of the returned embedding.
Defaults to \code{30L}.}

\item{k_anchor}{Integer. Neighbourhood size for the mutual nearest neighbour
anchor search. Defaults to \code{5L}.}

\item{k_score}{Integer. Neighbourhood size for the shared-neighbour anchor
scoring. Defaults to \code{30L}.}

\item{k_weight}{Integer. Neighbourhood size for the kernel weights applied
during the correction. Defaults to \code{100L}.}

\item{l2_norm}{Boolean. Shall the projected embeddings be L2-normalised per
cell. Defaults to \code{TRUE}.}

\item{sd}{Numeric. Bandwidth divisor of the Gaussian kernel used for the
anchor weights. Defaults to \code{1.0}.}

\item{knn}{List. Optional overrides for kNN parameters. See
\code{\link[=params_knn_defaults]{params_knn_defaults()}} for available parameters: \code{k},
\code{knn_method}, \code{ann_dist}, \code{search_budget}, \code{n_trees}, \code{delta},
\code{diversify_prob}, \code{ef_budget}, \code{extract_knn}, \code{m}, \code{ef_construction},
\code{ef_search}, \code{n_list} and \code{n_probe}. Note that \code{k} is unused here, the
neighbourhood sizes come from \code{k_anchor}, \code{k_score} and \code{k_weight}. See
\code{\link[=params_knn_defaults]{params_knn_defaults()}} for the available elements. Defaults to \code{list()}.}

\item{pca}{List. Parameters to feed through to the optional recalculation of
the PCA, see \code{\link[=params_sc_pca]{params_sc_pca()}}. See \code{\link[=params_sc_pca]{params_sc_pca()}} for the available
elements. Defaults to \code{params_sc_pca()}.}
}
\value{
A named list with the following elements:
\itemize{
\item dims - Integer. Number of dimensions used for the per-batch PCA
projections, the anchor kNN queries and the size of the returned embedding.
Defaults to \code{30L}.
\item k_anchor - Integer. Neighbourhood size for the mutual nearest
neighbour anchor search. Defaults to \code{5L}.
\item k_score - Integer. Neighbourhood size for the shared-neighbour anchor
scoring. Defaults to \code{30L}.
\item k_weight - Integer. Neighbourhood size for the kernel weights applied
during the correction. Defaults to \code{100L}.
\item l2_norm - Boolean. Shall the projected embeddings be L2-normalised per
cell. Defaults to \code{TRUE}.
\item sd - Numeric. Bandwidth divisor of the Gaussian kernel used for the
anchor weights. Defaults to \code{1.0}.
\item The elements of \code{\link[=params_knn_defaults]{params_knn_defaults()}}, overridden by \code{knn}, spliced
in at this position.
\item The elements of \code{\link[=params_sc_pca]{params_sc_pca()}}, overridden by \code{pca}, spliced in at
this position.
}
}
\description{
Wrapper function for the Seurat rPCA parameters
}
\references{
Stuart, et al., Cell, 2019
}
