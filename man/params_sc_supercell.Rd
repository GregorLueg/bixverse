% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-generated.R
\name{params_sc_supercell}
\alias{params_sc_supercell}
\title{Wrapper function for parameters for SuperCell generation}
\usage{
params_sc_supercell(
  walk_length = 3L,
  graining_factor = 20,
  use_kernel = TRUE,
  k_ith = NULL,
  max_support = NULL,
  knn = list()
)
}
\arguments{
\item{walk_length}{Integer. Walk length for the Walktrap algorithm. Defaults
to \code{3L}.}

\item{graining_factor}{Numeric. Graining level of data (proportion of number
of single cells in the initial dataset to the number of metacells in the
final dataset). (One meta cell per 20 cells.) Defaults to \code{20.0}.}

\item{use_kernel}{Boolean. Shall a kernel function akin to MAGIC be applied
akin to the approach in SuperCell2, see Hérault, et al., bioRxiv, 2026 and
van Dijk, et al., Cell, 2018. Defaults to \code{TRUE}.}

\item{k_ith}{Integer or \code{NULL}. The k-ith neighbour to use for the kernel.
Defaults to \code{NULL}.}

\item{max_support}{Integer or \code{NULL}. Caps each cell's walk-probability
vector to its top entries by mass, bounding memory at ~\code{max_support * n_cells} on large data. Makes the result an approximation. \code{NULL} (default)
keeps the walks exact. Defaults to \code{NULL}.}

\item{knn}{List. Optional overrides for kNN parameters. See
\code{\link[=params_knn_defaults]{params_knn_defaults()}} for available parameters: \code{k},
\code{knn_method}, \code{ann_dist}, \code{search_budget}, \code{n_trees}, \code{delta},
\code{diversify_prob}, \code{ef_budget}, \code{extract_knn}, \code{m}, \code{ef_construction},
\code{ef_search}, \code{n_list} and \code{n_probe}. See \code{\link[=params_knn_defaults]{params_knn_defaults()}} for the
available elements. Defaults to \code{list()}.}
}
\value{
A named list with the following elements:
\itemize{
\item walk_length - Integer. Walk length for the Walktrap algorithm.
Defaults to \code{3L}.
\item graining_factor - Numeric. Graining level of data (proportion of
number of single cells in the initial dataset to the number of metacells in
the final dataset). (One meta cell per 20 cells.) Defaults to \code{20.0}.
\item use_kernel - Boolean. Shall a kernel function akin to MAGIC be applied
akin to the approach in SuperCell2, see Hérault, et al., bioRxiv, 2026 and
van Dijk, et al., Cell, 2018. Defaults to \code{TRUE}.
\item k_ith - Integer or \code{NULL}. The k-ith neighbour to use for the kernel.
Defaults to \code{NULL}.
\item max_support - Integer or \code{NULL}. Caps each cell's walk-probability
vector to its top entries by mass, bounding memory at ~\code{max_support * n_cells} on large data. Makes the result an approximation. \code{NULL} (default)
keeps the walks exact. Defaults to \code{NULL}.
\item The elements of \code{\link[=params_knn_defaults]{params_knn_defaults()}}, overridden by \code{knn}, spliced
in at this position.
}
}
\description{
Wrapper function for parameters for SuperCell generation
}
