% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-generated.R
\name{params_sc_wnn}
\alias{params_sc_wnn}
\title{Wrapper function for WNN parameters}
\usage{
params_sc_wnn(
  k_nn = 20L,
  knn_range = 200L,
  sigma_method = c("snn_farthest", "sigma_idx"),
  sigma_idx = 19L,
  snn_type = c("full_connection", "limited"),
  s_nn = 20L,
  sd_scale = 1,
  kernel_power = 1,
  cross_const = 1e-04,
  sigma_floor = 1e-08,
  knn = list()
)
}
\arguments{
\item{k_nn}{Integer. Final number of multimodal neighbours per cell. Defaults
to \code{20L}.}

\item{knn_range}{Integer. Candidate pool size per modality. Each cell's kNN
input must contain at least this many neighbours. Defaults to \code{200L}.}

\item{sigma_method}{String. Bandwidth method. One of \code{c("snn_farthest", "sigma_idx")}. Defaults to \code{"snn_farthest"}.}

\item{sigma_idx}{Integer. \code{"sigma_idx"} only: 0-based kNN index for
bandwidth.e. \code{k_nn - 1}). Defaults to \code{19L}.}

\item{snn_type}{String. sNN type. The limited version only considers edges
that exist in the kNN. One of \code{c("full_connection", "limited")}. Defaults to
\code{"full_connection"}.}

\item{s_nn}{Integer. \code{"snn_farthest"} only: kNN size used to build the SNN
graph. Defaults to \code{20L}.}

\item{sd_scale}{Numeric. Multiplier on sigma. Defaults to \code{1.0}.}

\item{kernel_power}{Numeric. Kernel exponent power. Defaults to \code{1.0}.}

\item{cross_const}{Numeric. Cross-modality kernel stabiliser. Defaults to
\code{1e-04}.}

\item{sigma_floor}{Numeric. Minimum sigma value (avoids division by zero).
Defaults to \code{1e-08}.}

\item{knn}{List. Optional overrides for kNN parameters. See
\code{\link[=params_knn_defaults]{params_knn_defaults()}} for available parameters: \code{k},
\code{knn_method}, \code{ann_dist}, \code{search_budget}, \code{n_trees}, \code{delta},
\code{diversify_prob}, \code{ef_budget}, \code{extract_knn}, \code{m}, \code{ef_construction},
\code{ef_search}, \code{n_list} and \code{n_probe}. See \code{\link[=params_knn_defaults]{params_knn_defaults()}} for the
available elements. Defaults to \code{list()}.}
}
\value{
A named list with the following elements:
\itemize{
\item k_nn - Integer. Final number of multimodal neighbours per cell.
Defaults to \code{20L}.
\item knn_range - Integer. Candidate pool size per modality. Each cell's kNN
input must contain at least this many neighbours. Defaults to \code{200L}.
\item sigma_method - String. Bandwidth method. One of \code{c("snn_farthest", "sigma_idx")}. Defaults to \code{"snn_farthest"}.
\item sigma_idx - Integer. \code{"sigma_idx"} only: 0-based kNN index for
bandwidth.e. \code{k_nn - 1}). Defaults to \code{19L}.
\item snn_type - String. sNN type. The limited version only considers edges
that exist in the kNN. One of \code{c("full_connection", "limited")}. Defaults to
\code{"full_connection"}.
\item s_nn - Integer. \code{"snn_farthest"} only: kNN size used to build the SNN
graph. Defaults to \code{20L}.
\item sd_scale - Numeric. Multiplier on sigma. Defaults to \code{1.0}.
\item kernel_power - Numeric. Kernel exponent power. Defaults to \code{1.0}.
\item cross_const - Numeric. Cross-modality kernel stabiliser. Defaults to
\code{1e-04}.
\item sigma_floor - Numeric. Minimum sigma value (avoids division by zero).
Defaults to \code{1e-08}.
\item The elements of \code{\link[=params_knn_defaults]{params_knn_defaults()}}, overridden by \code{knn}, spliced
in at this position.
}
}
\description{
Wrapper function for WNN parameters
}
