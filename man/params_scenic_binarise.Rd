% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_constructors.R
\name{params_scenic_binarise}
\alias{params_scenic_binarise}
\title{Wrapper function for parameters for the SCENIC binarisation}
\usage{
params_scenic_binarise(bw_adjust = 1, n_grid = 512L, n_bins = 512L)
}
\arguments{
\item{bw_adjust}{Float. Multiplier on the Silverman bandwidth of the kernel
density estimate. Higher values smooth more. Defaults to \code{1}.}

\item{n_grid}{Integer. Number of points at which the density is evaluated
between the two component means. Defaults to \code{512L}.}

\item{n_bins}{Integer. Number of histogram bins used to approximate the
density. Defaults to \code{512L}.}
}
\value{
A list with the binarisation parameters.
}
\description{
Each regulon gets its own threshold. A two-component Gaussian mixture is
fitted and compared against a single Gaussian by BIC; if the mixture wins,
the threshold is the kernel density minimum between the two component means,
otherwise it falls back to \code{mean + 2 * sd}. This follows pySCENIC. AUCell
fits six candidates and then lets the density trough override all of them
whenever one exists, so the two land in much the same place.

Turn \code{bw_adjust} up if shallow wobbles in the density are being picked up as
troughs. AUCell effectively runs at \code{2}.
}
\references{
Aibar, et al., Nat Methods, 2017
}
