% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_constructors.R
\name{params_scenic_gradient_boosting_defaults}
\alias{params_scenic_gradient_boosting_defaults}
\title{Default parameters for the SCENIC GradientBoosting (GRNBoost2) regression
learner}
\usage{
params_scenic_gradient_boosting_defaults()
}
\value{
A list with the following parameters:
\itemize{
\item n_trees_max - Integer. Maximum number of boosting rounds. Early
stopping usually triggers well before this limit. Defaults to \code{1000L}.
\item learning_rate - Numeric. Shrinkage applied to each tree's
predictions. Defaults to \code{0.01}.
\item max_depth - Integer. Maximum depth of each tree. Shallow trees
(3-5) work best for GBM. Defaults to \code{3L}.
\item min_samples_leaf - Integer. Minimum number of training samples
required at a leaf node. Defaults to \code{50L}.
\item early_stop_window - Integer. Number of recent OOB improvements to
average for the early stopping criterion. Stops when the rolling average
drops to zero or below. Defaults to \code{25L}.
\item subsample_rate - Numeric. Fraction of samples used for training
each tree. The complement forms the OOB set. Defaults to \code{0.9}.
\item n_features_split - Integer. Number of features to evaluate per
split. \code{0L} means all features (recommended with histogram subtraction).
Defaults to \code{0L}.
}
}
\description{
Default parameters for the SCENIC GradientBoosting (GRNBoost2) regression
learner
}
