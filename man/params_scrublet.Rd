% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-generated.R
\name{params_scrublet}
\alias{params_scrublet}
\title{Wrapper function for Scrublet doublet detection parameters}
\usage{
params_scrublet(
  sim_doublet_ratio = 1.5,
  expected_doublet_rate = 0.1,
  stdev_doublet_rate = 0.02,
  n_bins_histogram = 100L,
  manual_threshold = NULL,
  normalisation = list(),
  hvg = list(),
  pca = list(),
  knn = list(k = 0L)
)
}
\arguments{
\item{sim_doublet_ratio}{Numeric. Number of doublets to simulate relative to
the number of observed cells. For example, 2.0 simulates twice as many
doublets as there are cells. Defaults to \code{1.5}.}

\item{expected_doublet_rate}{Numeric. Expected doublet rate for the
experiment, typically 0.05-0.10 depending on cell loading. Must be between 0
and 1. Defaults to \code{0.1}.}

\item{stdev_doublet_rate}{Numeric. Uncertainty in the expected doublet rate.
Defaults to \code{0.02}.}

\item{n_bins_histogram}{Integer. Number of bins for histogram-based automatic
threshold detection. Typically 50-100. Defaults to \code{100L}.}

\item{manual_threshold}{Numeric or \code{NULL}. Manual doublet score threshold. If
\code{NULL} (default), threshold is automatically detected from simulated doublet
score distribution. Defaults to \code{NULL}.}

\item{normalisation}{List. Optional overrides for normalisation parameters.
See \code{\link[=params_norm_doublets_defaults]{params_norm_doublets_defaults()}} for available parameters:
\code{log_transform}, \code{mean_center}, \code{normalise_variance}, \code{target_size}. See
\code{\link[=params_norm_doublets_defaults]{params_norm_doublets_defaults()}} for the available elements. Defaults to
\code{list()}.}

\item{hvg}{List. Optional overrides for highly variable gene selection
parameters. See \code{\link[=params_hvg_defaults]{params_hvg_defaults()}} for available parameters:
\code{min_gene_var_pctl}, \code{hvg_method}, \code{loess_span}, \code{clip_max}. See
\code{\link[=params_hvg_defaults]{params_hvg_defaults()}} for the available elements. Defaults to \code{list()}.}

\item{pca}{List. Optional overrides for PCA parameters. See
\code{\link[=params_pca_defaults]{params_pca_defaults()}} for available parameters: \code{no_pcs},
\code{random_svd}, \code{sparse} and \code{skip_first_pc}. See \code{\link[=params_pca_defaults]{params_pca_defaults()}} for
the available elements. Defaults to \code{list()}.}

\item{knn}{List. Optional overrides for kNN parameters. See
\code{\link[=params_knn_defaults]{params_knn_defaults()}} for available parameters: \code{k},
\code{knn_method}, \code{ann_dist}, \code{search_budget}, \code{n_trees}, \code{delta},
\code{diversify_prob}, \code{ef_budget}, \code{extract_knn}, \code{m}, \code{ef_construction},
\code{ef_search}, \code{n_list} and \code{n_probe}. Note: this function defaults to \code{k = 0L}
(automatic neighbour detection). See \code{\link[=params_knn_defaults]{params_knn_defaults()}} for the
available elements. Defaults to \code{list(k = 0L)}.}
}
\value{
A named list with the following elements:
\itemize{
\item The elements of \code{\link[=params_norm_doublets_defaults]{params_norm_doublets_defaults()}}, overridden by
\code{normalisation}, spliced in at this position.
\item The elements of \code{\link[=params_hvg_defaults]{params_hvg_defaults()}}, overridden by \code{hvg}, spliced
in at this position.
\item The elements of \code{\link[=params_pca_defaults]{params_pca_defaults()}}, overridden by \code{pca}, spliced
in at this position.
\item The elements of \code{\link[=params_knn_defaults]{params_knn_defaults()}}, overridden by \code{knn}, spliced
in at this position.
\item sim_doublet_ratio - Numeric. Number of doublets to simulate relative
to the number of observed cells. For example, 2.0 simulates twice as many
doublets as there are cells. Defaults to \code{1.5}.
\item expected_doublet_rate - Numeric. Expected doublet rate for the
experiment, typically 0.05-0.10 depending on cell loading. Must be between 0
and 1. Defaults to \code{0.1}.
\item stdev_doublet_rate - Numeric. Uncertainty in the expected doublet
rate. Defaults to \code{0.02}.
\item n_bins_histogram - Integer. Number of bins for histogram-based
automatic threshold detection. Typically 50-100. Defaults to \code{100L}.
\item manual_threshold - Numeric or \code{NULL}. Manual doublet score threshold.
If \code{NULL} (default), threshold is automatically detected from simulated
doublet score distribution. Defaults to \code{NULL}.
}
}
\description{
Constructor for the various Scrublet parameters. In this case,
the default for the kNN graph generation was set to \code{"hnsw"} as this
algorithm showed the best performance in different empirical benchmarks.
}
