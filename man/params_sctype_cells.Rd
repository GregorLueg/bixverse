% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_wrappers.R
\name{params_sctype_cells}
\alias{params_sctype_cells}
\title{Parameters for the per-cell ScType assignment}
\usage{
params_sctype_cells(
  alpha = 0.5,
  iterations = 2L,
  tolerance = 1e-04,
  calibration = c("none", "column_z"),
  score_floor = 0.25,
  purity_threshold = 0.9
)
}
\arguments{
\item{alpha}{Numeric in \verb{[0, 1]}. Self-retention during smoothing. Each
iteration computes \code{alpha * original + (1 - alpha) * neighbour_average}.}

\item{iterations}{Integer >= 0. Number of smoothing iterations. \code{0} disables
smoothing.}

\item{tolerance}{Numeric > 0. Convergence tolerance for the smoothing.}

\item{calibration}{String. One of \code{c("none", "column_z")}. \code{"column_z"}
standardises each cell type's score column across cells, which removes the
bias towards cell types whose marker sets happen to produce larger scores.}

\item{score_floor}{Numeric >= 0. Minimum score for a cell to get a call
instead of \code{NA}.}

\item{purity_threshold}{Numeric in \verb{[0, 1]}. Cluster purity above which the
hybrid assignment keeps the cluster-level call.}
}
\value{
A list with the parameters for usage in subsequent functions.
}
\description{
Controls the per-cell path of \code{\link[=assign_sc_type]{assign_sc_type()}}: how the raw ScType scores
are rescaled, how hard the scores get smoothed over the sNN graph, and where
the cut-offs for an Unknown call and for a mixed cluster sit.
}
\references{
Zhou et al., NIPS, 2004.
}
