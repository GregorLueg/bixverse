% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-generated.R
\name{params_sctype_cells}
\alias{params_sctype_cells}
\title{Parameters for the per-cell ScType assignment}
\usage{
params_sctype_cells(
  alpha = 0.5,
  iterations = 2L,
  tolerance = 1e-04,
  calibration = c("none", "column_z"),
  score_floor = 0.25,
  purity_threshold = 0.9
)
}
\arguments{
\item{alpha}{Numeric. Numeric in \verb{[0, 1]}. Self-retention during smoothing.
Each iteration computes \code{alpha * original + (1 - alpha) * neighbour_average}.
Defaults to \code{0.5}.}

\item{iterations}{Integer. Integer >= 0. Number of smoothing iterations. \code{0}
disables smoothing. Defaults to \code{2L}.}

\item{tolerance}{Numeric. Numeric > 0. Convergence tolerance for the
smoothing. Defaults to \code{1e-04}.}

\item{calibration}{String. \code{"column_z"} standardises each cell type's score
column across cells, which removes the bias towards cell types whose marker
sets happen to produce larger scores. One of \code{c("none", "column_z")}.
Defaults to \code{"none"}.}

\item{score_floor}{Numeric. Numeric >= 0. Minimum score for a cell to get a
call instead of \code{NA}. Defaults to \code{0.25}.}

\item{purity_threshold}{Numeric. Numeric in \verb{[0, 1]}. Cluster purity above
which the hybrid assignment keeps the cluster-level call. Defaults to \code{0.9}.}
}
\value{
A named list with the following elements:
\itemize{
\item alpha - Numeric. Numeric in \verb{[0, 1]}. Self-retention during smoothing.
Each iteration computes \code{alpha * original + (1 - alpha) * neighbour_average}. Defaults to \code{0.5}.
\item iterations - Integer. Integer >= 0. Number of smoothing iterations.
\code{0} disables smoothing. Defaults to \code{2L}.
\item tolerance - Numeric. Numeric > 0. Convergence tolerance for the
smoothing. Defaults to \code{1e-04}.
\item calibration - String. \code{"column_z"} standardises each cell type's score
column across cells, which removes the bias towards cell types whose marker
sets happen to produce larger scores. One of \code{c("none", "column_z")}.
Defaults to \code{"none"}.
\item score_floor - Numeric. Numeric >= 0. Minimum score for a cell to get a
call instead of \code{NA}. Defaults to \code{0.25}.
\item purity_threshold - Numeric. Numeric in \verb{[0, 1]}. Cluster purity above
which the hybrid assignment keeps the cluster-level call. Defaults to \code{0.9}.
}
}
\description{
Controls the per-cell path of \code{\link[=assign_sc_type]{assign_sc_type()}}: how the raw
ScType scores are rescaled, how hard the scores get smoothed over the sNN
graph, and where the cut-offs for an Unknown call and for a mixed cluster
sit.
}
\references{
Zhou et al., NIPS, 2004.
}
