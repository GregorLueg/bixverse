% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_wrappers.R
\name{params_sp_graph}
\alias{params_sp_graph}
\title{Wrapper function to provide the spatial graph parameters}
\usage{
params_sp_graph(
  layout = c("hex", "square", "knn", "radius"),
  weighting = c("binary", "inverse_distance", "gaussian"),
  connectivity = 4L,
  k = 6L,
  radius = NULL,
  power = 1,
  bandwidth = NULL,
  row_normalise = FALSE
)
}
\arguments{
\item{layout}{String. One of \code{c("hex", "square", "knn", "radius")}. \code{"hex"}
is the Visium lattice, \code{"square"} the Visium HD one.}

\item{weighting}{String. One of \code{c("binary", "inverse_distance", "gaussian")}.}

\item{connectivity}{Integer. \code{4L} or \code{8L}. Only used by \code{"square"}.}

\item{k}{Integer. Neighbours per spot. Only used by \code{"knn"}.}

\item{radius}{Float or \code{NULL}. Cut-off in full-resolution pixels. Required by
\code{"radius"} and ignored otherwise.}

\item{power}{Float. Decay exponent for \code{"inverse_distance"}.}

\item{bandwidth}{Float or \code{NULL}. Gaussian bandwidth. \code{NULL} resolves to the
median nearest-neighbour distance inside Rust.}

\item{row_normalise}{Boolean. Normalise the weights per row, i.e. spdep's
\code{style = "W"}.}
}
\value{
A list with the spatial graph parameters for usage in subsequent
functions.
}
\description{
Controls how \code{\link[=build_spatial_graph_sp]{build_spatial_graph_sp()}} turns spot coordinates into
the neighbour and weight lists every other spatial statistic consumes. The
two lattice layouts are exact table look-ups over the array coordinates and
never compute a distance, which is both faster and more robust than a kNN on
a regular grid. \code{"knn"} and \code{"radius"} cover irregular geometries.

\code{array_row} and \code{array_col} are not arguments here. They come out of the obs
table of the object and are injected by the method.
}
