% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_wrappers.R
\name{params_sp_h5ad_io}
\alias{params_sp_h5ad_io}
\title{Wrapper function to provide the spatial h5ad ingest parameters}
\usage{
params_sp_h5ad_io(
  library_id = NULL,
  assume_orientation = c("xy", "yx"),
  in_tissue_only = TRUE,
  technology = c("visium", "visium_hd", "xenium")
)
}
\arguments{
\item{library_id}{Optional string. Which \code{uns/spatial} library to read.
\code{NULL} takes the only one and errors when the file holds several.}

\item{assume_orientation}{String. One of \code{c("xy", "yx")}. Column order of
\code{obsm/spatial} to fall back on when the file settles nothing.}

\item{in_tissue_only}{Boolean. Keep only spots with \code{in_tissue == 1}, when
the obs table carries that column. Defaults to \code{TRUE}.}

\item{technology}{String. One of \code{c("visium", "visium_hd", "xenium")}.
Recorded on the resulting \code{\link[=new_spatial_sample]{new_spatial_sample()}}.}
}
\value{
A list with the h5ad ingest parameters for usage in subsequent
functions.
}
\description{
Controls how \code{\link[=load_spatial_h5ad]{load_spatial_h5ad()}} reads the spatial extras of an
h5ad. Counts, obs and var go through \code{\link[=load_h5ad]{load_h5ad()}} and are not
configured here.

\code{assume_orientation} is the one worth reading about. Nothing in the AnnData
spec fixes the column order of \code{obsm/spatial}, so the reader works it out
from the file: whether the spots land on the tissue in the shipped image,
failing that whether they fit inside its frame, failing that the \code{obs}
\verb{pxl_*_in_fullres} labels. This parameter is only consulted when none of
that applies, which is a file with no image and no pixel columns. \code{"xy"} is
what \code{scanpy.read_visium} produces and what all 236 files in the survey this
reader was built against hold.

Getting the order wrong costs nothing statistically: a swap of \code{x} and \code{y}
is a reflection, so the graph, Moran's I and SPARK-X are unchanged. It costs
everything for \code{\link[=image_features_sp]{image_features_sp()}}, which would cut every tile
from the transpose of the spot.
}
