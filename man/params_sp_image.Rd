% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_wrappers.R
\name{params_sp_image}
\alias{params_sp_image}
\title{Wrapper function to provide the histology image feature parameters}
\usage{
params_sp_image(
  tile_scale = 1,
  glcm_levels = 32L,
  glcm_offsets_dy = NULL,
  glcm_offsets_dx = NULL,
  stain_haem = NULL,
  stain_eosin = NULL
)
}
\arguments{
\item{tile_scale}{Float. Multiplier on the spot diameter when cutting a
tile.}

\item{glcm_levels}{Integer. Grey levels the GLCM quantises to, 2 to 255.}

\item{glcm_offsets_dy}{Integer vector or \code{NULL}. Row offsets. Must be given
together with \code{glcm_offsets_dx}. \code{NULL} uses the four offsets at distance
one.}

\item{glcm_offsets_dx}{Integer vector or \code{NULL}. Column offsets, aligned
with \code{glcm_offsets_dy}.}

\item{stain_haem}{Numeric of length 3 or \code{NULL}. Haematoxylin stain vector
for the colour deconvolution. Must be given together with \code{stain_eosin}.
\code{NULL} uses the Ruifrok-Johnston H&E basis.}

\item{stain_eosin}{Numeric of length 3 or \code{NULL}. Eosin stain vector.}
}
\value{
A list with the image feature parameters for usage in subsequent
functions.
}
\description{
Controls the tile cut around each spot and the Haralick texture features
taken off it. \code{tile_scale = 1.0} takes the spot itself; anything larger
pulls in surrounding context, which usually helps for tissue architecture
and hurts for anything spot-resolved.

The GLCM offsets are two aligned vectors rather than one interleaved one.
An interleaved vector transposes the whole offset set when the order gets
mixed up and nothing downstream would notice.
}
\references{
Ruifrok & Johnston, Anal Quant Cytol Histol, 2001

Haralick, Shanmugam & Dinstein, IEEE Trans Syst Man Cybern, 1973
}
