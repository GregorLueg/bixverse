% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_wrappers.R
\name{params_sp_sparkx}
\alias{params_sp_sparkx}
\title{Wrapper function to provide the SPARK-X parameters}
\usage{
params_sp_sparkx(
  kernels = NULL,
  n_landmarks = 20L,
  bandwidth_subsample = 1000L
)
}
\arguments{
\item{kernels}{List or \code{NULL}. Each element a list with \code{kernel}
(\code{"gaussian"} or \code{"cosine"}) and \code{bandwidth} (a positive float).}

\item{n_landmarks}{Integer. Nystroem landmarks per Gaussian kernel.}

\item{bandwidth_subsample}{Integer. Sub-sample size used to derive the
default bandwidths.}
}
\value{
A list with the SPARK-X parameters for usage in subsequent
functions.
}
\description{
An empty kernel bank triggers the paper defaults: five Gaussian plus five
cosine bandwidths, taken from quantiles of the pairwise distance
distribution on a sub-sample of the coordinates. Leave \code{kernels} at \code{NULL}
unless you know which bandwidths you want.
}
