% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_wrappers.R
\name{params_sp_visium_io}
\alias{params_sp_visium_io}
\title{Wrapper function to provide the Visium ingest parameters}
\usage{
params_sp_visium_io(
  in_tissue_only = TRUE,
  matrix_type = c("auto", "raw", "filtered"),
  slide_file = NULL
)
}
\arguments{
\item{in_tissue_only}{Boolean. Keep only spots with \code{in_tissue == 1} in the
tissue positions file. Defaults to \code{TRUE}.}

\item{matrix_type}{String. One of \code{c("auto", "raw", "filtered")}. Which of
\verb{raw_feature_bc_matrix/} and \verb{filtered_feature_bc_matrix/} to read.
\code{"auto"} prefers the filtered matrix and falls back to the raw one.}

\item{slide_file}{Optional string. Path to a user-supplied full-resolution
scanner slide. Space Ranger does not ship one; if given, it registers under
the \code{fullres} image key. Defaults to \code{NULL}.}
}
\value{
A list with the Visium ingest parameters for usage in subsequent
functions.
}
\description{
Controls how \code{\link[=load_visium]{load_visium()}} resolves a Space Ranger output
directory. \code{in_tissue_only} matters more than it looks: roughly a fifth of
the spots on a standard capture area sit off-tissue, carry a handful of
counts each and drag every downstream summary around if you keep them.
}
