% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_wrappers.R
\name{params_synthetic_bulk_rnaseq}
\alias{params_synthetic_bulk_rnaseq}
\title{Wrapper function to generate synthetic bulk RNAseq parameters}
\usage{
params_synthetic_bulk_rnaseq(
  num_samples = 100L,
  num_genes = 1000L,
  module_sizes = c(100L, 100L, 100L),
  generator = c("hub_modular", "modular", "non_negative_factor", "non_gaussian_factor"),
  seed = 123L,
  mean_exp_gamma_shape = 5,
  mean_exp_gamma_scale = 10,
  disp_intercept = 0.2,
  disp_slope = 0.3,
  noise_std = 0.1,
  factor_std = 0.5,
  factor_shape = 2,
  factor_scale = 0.3,
  loading_mu = 0,
  loading_sigma = 0.7,
  hub_percentile = 0.1
)
}
\arguments{
\item{num_samples}{Integer. Number of samples (columns) to simulate.}

\item{num_genes}{Integer. Number of genes (rows) to simulate.}

\item{module_sizes}{Integer vector. Sizes of the co-expression modules. The
sum must be smaller or equal to \code{num_genes}. Genes are assigned in
contiguous blocks from the first gene onwards, any remainder is background.
Use \code{integer(0)} for no modules. Must be an integer vector, see the note
below.}

\item{generator}{String. Which topology and distribution family to plant.
One of \code{c("hub_modular", "modular", "non_negative_factor", "non_gaussian_factor")}, see the description. Defaults to \code{"hub_modular"}.}

\item{seed}{Integer. Seed for reproducibility purposes.}

\item{mean_exp_gamma_shape, mean_exp_gamma_scale}{Float. Shape and scale of
the Gamma the per-gene mean expression is drawn from.}

\item{disp_intercept, disp_slope}{Float. Intercept and slope of the negative
binomial dispersion trend \code{disp = 1 / (a + b * mean)}. This is what gives you
heteroskedasticity: lowly expressed genes show higher variance.}

\item{noise_std}{Float. Per-gene per-sample noise standard deviation on the
latent log-signal. Smaller values track the module factor more tightly and
give stronger within-module correlation. Defaults to \code{0.1} rather than the
crate's \code{0.3}, see the note below.}

\item{factor_std}{Float. Standard deviation of the Normal factor. Only used
by \code{"hub_modular"} and \code{"modular"}; the other two generators draw their
factor from \code{factor_shape}/\code{factor_scale} instead. Defaults to \code{0.5} rather
than the crate's \code{0.3}, see the note below.}

\item{factor_shape, factor_scale}{Float. Shape and scale of the Gamma factor
for \code{"non_negative_factor"}. \code{factor_scale} doubles as the Laplace scale for
\code{"non_gaussian_factor"}.}

\item{loading_mu, loading_sigma}{Float. Location and scale of the LogNormal
the loadings are drawn from. Unused by \code{"modular"}, which draws Beta(5, 2).}

\item{hub_percentile}{Float. Top fraction of module genes flagged as hubs by
loading rank. Must be in \verb{(0, 1]}.}
}
\value{
A list with the parameters for usage in subsequent functions.
}
\description{
Parameters for \code{\link[=synthetic_bulk_cor_matrix]{synthetic_bulk_cor_matrix()}}. Counts come from a
negative binomial with a mean-dispersion trend; co-expression modules are
planted by putting each module's genes on a shared latent factor. The
\code{generator} picks how loadings and factors are drawn, which is what makes a
given dataset a fair or unfair benchmark for a given method:

\itemize{
\item \code{"hub_modular"} - LogNormal loadings on a Normal factor. Some genes
end up far more connected than others, so this is the WGCNA-style default.
\item \code{"modular"} - Beta(5, 2) loadings on a Normal factor. Homogeneous
within-module correlation and no hubs.
\item \code{"non_negative_factor"} - LogNormal loadings on a Gamma factor. The
activity matrix is non-negative by construction, so NMF has a ground truth
it can actually reach.
\item \code{"non_gaussian_factor"} - LogNormal loadings on a Laplace factor.
Non-Gaussian sources satisfy ICA identifiability.
}
}
\details{
\code{noise_std} and \code{factor_std} default to \code{0.1} and \code{0.5}, not to the
\code{bixverse-rs} values of \code{0.3} and \code{0.3}. At the crate defaults the
\code{"modular"} generator plants modules too weakly to detect at 1000 genes by
100 samples: the within-module minus cross-module mean absolute Spearman gap
comes out around \code{0.06}, against \code{0.17} to \code{0.23} for the other three. The
values here put all four generators in the \code{0.30} to \code{0.39} band, so a
comparison across generators reflects the method rather than the signal
strength it happened to be handed. Pass the crate values explicitly if you
want a harder problem.
}
\references{
Zhang & Horvath, Stat Appl Genet Mol Biol, 2005
}
