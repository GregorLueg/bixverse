% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_sc_io.R
\name{prescan_mtx_dirs}
\alias{prescan_mtx_dirs}
\title{Prescan multiple mtx directories for a multi-load}
\usage{
prescan_mtx_dirs(
  dirs,
  exp_ids,
  cells_as_rows = FALSE,
  has_hdr = FALSE,
  .verbose = TRUE
)
}
\arguments{
\item{dirs}{Character vector of input directories. Length >= 2.}

\item{exp_ids}{Character vector of experiment identifiers, one per directory.
Must be unique.}

\item{cells_as_rows}{Boolean. Applied uniformly to all inputs. Defaults
to \code{FALSE} (10x convention: genes are rows).}

\item{has_hdr}{Boolean. Whether the barcodes/features files have a
header row. Applied uniformly. Defaults to \code{FALSE} (10x convention).}

\item{.verbose}{Boolean. Controls verbosity of the function.}
}
\value{
A list with:
\itemize{
\item universe - Character vector of gene IDs in the intersection, in the
order they will appear in the final var table.
\item universe_size - Length of the universe.
\item file_tasks - List of per-input task lists for Rust and DuckDB.
\item temp_files - Character vector of temp files created during
decompression; the caller should \code{unlink()} these after use.
}
}
\description{
Walks each input directory, reads the features file to build the
\strong{intersection} of gene IDs across inputs (matched by the first column,
typically Ensembl gene IDs), decompresses any \code{.mtx.gz} files into a
temporary directory, and builds the file tasks expected by
\code{\link[=load_multi_mtx]{load_multi_mtx()}}.

Each input directory must contain the standard 10x trio: a \code{.mtx} (or
\code{.mtx.gz}) file, a barcodes file, and a features/genes file. File names are
matched by extension; if a directory contains multiple matching files an
error is raised.
}
