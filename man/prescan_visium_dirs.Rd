% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_sp_io.R
\name{prescan_visium_dirs}
\alias{prescan_visium_dirs}
\title{Prescan multiple Visium directories for a multi-load}
\usage{
prescan_visium_dirs(dirs, exp_ids, .verbose = TRUE)
}
\arguments{
\item{dirs}{Character vector of Space Ranger output directories. Length >= 2.}

\item{exp_ids}{Character vector of experiment identifiers, one per
directory. Must be unique.}

\item{.verbose}{Boolean. Controls the verbosity of the function.}
}
\value{
A list with:
\itemize{
\item universe - Character vector of gene IDs in the intersection.
\item universe_size - Length of the universe.
\item file_tasks - List of per-input task lists.
\item temp_files - Character vector of temp files created during
decompression; the caller should \code{unlink()} these after use.
}
}
\description{
Walks each Space Ranger output directory, builds the \strong{intersection} of
gene identifiers across inputs (matched on the first column of the features
file), decompresses the mtx files into a temporary directory and assembles
the per-input tasks \code{\link[=load_multi_visium]{load_multi_visium()}} expects.

The count matrix directory is resolved per input, preferring
\verb{filtered_feature_bc_matrix/} over \verb{raw_feature_bc_matrix/}.
}
