% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions_dge.R
\name{pseudobulk_dge_sc}
\alias{pseudobulk_dge_sc}
\title{Run the edgeR quasi-likelihood workflow on pseudo-bulked single cells}
\usage{
pseudobulk_dge_sc(
  object,
  cell_list,
  design,
  coef = NULL,
  contrast = NULL,
  edger_params = params_edger_ql(),
  .verbose = TRUE
)
}
\arguments{
\item{object}{\code{SingleCells} or \code{SingleCellsSubset} class.}

\item{cell_list}{Named list of character vectors. The cell identifiers per
pseudo-bulk sample. The names become the sample identifiers, and the rows of
\code{design} must follow that order.}

\item{design}{Numeric matrix. The design matrix of samples x coefficients,
including the intercept. Rows aligned to \code{cell_list}.}

\item{coef}{Optional integer or character. See \code{\link[=run_edger_ql]{run_edger_ql()}}.}

\item{contrast}{Optional numeric vector or matrix. See
\code{\link[=run_edger_ql]{run_edger_ql()}}.}

\item{edger_params}{A list, see \code{\link[=params_edger_ql]{params_edger_ql()}}.}

\item{.verbose}{Boolean or integer. Controls verbosity and returns run
times. \code{FALSE} -> quiet, \code{TRUE} or \code{1L} -> normal verbosity, \code{2L} ->
detailed verbosity.}
}
\value{
A data.table, see \code{\link[=run_edger_ql]{run_edger_ql()}}. The \code{feature_id}
column holds the gene identifiers.
}
\description{
Sums the raw counts per sample and treats the result as a bulk experiment.
That is the whole method, and it is the one that holds its nominal false
discovery rate when the cells within a sample are not independent, which
they never are.

This is a convenience wrapper: it pseudo-bulks with
\code{\link[=get_pseudobulked_sc]{get_pseudobulked_sc()}} and hands the aggregate to
\code{\link[=run_edger_ql]{run_edger_ql()}}. Reach for the two separately if you want to
inspect or reuse the aggregated matrix.
}
\references{
Squair, et al., Nat Commun, 2021; Chen, Lun and Smyth,
F1000Research, 2016
}
