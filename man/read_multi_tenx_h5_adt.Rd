% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions_h5ad.R
\name{read_multi_tenx_h5_adt}
\alias{read_multi_tenx_h5_adt}
\title{Read in 10x h5 ADT data from multiple files}
\usage{
read_multi_tenx_h5_adt(
  h5_paths,
  feature_type = "Antibody Capture",
  gene_universe = c("intersection", "union")
)
}
\arguments{
\item{h5_paths}{Character vector of file paths to 10x h5 files. If names
are provided, these will be used as \code{exp_id}s; otherwise the file basename
is used.}

\item{feature_type}{String. The feature type to return. Defaults to
\code{"Antibody Capture"}.}

\item{gene_universe}{One of \code{"intersection"} or \code{"union"}.}
}
\value{
A dense matrix of cells x features with \code{exp_id_barcode}
rownames and feature names as colnames.
}
\description{
Multi-file counterpart to \code{\link[=read_tenx_h5_adt]{read_tenx_h5_adt()}}. Reads the same
modality from each input, stacks the cells (rows), and prefixes each
barcode with its \code{exp_id} so the result matches the cell_id convention
used by \code{\link[=load_multi_tenx_h5]{load_multi_tenx_h5()}}. The feature space is either
the intersection or union of features across inputs; missing features
in the union case are filled with zero.
}
