% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/base_generics_sc.R, R/classes_meta_cell.R,
%   R/classes_single_cell_.R, R/classes_single_cell_multimodal.R,
%   R/classes_single_cell_subset.R
\name{reset_cells_to_keep}
\alias{reset_cells_to_keep}
\alias{reset_cells_to_keep.MetaCells}
\alias{reset_cells_to_keep.SingleCells}
\alias{reset_cells_to_keep.SingleCellsMultiModal}
\alias{reset_cells_to_keep.SingleCellsSubset}
\title{Reset the cells to keep}
\usage{
reset_cells_to_keep(object, force = FALSE)

## S7 method for class <bixverse::MetaCells>
reset_cells_to_keep(object, force = FALSE)

## S7 method for class <bixverse::SingleCells>
reset_cells_to_keep(object, force = FALSE)

## S7 method for class <bixverse::SingleCellsMultiModal>
reset_cells_to_keep(object, force = FALSE)

## S7 method for class <bixverse::SingleCellsSubset>
reset_cells_to_keep(object, force = FALSE)
}
\arguments{
\item{object}{\code{SingleCells} or \code{SingleCellsMultiModal} class.}

\item{force}{Boolean. Skip the confirmation prompt. Defaults to \code{FALSE}, in
which case an interactive session asks before wiping the cache and a
non-interactive one errors, because there is no one there to ask.}
}
\value{
The object with every cell restored and an empty cache. Unchanged
if the confirmation was declined.
}
\description{
Restores every cell found in the binary count file and wipes the cache,
taking the object back to a pristine state. Filtering only ever flips a
\code{to_keep} flag in the DuckDB, so nothing was deleted and nothing is lost by
resetting.

Wiping the cache is not optional: a PCA or a kNN computed on a filtered
subset does not describe the full cell set, and keeping it would recreate
exactly the mismatch this guards against. With \code{force = FALSE} you are asked
to confirm before that happens.
}
