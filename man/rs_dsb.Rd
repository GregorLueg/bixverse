% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rs_dsb}
\alias{rs_dsb}
\title{Run DSB normalisation on raw ADT counts}
\usage{
rs_dsb(
  raw_counts,
  background_counts,
  isotype_indices,
  dsb_params,
  scale_factor,
  seed,
  verbose
)
}
\arguments{
\item{raw_counts}{Numeric matrix. Cells x proteins matrix of raw ADT
counts.}

\item{background_counts}{Optional numeric matrix. Cells x proteins matrix
of empty-droplet ADT counts used to estimate per-protein ambient
background. Must have the same number of columns as \code{raw_counts}. If
\code{NULL}, the function falls back to k-means-based background estimation.}

\item{isotype_indices}{Integer vector. 0-based column indices into
\code{raw_counts} identifying isotype control proteins. Used in Step II if
\code{dsb_params$use_isotype_controls = TRUE}. Pass an empty integer vector
if there are no isotype controls.}

\item{dsb_params}{List. DSB parameters.}

\item{scale_factor}{String. One of \code{"standardise"} or \code{"mean_subtract"}.
Only used when \code{background_counts} is provided. \code{"standardise"} subtracts
the per-protein background mean and divides by the per-protein background
SD. \code{"mean_subtract"} subtracts the mean only.}

\item{seed}{Integer. Random seed for k-means initialisation.}

\item{verbose}{Integer. \code{0L} - quiet; \code{1L} - normal verbosity; \code{2L} -
detailed verbosity.}
}
\value{
A list with the following elements:
\itemize{
\item norm_counts - Numeric matrix. The DSB-normalised cells x proteins
matrix.
\item protein_background_mean - Numeric vector of length \code{n_proteins}.
Per-protein background mean used in Step I.
\item protein_background_sd - Numeric vector of length \code{n_proteins}, or
empty vector if \code{background_counts} was \code{NULL}. Per-protein background
SD used in Step I.
\item technical_component - Numeric vector of length \code{n_cells}, or
empty vector if \code{dsb_params$denoise_counts = FALSE}. Per-cell
technical component regressed out in Step II.
\item cellwise_background_mean - Numeric vector of length \code{n_cells},
or empty vector if \code{dsb_params$denoise_counts = FALSE}. Per-cell
background mean from the 2-component k-means clustering.
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
This function applies the DSB algorithm to normalise antibody-derived tag
(ADT) counts from CITE-seq experiments. Two variants are supported. When
\code{background_counts} is provided, per-protein ambient background is estimated
from empty droplets ("Step I" of the original paper). When
\code{background_counts} is \code{NULL}, per-protein background is estimated by a
two-component k-means on the log-transformed cell counts, with the lower
centroid taken as the background level. An optional second step removes
cell-to-cell technical noise by regressing out PC1 of a noise matrix built
from isotype controls (if available) and the per-cell background mean.
}
\keyword{internal}
