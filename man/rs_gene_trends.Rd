% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rs_gene_trends}
\alias{rs_gene_trends}
\title{Fit Palantir gene trends over pseudotime}
\usage{
rs_gene_trends(
  expression,
  pseudotime,
  branch_probs,
  branch_params,
  gene_trend_params
)
}
\arguments{
\item{expression}{Numerical matrix of cells x genes. All cells, in the same
row order as \code{pseudotime}, not just the branch members.}

\item{pseudotime}{Numerical vector. Pseudotime per cell.}

\item{branch_probs}{Numerical matrix of cells x fates with the fate
probabilities. Rows need not sum to one.}

\item{branch_params}{List. Parameter list, see
\code{\link[=params_sc_branch_selection]{params_sc_branch_selection()}}.}

\item{gene_trend_params}{List. Parameter list, see
\code{\link[=params_sc_gene_trends]{params_sc_gene_trends()}}.}
}
\value{
A list with
\itemize{
\item trends - List of numerical matrices, one per branch, each of
resolution x genes.
\item grids - List of numerical vectors with the pseudotime grid per
branch, running from the branch minimum to its maximum.
\item branch_cells - List of integer vectors with the cell indices
(0-indexed!) selected for each branch.
\item n_cells - Integer vector with the cell count per branch.
\item jitter_used - Numerical vector with the jitter each branch's
Cholesky needed.
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
Selects the cells belonging to each branch from the fate probabilities, then
fits a landmark Gaussian process with a Matern-5/2 kernel per branch. This
is the Mellon-based estimator of the reference, not the legacy GAM.

Whatever expression matrix is handed over is what gets fitted. Nothing here
imputes; that decision belongs to the caller.

The defaults are prior-dominated. Palantir's pseudotime is min-max scaled to
\verb{[0, 1]}, so a \code{length_scale} of \code{1.0} spans the whole domain and a \code{sigma}
of \code{1.0} sits at roughly the signal scale of log-normalised expression. That
resolves almost any gene into a smooth monotone or single-peaked curve.
Shorten \code{length_scale} before believing a bump.
}
\references{
Setty, et al., Nat. Biotechnol., 2019.
}
\keyword{internal}
