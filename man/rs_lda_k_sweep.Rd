% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rs_lda_k_sweep}
\alias{rs_lda_k_sweep}
\title{Fit LDA across a range of topic counts and score each fit}
\usage{
rs_lda_k_sweep(sparse_data, k_range, lda_params, top_topics_coh, seed, verbose)
}
\arguments{
\item{sparse_data}{A named list that needs to have \code{data}, \code{indptr},
\code{indices}, \code{nrow}, \code{ncol} and \code{cs_type}. Documents x terms.}

\item{k_range}{Integer vector. Topic counts to evaluate.}

\item{lda_params}{List. The LDA parameters, see \code{\link[=params_lda]{params_lda()}}.}

\item{top_topics_coh}{Optional integer. Number of top-scoring topics
averaged into the reported coherence. If \code{NULL}, defaults to \code{5L}.}

\item{seed}{Integer. Seed for the variational initialisation.}

\item{verbose}{Integer. \code{0L} - quiet; \code{1L} - normal verbosity; \code{2L} -
detailed verbosity.}
}
\value{
A list with the following items
\itemize{
\item k - The topic counts that were tried.
\item models - One fitted model per topic count, see \code{\link[=rs_lda]{rs_lda()}}.
\item metrics - One metric list per topic count, with \code{arun_2010},
\code{cao_juan_2009}, \code{mimno_2011}, \code{coherence_per_topic}, \code{bound} and
\code{perplexity}.
\item combined_score - Rescaled combined score per entry. \code{NaN} where the
entry was excluded from selection by the coherence topic-count floor.
\item best_k - Topic count with the highest combined score.
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
Fits one model per requested topic count and evaluates each with the Arun,
Cao Juan and Mimno metrics. The corpus is built once and shared, and each
fit is already parallel over documents, so the topic counts run
sequentially rather than nesting Rayon pools.
}
\references{
Arun, et al., PAKDD, 2010; Cao, et al., Neurocomputing, 2009;
Mimno, et al., EMNLP, 2011
}
\keyword{internal}
