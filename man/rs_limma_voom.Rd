% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rs_limma_voom}
\alias{rs_limma_voom}
\title{Run the limma linear model chain on a count matrix}
\usage{
rs_limma_voom(counts, design, lib_size, limma_params)
}
\arguments{
\item{counts}{Integer or double matrix. Raw counts of genes x samples. Must
not be normalised or log-transformed.}

\item{design}{Numeric matrix. The design matrix of samples x coefficients,
including the intercept. Must be full rank.}

\item{lib_size}{Numeric vector or NULL. Library size per sample. NULL uses
the column sums of \code{counts}. Pass the column sums from before gene filtering
to match edgeR, which keeps those on a subset \code{DGEList}.}

\item{limma_params}{Named list. The limma parameters, see
\code{\link[=params_limma_voom]{params_limma_voom()}}, plus either \code{coef} (a single 0-indexed(!)
design column) or \code{contrast} (column-major weights with \code{n_contrasts}
columns).}
}
\value{
A list with the following elements, all but \code{features_to_keep}
with one entry per kept gene, in input order
\itemize{
\item features_to_keep - Boolean. Which genes survived the filters. Spans
the full gene axis of \code{counts}.
\item log_fc - Log2 fold changes of the tested coefficient or contrast.
\item ci_lower - Lower end of the 95\% confidence interval on \code{log_fc}.
\item ci_upper - Upper end of the 95\% confidence interval on \code{log_fc}.
\item ave_expr - Average log2 counts per million.
\item t_stat - Moderated t statistic.
\item p_values - Raw p-values.
\item fdr - Benjamini-Hochberg adjusted p-values.
\item b_stat - Log-odds of differential expression.
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
Runs optional \code{filterByExpr} -> \code{calcNormFactors} -> \code{voomLmFit} (or
limma-trend) -> \code{contrasts.fit} -> \code{eBayes} -> \code{topTable} for one
coefficient or contrast, implemented in Rust via the \code{edge-rs} crate and
gated against limma 3.66.0.
}
\references{
Law, et al., Genome Biol, 2014; Smyth, Stat Appl Genet Mol Biol,
2004
}
