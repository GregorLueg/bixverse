% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rs_lisi}
\alias{rs_lisi}
\title{Calculate LISI scores on any label}
\usage{
rs_lisi(knn_mat, knn_dist, labels, perplexity, verbose)
}
\arguments{
\item{knn_mat}{Integer matrix. The rows represent the cells and the
columns the neighbour indices.}

\item{knn_dist}{Numeric matrix or NULL. The kNN distances, same shape as
\code{knn_mat}. If provided, neighbours are weighted with a perplexity-calibrated
Gaussian kernel as in Korsunsky et al.; if NULL, neighbours are weighted
uniformly.}

\item{labels}{Integer vector. The label (batch or cell type) per cell.}

\item{perplexity}{Numeric or NULL. Perplexity for the weighted version.
NULL defaults to 30. Ignored if \code{knn_dist} is NULL.}

\item{verbose}{Boolean. Controls verbosity of the function.}
}
\value{
A list with the following items
\itemize{
\item per_cell - Per-cell LISI scores
\item mean_lisi - Mean LISI
\item median_lisi - Median LISI
\item n_labels - Number of distinct labels
\item ilisi_norm - Median LISI rescaled as iLISI, \code{(median - 1) / (n - 1)}
\item clisi_norm - Median LISI rescaled as cLISI, \code{(n - median) / (n - 1)}
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
Computes the Local Inverse Simpson's Index on the kNN graph: the effective
number of labels in each cell's neighbourhood. On batch labels this is
iLISI (higher is better mixing), on cell type labels cLISI (lower is better
separation). Both come back rescaled to \verb{[0, 1]}, higher is better, as in
scIB.
}
\references{
Korsunsky, et al., Nat Methods, 2019; Luecken, et al., Nat
Methods, 2022
}
