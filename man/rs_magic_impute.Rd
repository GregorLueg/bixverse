% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rs_magic_impute}
\alias{rs_magic_impute}
\title{Impute a subset of genes with MAGIC}
\usage{
rs_magic_impute(
  f_path,
  knn_data,
  cell_indices,
  total_cells,
  gene_indices,
  magic_params,
  verbose
)
}
\arguments{
\item{f_path}{String. Path to the \code{counts_genes.bin} file.}

\item{knn_data}{List. The \code{SingleCellNearestNeighbour} data with \code{indices}
(0-indexed!), \code{dist}, \code{k} and \code{dist_metric}. The indices are positions
within \code{cell_indices}, not global cell ids.}

\item{cell_indices}{Integer vector. The global cell indices (0-indexed!)
the kNN graph was built over, in kNN row order.}

\item{total_cells}{Integer. The cell count of the binary store, not of the
selection.}

\item{gene_indices}{Integer vector. Gene indices (0-indexed!) to impute.}

\item{magic_params}{List. Parameter list, see
\code{\link[=params_sc_magic]{params_sc_magic()}}.}

\item{verbose}{Integer. \code{0L} - quiet; \code{1L} - normal verbosity; \code{2L} -
detailed verbosity.}
}
\value{
Numerical matrix of cells x genes with the imputed counts. Rows
follow \code{cell_indices}, columns follow \code{gene_indices}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
Implementation of MAGIC in Rust. A row-stochastic diffusion operator is
built over the provided kNN graph and applied \code{n_steps} times to the counts
of the requested genes. The operator matches the one Palantir builds, i.e.
an adaptive bandwidth taken from the \code{k/3}-th neighbour distance, which
differs from the graphtools kernel the reference implementation uses.

Deliberately restricted to a gene subset. The output is dense, and
smoothing over overlapping neighbourhoods inflates gene-gene correlation,
so imputed counts must not be fed into correlation-based methods such as
Hotspot, SCENIC, differential correlation or CoReMo.
}
\references{
van Dijk, et al., Cell, 2018.
}
\keyword{internal}
