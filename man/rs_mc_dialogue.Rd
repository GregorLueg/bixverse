% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rs_mc_dialogue}
\alias{rs_mc_dialogue}
\title{Run DIALOGUE over meta cells}
\usage{
rs_mc_dialogue(
  sparse_data,
  cell_type_indices,
  features,
  sample_ids,
  cell_quality,
  gene_indices,
  dialogue_params,
  verbose
)
}
\arguments{
\item{sparse_data}{A named list that needs to have \code{data}, \code{indptr},
\code{indices}, \code{cs_type}, \code{nrow} and \code{ncol}, holding the \emph{normalised} meta cell
counts with shape (metacells, genes).}

\item{cell_type_indices}{List of integer vectors. 0-indexed(!) positions of
the meta cells belonging to each cell type. At least two cell types are
needed.}

\item{features}{List of numeric matrices, one per cell type, shaped
\verb{n_metacells_in_type x k_i} with rows aligned to \code{cell_type_indices}. Needs
at least two columns per cell type.}

\item{sample_ids}{Integer vector. 0-indexed(!) sample code per meta cell,
over all meta cells rather than per cell type.}

\item{cell_quality}{Numeric vector. Quality covariate per meta cell, indexed
the same way as \code{sample_ids}.}

\item{gene_indices}{Integer vector. 0-indexed(!) positions of the genes to
consider when building signatures.}

\item{dialogue_params}{Named list. Contains the DIALOGUE parameters across
all three stages, see \code{\link[=params_dialogue_pmd]{params_dialogue_pmd()}}, \code{\link[=params_dialogue_hlm]{params_dialogue_hlm()}} and
\code{\link[=params_dialogue_refine]{params_dialogue_refine()}}. The three blocks share one flat list.}

\item{verbose}{Integer. \code{0L} - quiet; \code{1L} - normal verbosity; \code{2L} -
detailed verbosity.}
}
\value{
A list, identical in shape to \code{\link[=rs_dialogue_sc]{rs_dialogue_sc()}}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
The meta cell entry point into DIALOGUE, see \code{\link[=rs_dialogue_sc]{rs_dialogue_sc()}} for what the
method does. This is a shim rather than a second implementation: everything
DIALOGUE asks of the expression matrix is per-gene, so the in-memory matrix
is wrapped as a gene-major reader and the same core runs.

Only the normalised layer is ever read, so \code{sparse_data} has to carry the
normalised counts. The \code{data} layer is cast to integers on the way in and
then goes unused.

Meta cells are already aggregates, so the sample a meta cell belongs to has
to be unambiguous: build them within samples, not across them. The random
intercept in stage two is over samples, and a meta cell straddling two of
them has no well-defined level.
}
\references{
Jerby-Arnon & Regev, Nature Biotechnology, 2022
}
\keyword{internal}
