% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rs_mc_hotspot_gene_cor}
\alias{rs_mc_hotspot_gene_cor}
\title{Calculate gene to gene spatial correlations (for meta cells)}
\usage{
rs_mc_hotspot_gene_cor(
  sparse_data,
  embd,
  knn_data,
  hotspot_params,
  cells_to_keep,
  genes_to_use,
  verbose,
  seed
)
}
\arguments{
\item{sparse_data}{A named list that needs to have \code{data}, \code{indptr},
\code{indices}, \code{nrow}, \code{ncol} and \code{format}. Shape is (metacells, genes) and the
data are the raw counts.}

\item{embd}{Numerical matrix. The embedding matrix from which to generate
the kNN graph.}

\item{knn_data}{Optional list. This contains pre-computed kNN data
(including distances) and the \code{dist_metric} it was built with. The user has
to ensure consistency! If provided, this will be used and whether the
distances are treated as squared is derived from \code{dist_metric} rather than
from the parameter list.}

\item{hotspot_params}{List. The HotSpot parameter list. The kNN parameters
are only read when no \code{knn_data} is provided; \code{normalise} is unused on this
path.}

\item{cells_to_keep}{Integer vector. 0-index vector indicating which meta
cells to include in the analysis. Ensure that this is of same order/length
as the embedding matrix.}

\item{genes_to_use}{Integer vector. 0-index vector indicating which genes
to include.}

\item{verbose}{Integer. \code{0L} - quiet; \code{1L} - normal verbosity; \code{2L} -
detailed verbosity.}

\item{seed}{Integer. Random seed for reproducibility.}
}
\value{
A list with the following elements.
\itemize{
\item cor - The gene x gene local correlation matrix.
\item z - The Z-scores of these local correlations.
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
This function implements the HotSpot gene <> gene local correlation
functionality from HotSpot, see DeTomaso, et al. This version works on
MetaCell counts which are stored in memory directly.

Three dense metacells x genes blocks are live at once, so keep
\code{genes_to_use} to the panel actually of interest rather than the whole
transcriptome.
}
\references{
DeTomaso, et al., Cell Systems, 2021
}
\keyword{internal}
