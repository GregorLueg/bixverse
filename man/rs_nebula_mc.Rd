% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rs_nebula_mc}
\alias{rs_nebula_mc}
\title{Fit the NEBULA negative binomial gamma mixed model over meta cells}
\usage{
rs_nebula_mc(
  sparse_data,
  metacells_to_keep,
  gene_indices,
  subject_ids,
  design,
  offset,
  nebula_params,
  verbose
)
}
\arguments{
\item{sparse_data}{A named list that needs to have \code{data}, \code{indptr},
\code{indices}, \code{nrow}, \code{ncol} and \code{cs_type}. Shape (metacells, genes), holding
the raw counts.}

\item{metacells_to_keep}{Integer vector. 0-indexed(!) positions of the
meta cells to analyse, in any order. Must not hold duplicates.}

\item{gene_indices}{Integer vector. 0-indexed(!) positions of the genes to
fit.}

\item{subject_ids}{Integer vector. 0-indexed(!) subject label per meta
cell. One entry per row of \code{sparse_data}, not per element of
\code{metacells_to_keep}.}

\item{design}{Numeric matrix. Predictors of meta cells x coefficients, rows
aligned to \code{metacells_to_keep} and including an intercept.}

\item{offset}{Optional numeric vector. Strictly positive scaling factor per
selected meta cell. \code{NULL} uses the aggregated library sizes.}

\item{nebula_params}{Named list. The NEBULA parameters, see
\code{\link[=params_nebula]{params_nebula()}}, plus either \code{coef} (a 0-indexed(!) coefficient)
or \code{contrast} (one weight per coefficient).}

\item{verbose}{Integer. \code{0L} - quiet; \code{1L} - normal verbosity; \code{2L} -
detailed verbosity.}
}
\value{
A list with the same elements \code{\link[=rs_nebula_sc]{rs_nebula_sc()}} returns.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
The arithmetic is the single cell one verbatim, only the counts come from
memory rather than the streamed store. What changes is the interpretation:
the cell-level overdispersion becomes the spread between aggregates within
a subject, not between cells, so it is smaller and absorbs whatever the
aggregation smoothed away. The subject-level term keeps its meaning. Do not
compare the two against a single cell run.
}
\references{
He, et al., Commun Biol, 2021
}
\keyword{internal}
