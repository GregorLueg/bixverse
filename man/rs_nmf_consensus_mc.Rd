% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rs_nmf_consensus_mc}
\alias{rs_nmf_consensus_mc}
\title{Run consensus NMF on MetaCells}
\usage{
rs_nmf_consensus_mc(
  sparse_data,
  k,
  preprocessing,
  use_second_layer,
  nmf_hals_params,
  nmf_consensus_params,
  n_runs,
  seed,
  verbose
)
}
\arguments{
\item{sparse_data}{A named list with \code{data}, \code{indptr}, \code{indices}, \code{nrow},
\code{ncol} and \code{format}.}

\item{k}{Integer. Number of latent factors. Must be at least 2.}

\item{preprocessing}{String. One of \code{c("none", "sd", "sqrt_sd")}.}

\item{use_second_layer}{Boolean. If \code{TRUE}, runs NMF on normalised counts.}

\item{nmf_hals_params}{Named list. Contains the NMF parameters. The
\code{nmf_init} field is ignored, restarts always use random initialisation.}

\item{nmf_consensus_params}{Named list. Contains the consensus parameters.}

\item{n_runs}{Integer. Number of restarts. Must be at least 2.}

\item{seed}{Integer. Base random seed. Restart \code{i} uses \code{seed + i}.}

\item{verbose}{Integer. \code{0L} - quiet; \code{1L} - normal verbosity; \code{2L} -
detailed verbosity.}
}
\value{
A list with the following items
\itemize{
\item w - The left factor matrix (n_meta_cells x k)
\item h - The right factor matrix (k x n_genes)
\item rel_error - Reconstruction error relative to the squared Frobenius
norm of the input. Not comparable with the absolute \code{final_loss} the
single-run version returns.
\item rel_run_errors - The same, per restart.
\item labels - Integer vector of length \code{k * n_runs}. Cluster each pooled
component landed in, \code{NA} if it was dropped.
\item local_density - Mean cosine distance to the nearest neighbours per
pooled component.
\item kept - 1-indexed positions of the surviving pooled components.
\item silhouette - Silhouette per survivor, aligned with \code{kept}.
\item stability - Mean silhouette over the survivors.
\item cluster_sizes - Number of survivors per cluster.
\item n_dropped - Number of pooled components removed.
\item n_empty_clusters - Number of clusters left with no members.
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
Runs \code{n_runs} HALS NMF restarts, pools their components, drops unstable
ones by local density, k-means clusters the survivors and refits the
partner factor against the per-cluster median. Assumes that the sparse data
is pre-filtered for the cells/genes you wish to include. Indices in the
sparse data need to be 0-indexed.
}
\references{
Kotliar et al., eLife, 2019
}
\keyword{internal}
