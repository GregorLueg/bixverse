% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rs_nmf_multi_sc}
\alias{rs_nmf_multi_sc}
\title{Run multiple NMF (HALS) restarts over a set of single cells and genes}
\usage{
rs_nmf_multi_sc(
  f_path_gene,
  gene_indices,
  cell_indices,
  k,
  preprocessing,
  use_second_layer,
  nmf_hals_params,
  n_runs,
  seed,
  verbose
)
}
\arguments{
\item{f_path_gene}{Path to the \code{counts_genes.bin} file.}

\item{gene_indices}{Integer vector. 0-indexed(!) positions of the genes
to include.}

\item{cell_indices}{Integer vector. 0-indexed(!) positions of cells to
include in the analysis.}

\item{k}{Integer. Number of latent factors per run.}

\item{preprocessing}{String. One of \code{c("none", "sd", "sqrt_sd")}.}

\item{use_second_layer}{Boolean. If \code{TRUE}, runs NMF on the normalised
counts; if \code{FALSE}, on the raw counts.}

\item{nmf_hals_params}{Named list. Contains the NMF parameters.}

\item{n_runs}{Integer. Number of random restarts.}

\item{seed}{Integer. Base random seed. Run \code{i} uses \code{seed + i}.}

\item{verbose}{Integer. \code{0L} - quiet; \code{1L} - normal verbosity; \code{2L} -
detailed verbosity.}
}
\value{
A list with the following items
\itemize{
\item w_all - Column-bound W matrices across all runs,
shape \verb{n_features x (k * n_runs)}. Columns \verb{i*k+1..(i+1)*k} are run \code{i}'s
components (1-indexed).
\item h_per_run - List of H matrices, each \verb{k x n_cells}.
\item losses - Numeric vector. Final reconstruction loss per run.
\item converged - Logical vector. Convergence flag per run.
\item best_idx - Integer. 1-indexed position of the run with the lowest
final loss.
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
Runs \code{n_runs} HALS NMF with random initialisations seeded by \code{seed + i}.
The \code{nmf_init} field in \code{nmf_hals_params} is ignored; random init is always
used. The returned \code{w_all} is the column-binding of all run W matrices.
}
\keyword{internal}
