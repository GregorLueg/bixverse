% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rs_sc_residual_variance}
\alias{rs_sc_residual_variance}
\title{Residual variance and the variable features it selects}
\usage{
rs_sc_residual_variance(
  f_path_gene,
  residual_fit,
  cell_indices,
  n_hvg,
  gene_batch_size,
  verbose
)
}
\arguments{
\item{f_path_gene}{String. Path to the \code{counts_genes.bin} file.}

\item{residual_fit}{List. A fit from \code{\link[=rs_sc_fit_residuals]{rs_sc_fit_residuals()}}.}

\item{cell_indices}{Integer vector. The cell indices to use. (0-indexed!)
Must be the selection the fit was fitted on.}

\item{n_hvg}{Integer. Variable features to take from each group.}

\item{gene_batch_size}{Integer or \code{NULL}. Genes held in memory per batch.}

\item{verbose}{Integer. \code{0L} - quiet; \code{1L} - normal verbosity; \code{2L} -
detailed verbosity.}
}
\value{
A list with the following items
\itemize{
\item genes - The gene indices the variances are indexed by. (0-indexed!)
\item variance - Matrix of residual variance, genes by groups.
\item hvg - The selected gene indices, ascending. (0-indexed!)
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
Computes the per-gene residual variance from a fitted model and selects the
variable features from it. With more than one group the selection follows
Seurat: rank within each group, take the top \code{n_hvg} of each, and union
them, so a marker only one sample carries is not buried by a pooled ranking.
The returned set can therefore be larger than \code{n_hvg}.

Each gene's residual row is regenerated, reduced and dropped, so memory is
one row per worker rather than a genes-by-cells matrix.
}
\references{
Seurat v5, \code{SCTransform.StdAssay}
}
\keyword{internal}
