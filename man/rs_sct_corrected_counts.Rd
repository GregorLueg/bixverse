% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rs_sct_corrected_counts}
\alias{rs_sct_corrected_counts}
\title{Writes scTransform-corrected counts to a new store}
\usage{
rs_sct_corrected_counts(
  f_path_gene,
  residual_fit,
  cell_indices,
  f_path_out,
  gene_batch_size,
  verbose
)
}
\arguments{
\item{f_path_gene}{String. Path to the \code{counts_genes.bin} file.}

\item{residual_fit}{List. A scTransform fit from
\code{\link[=rs_sc_fit_residuals]{rs_sc_fit_residuals()}}. The analytic Pearson model has no
corrected-count equivalent.}

\item{cell_indices}{Integer vector. The cell indices to use. (0-indexed!)
Must be the selection the fit was fitted on.}

\item{f_path_out}{String. Path of the gene-major file to write.}

\item{gene_batch_size}{Integer or \code{NULL}. Genes held in memory per batch.}

\item{verbose}{Integer. \code{0L} - quiet; \code{1L} - normal verbosity; \code{2L} -
detailed verbosity.}
}
\value{
A list with the following items
\itemize{
\item f_path - The file that was written.
\item genes - The source gene indices of the written axis. (0-indexed!)
\item n_genes - Number of genes written.
\item n_cells - Number of cells written.
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
Reverses the residual transform with every latent variable, the library size
included, held at its median, so the depth structure is removed while the
per-sample intercept is kept. The result is written as a new gene-major
store.

The output is re-indexed: its gene axis is the model's, so gene \code{j} in the
written store is \code{genes[j + 1]} of the source. It also has no normalised
layer, since corrected counts carry no library size to scale to.
}
\keyword{internal}
