% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rs_singscore_permutation_test}
\alias{rs_singscore_permutation_test}
\title{Rust version of the singscore permutation test}
\usage{
rs_singscore_permutation_test(
  ranks,
  up_set,
  down_set,
  center_score,
  known_direction,
  stable,
  n_permutations,
  seed
)
}
\arguments{
\item{ranks}{Numerical matrix. The ranked expression matrix.}

\item{up_set}{Integer vector. Zero-indexed.}

\item{down_set}{Integer vector or NULL.}

\item{center_score, known_direction, stable}{Booleans. Should match the
values used for the real \code{\link[=rs_singscore_single]{rs_singscore_single()}} call.}

\item{n_permutations}{Integer. Number of random draws (B).}

\item{seed}{Integer. RNG seed.}
}
\value{
A named list with \code{observed_scores} (length n_samples),
\code{null_distribution} (B × n_samples matrix), and \code{p_values}
(length n_samples).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
For \code{n_permutations} iterations, draws random gene indices of the same
total size as the real gene set(s), scores them with the same options as
the real call, and builds a per-sample null distribution. Returns empirical
one-tailed p-values: \code{max(1 / n_permutations, mean(null > observed))}.
}
