% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rs_sp_build_graph}
\alias{rs_sp_build_graph}
\title{Build the spatial neighbourhood graph for one sample}
\usage{
rs_sp_build_graph(coords, graph_params, knn_params, seed, verbose)
}
\arguments{
\item{coords}{Numeric matrix. Two columns, \code{x} then \code{y}, in full-resolution
pixels. One row per spot.}

\item{graph_params}{List. Layout and weighting. Flat list with:
\itemize{
\item layout - String. One of \code{c("hex", "square", "knn", "radius")}.
\item array_row, array_col - Integer. Lattice coordinates, needed by
\code{"hex"} and \code{"square"}.
\item connectivity - Integer. \code{4L} or \code{8L}, needed by \code{"square"}.
\item k - Integer. Neighbours per spot, needed by \code{"knn"}.
\item radius - Float. Cut-off in full-res pixels, needed by \code{"radius"}.
\item weighting - String. One of
\code{c("binary", "inverse_distance", "gaussian")}.
\item power - Float. Decay exponent for \code{"inverse_distance"}.
\item bandwidth - Float or \code{NULL}. Gaussian bandwidth; \code{NULL} resolves to
the median nearest-neighbour distance.
\item row_normalise - Boolean. spdep \code{style = "W"}.
}}

\item{knn_params}{List or \code{NULL}. ANN settings for the \code{"knn"} layout, see
\code{\link[=params_sc_knn]{params_sc_knn()}}. Ignored by every other layout. \code{k} and the
distance metric are overridden from the layout and the 2D geometry.}

\item{seed}{Integer. Seed for the ANN index, for reproducibility.}

\item{verbose}{Integer. \code{0L} - quiet; \code{1L} - normal verbosity; \code{2L} -
detailed verbosity.}
}
\value{
A list with the following elements
\itemize{
\item indices - List of integer vectors. Local 0-based neighbour indices,
sorted ascending, one element per spot.
\item weights - List of numeric vectors. Edge weights, aligned element
for element with \code{indices}.
\item degree - Numeric. Weighted degree per spot, taken over the
symmetrised graph \code{W + t(W)} that every statistic downstream consumes.
A reciprocal edge therefore contributes \code{w_ij + w_ji}, so a binary
layout with mutual neighbours gives twice the neighbour count.
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
Maps per-spot coordinates onto the \verb{(neighbours, weights)} adjacency pair
that every other spatial statistic in this package consumes. Four layouts:
the two Visium lattices are exact table look-ups and never compute a
distance, kNN and fixed-radius cover irregular geometries like Xenium or
MERFISH.

The returned indices are \strong{local}: entry \code{i} describes the spot at row \code{i}
of \code{coords}, and the indices inside it position against that same matrix.
They are not global spot ids.
}
\keyword{internal}
