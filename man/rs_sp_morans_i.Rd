% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rs_sp_morans_i}
\alias{rs_sp_morans_i}
\title{Detect spatially variable genes with Moran's I}
\usage{
rs_sp_morans_i(
  f_path_gene,
  spots_to_keep,
  neighbours,
  weights,
  gene_indices,
  svg_params,
  streaming,
  verbose
)
}
\arguments{
\item{f_path_gene}{String. Path to the \code{counts_genes.bin} file.}

\item{spots_to_keep}{Integer. Global 0-based spot indices making up this
sample.}

\item{neighbours}{List of integer vectors. Local 0-based neighbour indices,
one element per spot. Must be as long as \code{spots_to_keep}.}

\item{weights}{List of numeric vectors. Edge weights, aligned element for
element with \code{neighbours}.}

\item{gene_indices}{Integer. On-disk 0-based gene indices to test.}

\item{svg_params}{List. With element \code{assay}, one of \code{c("raw", "norm")}.}

\item{streaming}{Logical. If \code{TRUE}, gene chunks are loaded in batches of
1000 rather than all at once.}

\item{verbose}{Integer. \code{0L} - quiet; \code{1L} - normal verbosity; \code{2L} -
detailed verbosity.}
}
\value{
A list with the following elements
\itemize{
\item gene_idx - Integer. On-disk gene indices that were tested.
\item i - Numeric. Moran's I per gene. \code{NaN} for constant genes.
\item e_i - Numeric scalar. Expected I under the null.
\item var_i - Numeric scalar. Variance of I under the null.
\item z - Numeric. Standardised statistic per gene.
\item pval - Numeric. Two-sided p-value per gene.
\item fdr - Numeric. BH-adjusted p-value per gene.
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
Per-gene Moran's I against a precomputed spatial graph, tested under the
Cliff-Ord normality null. Expectation and variance depend only on the spot
count and the graph weights, so both are scalars across genes and get
computed once. Per-gene work is a single sparse quadratic form.

Index spaces differ between the two spot arguments and mixing them up is
the classic failure here. \code{spots_to_keep} is \strong{global} (0-based positions
in the count store), while \code{neighbours} and \code{weights} are \strong{local}
(0-based, positional against \code{spots_to_keep}). The output of
\code{\link[=rs_sp_build_graph]{rs_sp_build_graph()}} is already local, so the two compose.
}
\references{
Cliff & Ord, Spatial Autocorrelation, 1973
}
\keyword{internal}
