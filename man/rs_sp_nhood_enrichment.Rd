% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rs_sp_nhood_enrichment}
\alias{rs_sp_nhood_enrichment}
\title{Neighbourhood enrichment between spot labels}
\usage{
rs_sp_nhood_enrichment(
  indices,
  weights,
  labels,
  label_levels,
  nhood_params,
  seed,
  verbose
)
}
\arguments{
\item{indices}{List of integer vectors. Local 0-based neighbour indices,
one element per spot. The output of \code{\link[=rs_sp_build_graph]{rs_sp_build_graph()}}.}

\item{weights}{List of numeric vectors. Edge weights, aligned element for
element with \code{indices}.}

\item{labels}{Integer. Label code per spot, 0-based and local, so
\code{length(labels)} must equal \code{length(indices)}. Codes must be in
\code{0:(length(label_levels) - 1)}.}

\item{label_levels}{Character. Name per code, in encoding order.}

\item{nhood_params}{List. With the following elements:
\itemize{
\item n_perm - Integer. Number of permutations.
\item symmetrise - Boolean. Average \code{Z} with its transpose at the end.
}}

\item{seed}{Integer. Seed for the permutations.}

\item{verbose}{Integer. \code{0L} - quiet; \code{1L} - normal verbosity; \code{2L} -
detailed verbosity.}
}
\value{
A list with the following elements
\itemize{
\item label_levels - Character. Levels in encoding order, matching the
row and column order of every matrix below.
\item observed - Numeric matrix, K x K. Observed edge counts.
\item perm_mean - Numeric matrix, K x K. Permutation mean.
\item perm_sd - Numeric matrix, K x K. Permutation standard deviation.
\item z_scores - Numeric matrix, K x K. Z-scores.
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
Asks whether each pair of labels shows up as spatial neighbours more or
less often than chance. The graph is fixed and only the label assignment is
permuted, so the null keeps the spatial structure and tests the labelling
against it.

Each undirected edge contributes once to a diagonal entry and to both
off-diagonal entries, which keeps the count matrix symmetric throughout.
}
\keyword{internal}
