% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rs_sp_read_h5ad_spatial}
\alias{rs_sp_read_h5ad_spatial}
\title{Read the spatial extras out of an h5ad file}
\usage{
rs_sp_read_h5ad_spatial(h5_path, library_id, orientation)
}
\arguments{
\item{h5_path}{String. Path to the \code{.h5ad} file.}

\item{library_id}{String or \code{NULL}. Which \code{uns/spatial} library to read.
\code{NULL} takes the only one and errors when there is a choice to make.}

\item{orientation}{String. \code{"xy"} or \code{"yx"}. The column order to fall back
on when nothing in the file settles it. \code{"xy"} is what
\code{scanpy.read_visium} produces.}
}
\value{
A list with the following elements
\itemize{
\item coords - Numeric matrix, spots x 2. Columns \code{x} then \code{y}, in
full-resolution pixels, already in the contract order.
\item orientation - String. The column order that was used.
\item evidence - String. What settled it, see above.
\item obsm_keys - Character. Every key under \code{obsm}.
\item library_id - String. The \code{uns/spatial} library read, or \code{NA}.
\item library_ids - Character. Every library the file holds.
\item scale_factor_names, scale_factor_values - Character and numeric.
The scale factors, passed through untouched.
\item image_keys - Character. Images present under the library.
\item image_heights, image_widths - Integer. Their pixel dimensions,
aligned with \code{image_keys}.
\item metadata_keys - Character. Keys under the library's \code{metadata}.
\item has_array_indices - Boolean. Whether \code{obs} carries \code{array_row} and
\code{array_col}, which the hex and square graph layouts need.
\item has_pixel_columns - Boolean. Whether \code{obs} carries the
\verb{pxl_*_in_fullres} pair.
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
Pulls \code{obsm/spatial} and whatever survived in \code{uns/spatial}, and resolves
which column of the coordinate array is \code{x}.

\strong{The column order is not fixed by the AnnData spec.} It gets worked out
from the file rather than assumed, strongest evidence first:

\itemize{
\item \code{"image_tissue"} - one order puts the spots on the tissue in the
shipped image and the other does not. The only test that measures the
thing that has a consequence.
\item \code{"image_frame"} - one order keeps every spot inside the frame the
image implies, the other pushes spots off the edge.
\item \code{"obs_pixel_columns"} - \code{obs} carries \code{pxl_row_in_fullres} and
\code{pxl_col_in_fullres} and one matched a column exactly. Weaker than it
looks: \code{scanpy.read_visium} swaps those two names relative to Space
Ranger, so the labels mean the opposite in a file that went through it.
\item \code{"assumed"} - nothing in the file settled it, so \code{orientation} was
taken as given.
}

Getting it wrong costs nothing statistically. A swap of \code{x} and \code{y} is a
reflection, and every distance survives it, so the graph, Moran's I and
SPARK-X all come out identical. It costs everything for image features,
which would then be cut from the transpose of each spot.
}
\keyword{internal}
