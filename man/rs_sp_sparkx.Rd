% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rs_sp_sparkx}
\alias{rs_sp_sparkx}
\title{Detect spatially variable genes with SPARK-X}
\usage{
rs_sp_sparkx(
  f_path_gene,
  spots_to_keep,
  coords,
  gene_indices,
  sparkx_params,
  seed,
  streaming,
  verbose
)
}
\arguments{
\item{f_path_gene}{String. Path to the \code{counts_genes.bin} file.}

\item{spots_to_keep}{Integer. Global 0-based spot indices making up this
sample.}

\item{coords}{Numeric matrix. Two columns, \code{x} then \code{y}, in full-resolution
pixels. One row per spot, aligned with \code{spots_to_keep}.}

\item{gene_indices}{Integer. On-disk 0-based gene indices to test.}

\item{sparkx_params}{List. With the following elements:
\itemize{
\item kernels - List or \code{NULL}. Each element a list with \code{kernel}
(\code{"gaussian"} or \code{"cosine"}) and \code{bandwidth}. \code{NULL} or empty uses the
defaults derived from the coordinates.
\item n_landmarks - Integer. Nystroem landmarks per Gaussian kernel.
\item bandwidth_subsample - Integer. Sub-sample size for the default
bandwidth estimation.
}}

\item{seed}{Integer. Seed for landmark selection and bandwidth
sub-sampling.}

\item{streaming}{Logical. If \code{TRUE}, gene chunks are loaded in batches of
1000 rather than all at once.}

\item{verbose}{Integer. \code{0L} - quiet; \code{1L} - normal verbosity; \code{2L} -
detailed verbosity.}
}
\value{
A list with the following elements
\itemize{
\item gene_idx - Integer. On-disk gene indices that were tested.
\item kernels - Character. Kernel labels, one per column of the two
matrices below.
\item stat_per_kernel - Numeric matrix, genes x kernels.
\item pval_per_kernel - Numeric matrix, genes x kernels.
\item pval_combined - Numeric. Cauchy-combined p-value per gene.
\item fdr - Numeric. BH-adjusted \code{pval_combined}.
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
Kernel-based non-parametric test for spatial expression patterns. Each
kernel gets a low-rank factorisation of the centred kernel matrix, the
per-gene statistic comes out of a projection against that factor, and the
per-kernel p-values are combined with the Cauchy test.

An empty kernel bank triggers the paper-default five Gaussian plus five
cosine bandwidths, derived from quantiles of the pairwise coordinate
distance distribution on a sub-sample.
}
\references{
Zhu, Sun & Zhou, Genome Biology, 2021
}
\keyword{internal}
