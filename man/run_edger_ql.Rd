% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions_dge.R
\name{run_edger_ql}
\alias{run_edger_ql}
\title{Run the edgeR quasi-likelihood workflow}
\usage{
run_edger_ql(
  counts,
  design,
  coef = NULL,
  contrast = NULL,
  edger_params = params_edger_ql()
)
}
\arguments{
\item{counts}{Numeric matrix. Raw counts of features x samples, with
rownames. Must not be normalised or log-transformed.}

\item{design}{Numeric matrix. The design matrix of samples x coefficients,
including the intercept. Usually the output of \code{\link[stats:model.matrix]{stats::model.matrix()}}.
Needs at least two columns, since the null model has to retain one.}

\item{coef}{Optional integer or character. Which coefficient(s) of \code{design}
to drop from the null model, given as 1-based column positions or column
names. Defaults to the last column, as edgeR does.}

\item{contrast}{Optional numeric vector or matrix. Weights over the
coefficients, one entry (or row) per column of \code{design}. Mutually exclusive
with \code{coef}.}

\item{edger_params}{A list, see \code{\link[=params_edger_ql]{params_edger_ql()}}. The list has
the following parameters:
\itemize{
\item norm_method - String. Library size normalisation. One of
\code{c("TMM", "TMMwsp", "RLE", "upperquartile", "none")}.
\item filter - Boolean. Run \code{filterByExpr()} before fitting.
\item min_mean - Numeric. Minimum mean count across samples.
\item robust - Boolean. Robust empirical Bayes squeezing.
\item legacy - Boolean. Take edgeR's pre-4.0 pipeline.
}}
}
\value{
A data.table with one row per feature that survived the filters:
\itemize{
\item feature_id - The rowname of \code{counts}.
\item log_fc - Log2 fold change of the tested coefficient or contrast.
\item log_cpm - Average log2 counts per million.
\item f_stat - The quasi-likelihood F statistic.
\item p_value - Raw p-value.
\item fdr - Benjamini-Hochberg adjusted p-value.
}
}
\description{
Runs \code{filterByExpr()} -> \code{calcNormFactors()} -> \code{glmQLFit()} ->
\code{glmQLFTest()} in Rust via the \code{edge-rs} crate, gated against edgeR 4.8.2.

The tested axis does not have to be genes. Anything with a counts matrix of
features x samples goes through here, which is why the Milo neighbourhood
test calls the same function with \code{filter = FALSE}.

By default this skips \code{estimateDisp()} and lets the fit estimate its own
dispersion from the most abundant features. That is edgeR 4's own
recommendation and where most of the runtime went. Set \code{legacy = TRUE} in
\code{\link[=params_edger_ql]{params_edger_ql()}} for the pre-4.0 pipeline, which does run
\code{estimateDisp()}.
}
\references{
Chen, Lun and Smyth, F1000Research, 2016
}
