% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_sc_trajectory.R
\name{run_gene_trends_sc}
\alias{run_gene_trends_sc}
\title{Fit gene trends over Palantir pseudotime}
\usage{
run_gene_trends_sc(
  object,
  palantir_res,
  features = NULL,
  use_magic = FALSE,
  branch_params = params_sc_branch_selection(),
  gene_trend_params = params_sc_gene_trends(),
  .verbose = TRUE
)
}
\arguments{
\item{object}{One of \code{SingleCells}, \code{SingleCellsSubset}, \code{MetaCells} or
\code{SingleCellsMultiModal}.}

\item{palantir_res}{\code{PalantirRes} class. The output of
\code{\link[=run_palantir_sc]{run_palantir_sc()}}, run over the same cells.}

\item{features}{Optional character vector. The genes to fit. If \code{NULL},
every gene in the imputed layer is used, which needs \code{use_magic = TRUE}.}

\item{use_magic}{Boolean. Fit the MAGIC imputed layer rather than the
normalised counts. Needs \code{\link[=run_magic_sc]{run_magic_sc()}} to have been run.}

\item{branch_params}{List. See \code{\link[=params_sc_branch_selection]{params_sc_branch_selection()}}.}

\item{gene_trend_params}{List. See \code{\link[=params_sc_gene_trends]{params_sc_gene_trends()}}.}

\item{.verbose}{Boolean or integer. Controls verbosity and returns run times.
\code{FALSE} -> quiet, \code{TRUE} or \code{1L} -> normal verbosity, \code{2L} -> detailed
verbosity.}
}
\value{
A \code{GeneTrendsRes} S3 object with:
\itemize{
\item trends - Long data.table with \code{branch}, \code{pseudotime}, \code{gene} and
\code{expression}, ready to facet.
\item branch_cells - Named list of the cell names selected for each
branch.
\item branches - Character vector with the branch names, taken from the
column names of the fate probability matrix.
\item params - List with the parameters the run used.
\item run_info - List with \code{n_cells} and \code{jitter_used} per branch and the
grid \code{resolution}.
}
}
\description{
Assigns cells to branches from their fate probabilities, then fits a
landmark Gaussian process with a Matern-5/2 kernel per branch, which is the
Mellon-based estimator of the reference rather than the legacy GAM. Out
comes one smooth curve per gene and branch, on a common pseudotime grid.

Whatever expression is handed over is what gets fitted. With
\code{use_magic = FALSE} that is the normalised counts; with \code{TRUE} it is the
imputed layer \code{\link[=run_magic_sc]{run_magic_sc()}} wrote. Imputing first smooths
twice, which is a defensible presentation choice and not usually a necessary
one.
}
\section{On the defaults}{

Palantir's pseudotime is min-max scaled to \verb{[0, 1]}, so the reference's
\code{length_scale} of \code{1.0} spans the entire domain and its \code{sigma} of \code{1.0}
sits at roughly the signal scale of log-normalised expression. The posterior
is prior-dominated: it flattens genuine transient structure and resolves
almost any gene into a smooth monotone or single-peaked curve. Shorten
\code{length_scale} in \code{\link[=params_sc_gene_trends]{params_sc_gene_trends()}} before believing a
bump.
}

\references{
Setty, et al., Nat. Biotechnol., 2019.
}
