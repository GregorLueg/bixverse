% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_sc_trajectory.R
\name{run_paga_sc}
\alias{run_paga_sc}
\title{Run PAGA graph abstraction}
\usage{
run_paga_sc(
  object,
  cluster_col,
  modality = c("rna", "adt", "wnn"),
  .verbose = TRUE
)
}
\arguments{
\item{object}{One of \code{SingleCells}, \code{SingleCellsSubset}, \code{MetaCells} or
\code{SingleCellsMultiModal}.}

\item{cluster_col}{String. The obs column holding the cluster assignments.
Empty factor levels are retained.}

\item{modality}{String. One of \code{c("rna", "adt", "wnn")}. Which kNN graph to
run over. Anything but \code{"rna"} requires a \code{SingleCellsMultiModal} object.}

\item{.verbose}{Boolean or integer. Controls verbosity and returns run times.
\code{FALSE} -> quiet, \code{TRUE} or \code{1L} -> normal verbosity, \code{2L} -> detailed
verbosity.}
}
\value{
A \code{PagaRes} S3 object with:
\itemize{
\item connectivities - Symmetric sparse matrix of clusters x clusters with
a zero diagonal. Values lie in \verb{(0, 1]}.
\item connectivities_tree - Maximum spanning forest of \code{connectivities},
carrying the original connectivity values on the retained edges.
\item sizes - data.table with \code{cluster} and \code{n_cells}.
\item params - List with \code{cluster_col} and \code{modality}.
}
}
\description{
PAGA abstracts the single cell kNN graph into a cluster-level graph, where
the edge weight between two clusters measures how much more connected they
are than expected under a random null model. It also returns the maximum
spanning forest of that graph, which is the backbone typically plotted. For
details, please refer to Wolf, et al.

Only the kNN indices are used, no distances and no expression data, so the
method is safe on any of the available graphs, including the WNN one. Run
\code{\link[=find_neighbours_sc]{find_neighbours_sc()}} and a clustering first.
}
\references{
Wolf, et al., Genome Biol., 2019.
}
