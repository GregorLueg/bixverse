% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_sc_trajectory.R
\name{run_palantir_sc}
\alias{run_palantir_sc}
\title{Run Palantir trajectory inference}
\usage{
run_palantir_sc(
  object,
  early_cell,
  terminal_states = NULL,
  modality = c("rna", "adt", "wnn"),
  palantir_params = params_sc_palantir(),
  seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{object}{One of \code{SingleCells}, \code{SingleCellsSubset}, \code{MetaCells} or
\code{SingleCellsMultiModal}.}

\item{early_cell}{String. Name of the cell to start the trajectory from. Must
be one of the cells the kNN graph was built over.}

\item{terminal_states}{Optional character vector. Names of the terminal state
cells. If \code{NULL}, they are detected from the waypoint Markov chain.}

\item{modality}{String. One of \code{c("rna", "adt", "wnn")}. Which kNN graph to
run over. Anything but \code{"rna"} requires a \code{SingleCellsMultiModal} object.}

\item{palantir_params}{List. See \code{\link[=params_sc_palantir]{params_sc_palantir()}}.}

\item{seed}{Integer. For reproducibility.}

\item{.verbose}{Boolean or integer. Controls verbosity and returns run times.
\code{FALSE} -> quiet, \code{TRUE} or \code{1L} -> normal verbosity, \code{2L} -> detailed
verbosity.}
}
\value{
A \code{PalantirRes} S3 object with:
\itemize{
\item pseudotime - data.table with \code{cell_id}, \code{pseudotime} (min-max scaled
to \verb{[0, 1]}; the start cell is not pinned to 0, and a start cell far from 0
means the refinement disagreed with the anchor) and \code{entropy}.
\item branch_probs - Numeric matrix of cells x terminal states with the
fate probabilities. Rows need not sum to one, as sub-threshold values are
zeroed without renormalisation.
\item terminal_states - Character vector with the terminal state cell
names. Sets the column order of \code{branch_probs}.
\item waypoints - Character vector with the waypoint cell names. The first
element is the start cell.
\item start_cell - String. The start cell that was actually used.
\item multiscale - Numeric matrix of cells x components with the multiscale
diffusion components.
\item run_info - List with \code{iterations}, \code{converged}, \code{eigen_converged},
\code{eigen_residual}, \code{repair_edges}, \code{stranded_waypoints}, \code{n_waypoints} and
\code{modality}. A \code{eigen_converged} of \code{FALSE} means the diffusion eigensolve
ran out of restarts and the embedding is under-resolved.
}
}
\description{
Palantir models differentiation as a Markov chain over a diffusion map of the
cells. It returns a pseudotime per cell, the probability of each cell
reaching each terminal state, and the differentiation entropy derived from
those probabilities. For details, please refer to Setty, et al.

The kNN graph stored on the object feeds the diffusion kernel, so run
\code{\link[=find_neighbours_sc]{find_neighbours_sc()}} first. Whether the stored distances are
treated as squared is derived from the distance metric of that graph. The
geodesics themselves are measured over a second kNN graph that Palantir
builds internally on the multiscale space, controlled by the \code{knn} element of
\code{\link[=params_sc_palantir]{params_sc_palantir()}}.

For \code{SingleCellsMultiModal} the \code{"wnn"} graph works, but its distances are
kernel-derived and not a metric, so the diffusion map on top is a heuristic
on a heuristic. You will get a warning saying as much.
}
\references{
Setty, et al., Nat. Biotechnol., 2019.
}
