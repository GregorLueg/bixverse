% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_sc_pathways.R
\name{scenic_grn_sc}
\alias{scenic_grn_sc}
\title{Run SCENIC GRN inference}
\usage{
scenic_grn_sc(
  object,
  tf_ids,
  scenic_params = params_scenic(),
  genes_to_take = NULL,
  cells_to_take = NULL,
  streaming = FALSE,
  random_seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{object}{\code{SingleCells} class.}

\item{tf_ids}{Character vector. Transcription factor gene identifiers to
use as predictors. Must be a subset of gene identifiers present in the
object.}

\item{scenic_params}{List. SCENIC parameters, see
\code{\link[=params_scenic]{params_scenic()}}.}

\item{genes_to_take}{Optional character vector. Target gene identifiers.
If \code{NULL}, genes are selected automatically via
\code{\link[=scenic_gene_filter_sc]{scenic_gene_filter_sc()}} using the \code{min_counts} and \code{min_cells}
thresholds in \code{scenic_params}.}

\item{cells_to_take}{Optional string vector. Cell identifiers to restrict
to. If \code{NULL}, defaults to all filtered cells in the class.}

\item{streaming}{Boolean. Whether to use the streaming implementation to
bound memory usage. Useful for large datasets. Defaults to \code{FALSE}.}

\item{random_seed}{Integer. Used for reproducibility. Defaults to \code{42L}.}

\item{.verbose}{Boolean. Controls verbosity. Defaults to \code{TRUE}.}
}
\value{
A \code{ScenicGrn} object.
}
\description{
Runs SCENIC GRN inference on the provided genes using the specified
transcription factors as predictors. Returns a \code{ScenicGrn} object
containing the TF-gene importance matrix for further processing.
}
\details{
TF identifiers that are not found in the object's gene list are silently
dropped with a warning indicating how many were removed. TF indices are
intersected with the target gene indices so that TFs not passing the gene
filter are excluded from the predictor set but remain as potential targets
if present in \code{genes_to_take}.
}
