% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/base_generics_sc.R
\name{sct_corrected_counts_sc}
\alias{sct_corrected_counts_sc}
\title{Write scTransform-corrected counts to a new store}
\usage{
sct_corrected_counts_sc(
  object,
  dir_out = NULL,
  build_cell_store = TRUE,
  overwrite = FALSE,
  gene_batch_size = NULL,
  .verbose = TRUE
)
}
\arguments{
\item{object}{\code{SingleCells} or \code{SingleCellsSubset} class with a scTransform
fit attached. The analytic Pearson model has no corrected-count equivalent.}

\item{dir_out}{String or \code{NULL}. Directory to write to. \code{NULL} uses
\code{sct_corrected} inside the object's own data directory.}

\item{build_cell_store}{Boolean. Also write the \code{counts_cells.bin} companion
and the database, giving back a \code{SingleCells} rather than a path. Costs a
second pass over the data.}

\item{overwrite}{Boolean. Overwrite an existing store in \code{dir_out}.}

\item{gene_batch_size}{Integer or \code{NULL}. Genes held in memory per batch.}

\item{.verbose}{Boolean or Integer. Controls verbosity.}
}
\value{
With \code{build_cell_store = TRUE} a new \code{SingleCells} over the
corrected counts, otherwise the path of the gene-major file, invisibly.
}
\description{
Reverses the residual transform with every latent variable, the library size
included, held at its median. The depth structure goes, the per-sample
intercept stays.

The result is a new store on disk, not a layer on \code{object}. Its gene axis is
the model's, so it is narrower than the source and the indices do not line
up, which is why the observation and variable tables are rebuilt rather than
copied.
}
\examples{
# corrected counts as a fresh object
sc <- demo_single_cells(prepped = FALSE)
sc <- fit_residuals_sc(sc, .verbose = FALSE)
corrected <- sct_corrected_counts_sc(sc, .verbose = FALSE)
dim(corrected)

unlink(sc@dir_data, recursive = TRUE, force = TRUE)
}
