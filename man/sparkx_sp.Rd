% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/base_generics_spatial.R
\name{sparkx_sp}
\alias{sparkx_sp}
\title{Detect spatially variable genes with SPARK-X}
\usage{
sparkx_sp(
  object,
  exp_id = NULL,
  genes = NULL,
  sparkx_params = params_sp_sparkx(),
  seed = 42L,
  streaming = TRUE,
  .verbose = TRUE
)
}
\arguments{
\item{object}{A \code{\link[=SpatialSpot]{SpatialSpot()}} or
\code{\link[=SpatialSpotSubset]{SpatialSpotSubset()}}.}

\item{exp_id}{Optional string. Restrict to one experiment. \code{NULL} runs over
every registered sample.}

\item{genes}{Optional character vector or integer vector. Genes to test.
Characters are matched against the var table, integers are taken as
1-indexed positions in it. \code{NULL} tests every gene.}

\item{sparkx_params}{List. Output of \code{\link[=params_sp_sparkx]{params_sp_sparkx()}}.}

\item{seed}{Integer. Seed for landmark selection and bandwidth
sub-sampling.}

\item{streaming}{Boolean. Load the gene chunks in batches of 1000 rather
than all at once.}

\item{.verbose}{Boolean or integer. Controls verbosity.}
}
\value{
The object with the SPARK-X results cached.
}
\description{
Kernel-based non-parametric test for spatial expression patterns. Works
straight off the coordinates and needs no graph. Results land in the
\code{per_sample_sparkx} slot of the \code{SpCache}.
}
\references{
Zhu, Sun & Zhou, Genome Biology, 2021
}
