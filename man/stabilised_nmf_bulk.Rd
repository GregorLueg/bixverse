% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_coexp_nmf.R
\name{stabilised_nmf_bulk}
\alias{stabilised_nmf_bulk}
\title{Run stabilised (multi-restart) NMF on a BulkCoExp}
\usage{
stabilised_nmf_bulk(
  object,
  k,
  n_runs = 30L,
  preprocessing = c("none", "sd", "sqrt_sd"),
  nmf_hals_params = params_nmf_hals(),
  membership_params = params_module_membership(),
  seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{object}{The class, see \code{\link[=BulkCoExp]{BulkCoExp()}}.}

\item{k}{Integer. Number of latent factors (modules) per run.}

\item{n_runs}{Integer. Number of random restarts.}

\item{preprocessing}{String. One of \code{c("none", "sd", "sqrt_sd")}.}

\item{nmf_hals_params}{List. Output of \code{\link[=params_nmf_hals]{params_nmf_hals()}}. Note
that \code{nmf_init} is ignored for the stabilised variant, which always uses
random initialisation seeded by \code{seed + i}.}

\item{membership_params}{List. Controls how the gene loadings are turned into
module membership, see \code{\link[=params_module_membership]{params_module_membership()}}.}

\item{seed}{Integer. Base random seed.}

\item{.verbose}{Boolean or integer \code{0L}/\code{1L}/\code{2L}. Controls verbosity.}
}
\value{
The class with \code{final_results} populated from the best run (lowest
final loss) plus stability diagnostics (\code{losses}, \code{converged}, \code{best_idx},
\code{w_all_runs}, \code{h_per_run}).
}
\description{
Runs \code{n_runs} HALS-NMF fits with random initialisations and returns the run
with the lowest final reconstruction loss as the primary result, along with
the per-run losses and convergence flags. Useful when the objective is
known to have multiple local minima. Uses \code{f64} precision.
}
