% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipelines_sc.R
\name{step_metacells_sc}
\alias{step_metacells_sc}
\title{Pipeline step: generate meta cells}
\usage{
step_metacells_sc(method = c("bootstrapped", "seacells", "supercells"), ...)
}
\arguments{
\item{method}{String. One of \code{c("bootstrapped", "seacells", "supercells")}.}

\item{...}{Arguments passed on to the generator, e.g.
\code{sc_meta_cell_params}, \code{target_size} or \code{.verbose}.}
}
\value{
An \code{ScStep}.
}
\description{
Wraps the meta cell generators as an \code{ScStep}. Unlike the other steps this
one changes the class of the object: it takes a \code{SingleCells} or
\code{SingleCellsSubset} and returns a \code{\link[=MetaCells]{MetaCells()}}. Steps that follow
it need \code{MetaCells} methods, which \code{\link[=validate_pipeline]{validate_pipeline()}} checks up front.

Combined with \code{\link[=apply_pipeline_per_group]{apply_pipeline_per_group()}} this gives you per-group
pre-processing (HVG, PCA, batch correction within a patient, kNN) followed by
source-pure meta cells, which you then hand to \code{\link[=merge_meta_cells]{merge_meta_cells()}}.
}
\examples{
# per group pre-processing that ends on source-pure meta cells
sc <- demo_single_cells(prepped = FALSE)
pipeline <- step_hvg_sc(hvg_no = 30L, .verbose = FALSE) \%>>\%
  step_pca_sc(no_pcs = 10L, .verbose = FALSE) \%>>\%
  step_neighbours_sc(.verbose = FALSE) \%>>\%
  step_metacells_sc(
    "bootstrapped",
    sc_meta_cell_params = params_sc_bt_metacells(target_no_metacells = 10L),
    .verbose = FALSE
  )

per_group <- apply_pipeline_per_group(pipeline, sc, group_col = "cell_grp")
merge_meta_cells(per_group, .verbose = FALSE)

unlink(sc@dir_data, recursive = TRUE, force = TRUE)
}
