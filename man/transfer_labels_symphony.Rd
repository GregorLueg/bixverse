% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_sc_annotations_reference.R
\name{transfer_labels_symphony}
\alias{transfer_labels_symphony}
\title{Transfer labels from a Symphony reference to a query via kNN majority vote}
\usage{
transfer_labels_symphony(
  reference,
  query,
  label_column,
  knn_params = params_sc_knn(),
  seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{reference}{\code{SymphonyReference} (must have \code{z_corr} and stored
labels).}

\item{query}{\code{SingleCells} with a \code{"symphony"} embedding attached.}

\item{label_column}{String. Name of a column in the reference's stored
labels.}

\item{knn_params}{List. Output of \code{\link[=params_sc_knn]{params_sc_knn()}}.}

\item{seed}{Integer.}

\item{.verbose}{Boolean or integer.}
}
\value{
A data.table with columns \verb{predicted_<label_column>} and
\verb{confidence_<label_column>}, in \code{get_cells_to_keep(query)} order.
}
\description{
kNN majority vote on the Harmony-corrected embeddings. The reference's
\code{z_corr} is the searchable index; the query's \code{"symphony"} embedding is
what gets queried. Reference labels come from the reference's stored
\code{labels} slot — populate it via the \code{label_columns} argument to
\code{\link[=build_symphony_ref]{build_symphony_ref()}} or post-hoc via \code{\link[=add_symphony_labels]{add_symphony_labels()}}.

Distances on \code{z_corr} are typically Euclidean — set \code{knn_params$ann_dist}
to \code{"euclidean"} unless you have a reason to use cosine.
}
\examples{
# kNN label transfer from the reference onto a mapped query
ref <- demo_single_cells(
  syn_data_params = params_sc_synthetic_data(
    n_cells = 500L,
    n_genes = 50L,
    n_batches = 2L
  )
)
symphony_ref <- build_symphony_ref(
  ref,
  batch_column = "batch_index",
  hvg = get_hvg(ref) + 1L,
  harmony_params = params_sc_harmony(k = 10L),
  no_pcs = 10L,
  label_columns = "cell_grp",
  .verbose = FALSE
)
query <- demo_single_cells(prepped = FALSE, seed = 7L)
query <- map_symphony_query(symphony_ref, query = query, .verbose = FALSE)
labels <- transfer_labels_symphony(
  symphony_ref,
  query = query,
  label_column = "cell_grp",
  .verbose = FALSE
)
head(labels)

unlink(c(ref@dir_data, query@dir_data), recursive = TRUE, force = TRUE)
}
