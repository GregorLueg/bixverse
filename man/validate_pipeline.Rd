% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipelines_sc.R
\name{validate_pipeline}
\alias{validate_pipeline}
\title{Check that a pipeline can run on a given class}
\usage{
validate_pipeline(pipeline, class)
}
\arguments{
\item{pipeline}{\code{ScPipeline}.}

\item{class}{String. Class of the object the pipeline would start on. One of
\code{c("SingleCells", "SingleCellsSubset", "MetaCells")}.}
}
\value{
Invisibly, the class the pipeline would return.
}
\description{
Walks the pipeline and tracks which class each step would receive. Steps
declare the classes their generic has methods for, and most steps hand back
what they were given; \code{\link[=step_metacells_sc]{step_metacells_sc()}} does not, it turns a
\code{SingleCells}/\code{SingleCellsSubset} into a \code{MetaCells}. Without this you would
only find out about a mismatch when S7 fails to dispatch, i.e. after the
expensive steps already ran.
}
