% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_single_cell.R
\name{write_tenx_h5_sc}
\alias{write_tenx_h5_sc}
\title{Helper function to write data to a 10x CellRanger-style h5 file}
\usage{
write_tenx_h5_sc(
  f_path,
  counts,
  barcodes,
  features,
  version = c("v3", "v2"),
  overwrite = TRUE
)
}
\arguments{
\item{f_path}{String. Output path.}

\item{counts}{Sparse matrix (\code{dgRMatrix} or \code{dgCMatrix}), cells x features.}

\item{barcodes}{Character. Cell barcodes, length \code{nrow(counts)}.}

\item{features}{data.table with \code{id} and \code{name} of length \code{ncol(counts)}. For
v3 may include \code{feature_type}; defaults to \code{"Gene Expression"} if absent.}

\item{version}{One of \code{"v3"} or \code{"v2"}.}

\item{overwrite}{Boolean.}
}
\value{
Invisible.
}
\description{
Helper function to write data to a 10x CellRanger-style h5 file
}
\examples{
# a CellRanger v3 style h5, ready for load_tenx_h5()
data <- generate_single_cell_test_data(
  syn_data_params = params_sc_synthetic_data(n_cells = 200L, n_genes = 40L)
)
f_path <- tempfile(fileext = ".h5")
write_tenx_h5_sc(
  f_path = f_path,
  counts = data$counts,
  barcodes = data$obs$cell_id,
  features = data.table::data.table(
    id = data$var$gene_id,
    name = data$var$ensembl_id
  )
)
read_tenx_h5_metadata(f_path)$dims

unlink(f_path)
}
\keyword{internal}
