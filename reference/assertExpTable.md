# Assert an expression info table has the expected columns and types

Assert an expression info table has the expected columns and types

## Usage

``` r
assertExpTable(x, name)
```

## Arguments

- x:

  The candidate `data.table`.

- name:

  Variable name for error messages.
