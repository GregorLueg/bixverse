# Check (bootstrapped) metacell generation parameters

Checkmate extension for checking the (bootstrapped) metacell generation
parameters.

## Usage

``` r
checkScBootstrappedMetacells(x)
```

## Arguments

- x:

  The list to check/assert

## Value

`TRUE` if the check was successful, otherwise an error message.
