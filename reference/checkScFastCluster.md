# Check singe cell fast clustering parameters

Checkmate extension for checking the fast clustering parameters.

## Usage

``` r
checkScFastCluster(x)
```

## Arguments

- x:

  The list to check/assert

## Value

`TRUE` if the check was successful, otherwise an error message.
