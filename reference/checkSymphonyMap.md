# Check Symphony map parameters

Checkmate extension for checking the Symphony query mapping parameters.

## Usage

``` r
checkSymphonyMap(x)
```

## Arguments

- x:

  The list to check/assert.

## Value

`TRUE` if the check was successful, otherwise an error message.
