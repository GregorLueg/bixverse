# Add "is_obs" attribute

Add "is_obs" attribute

## Usage

``` r
.add_is_obs_attr(x)
```

## Arguments

- x:

  data.table. The data.table to which to add the attribute.

## Value

`x` with added attribute
