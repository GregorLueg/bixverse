# Validate a k range for an LDA sweep

Validate a k range for an LDA sweep

## Usage

``` r
.assert_lda_k_range(k_range)
```

## Arguments

- k_range:

  Integer vector. The topic counts to evaluate.

## Value

The validated integer vector.
