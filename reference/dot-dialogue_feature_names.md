# Name the feature columns a cell type kept

Name the feature columns a cell type kept

## Usage

``` r
.dialogue_feature_names(feature, kept)
```

## Arguments

- feature:

  Numeric matrix. That cell type's features.

- kept:

  Integer. 0-indexed columns that survived the ANOVA filter.

## Value

Character vector of column names.
