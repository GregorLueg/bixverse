# Helper function to generate ADT vars

Helper function to generate ADT vars

## Usage

``` r
.generate_adt_var(adt_counts)
```

## Arguments

- adt_counts:

  Numeric matrix. Of shape samples x features with the ADT raw counts.

## Value

A data.table with the feature_idx and feature_id of the ADT data.
