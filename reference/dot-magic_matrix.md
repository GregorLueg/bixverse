# Fetch the imputed matrix, with a pointer when it is missing

Fetch the imputed matrix, with a pointer when it is missing

## Usage

``` r
.magic_matrix(object)
```

## Arguments

- object:

  `SingleCells` or `SingleCellsSubset` class.

## Value

The cells x genes matrix held by the `ScMagic` layer.
