# Match requested features against available feature names

Match requested features against available feature names

## Usage

``` r
.match_features(features, available)
```

## Arguments

- features:

  Character vector. Requested feature ids.

- available:

  Character vector. Feature names present in the data.

## Value

The subset of `features` that was matched, in input order.
