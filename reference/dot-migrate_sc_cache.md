# Bring a loaded `ScCache` into the current shape

Bring a loaded `ScCache` into the current shape

## Usage

``` r
.migrate_sc_cache(x)
```

## Arguments

- x:

  The `ScCache` loaded from disk.

## Value

The cache with every current slot present.
