# Bring a loaded `ScMap` into the current shape

Bring a loaded `ScMap` into the current shape

## Usage

``` r
.migrate_sc_map(x)
```

## Arguments

- x:

  The `ScMap` loaded from disk.

## Value

The map with every current slot present.
