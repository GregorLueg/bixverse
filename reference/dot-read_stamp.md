# Read the provenance stamp of an artefact

Read the provenance stamp of an artefact

## Usage

``` r
.read_stamp(obj)
```

## Arguments

- obj:

  The artefact payload.

## Value

The stamp list, or `NULL` when the artefact carries none (which is the
case for anything computed before stamping existed).
