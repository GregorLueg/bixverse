# Remap communities by size

Remap community assignment by size

## Usage

``` r
.remap_communities_by_size(labels)
```

## Arguments

- labels:

  Integer vector that represents the community assignment.

## Value

The remapped communities by size
