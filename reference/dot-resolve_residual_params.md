# Resolve and validate the parameters for a residual fit

Resolve and validate the parameters for a residual fit

## Usage

``` r
.resolve_residual_params(residual_params, method)
```

## Arguments

- residual_params:

  List or `NULL`. What the caller passed.

- method:

  String. The method being fitted.

## Value

The validated parameter list.
