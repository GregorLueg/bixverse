# Z-score and optionally clip a numeric vector

Z-score and optionally clip a numeric vector

## Usage

``` r
.scale_and_clip_expr(x, clip = NULL)
```

## Arguments

- x:

  Numeric vector.

- clip:

  Optional numeric. Clip the z-scores to `[-clip, clip]`.

## Value

The scaled (and optionally clipped) vector.
