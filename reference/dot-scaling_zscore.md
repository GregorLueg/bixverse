# Z-score scaling

Returns `(x - mean(x)) / sd(x)`. Returns zeros if `sd(x)` is zero or
`NA`.

## Usage

``` r
.scaling_zscore(x)
```

## Arguments

- x:

  Numeric vector.

## Value

Numeric vector.
