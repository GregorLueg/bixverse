# Get the PCA singular values

Get the PCA singular values

Get the PCA singular values from a `single_cell_exp`.

## Usage

``` r
get_pca_singular_val(x)

# S3 method for class 'sc_cache'
get_pca_singular_val(x)
```

## Arguments

- x:

  An object to get PCA singular values from.
