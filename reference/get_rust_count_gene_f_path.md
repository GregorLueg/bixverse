# Get the file path to the counts in gene format

Get the file path to the counts in gene format

## Usage

``` r
get_rust_count_gene_f_path(object)
```

## Arguments

- object:

  `single_cell_exp` class.

## Value

The path to the `counts_genes.bin`
