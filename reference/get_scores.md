# Get either the cxds or weighted scores

Get either the cxds or weighted scores

## Usage

``` r
get_scores(x, ..., score_type = c("weighted", "cxds_scores"))

# S3 method for class 'ScDblFinderRes'
get_scores(x, ..., score_type = c("weighted", "cxds_scores"))
```

## Arguments

- x:

  An object to get the weighted or cxds scores from.
