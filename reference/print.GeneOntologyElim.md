# print Method for GeneOntologyElim object

Print a GeneOntologyElim object.

## Arguments

- x:

  An object of class `GeneOntologyElim`, see
  [`GeneOntologyElim()`](GeneOntologyElim.md).

- ...:

  Additional arguments (currently not used).

## Value

Invisibly returns `x`.
