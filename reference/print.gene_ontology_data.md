# print Method for gene_ontology_data object

Print a gene_ontology_data object.

## Arguments

- x:

  An object of class `gene_ontology_data`, see
  [`gene_ontology_data()`](gene_ontology_data.md).

- ...:

  Additional arguments (currently not used).

## Value

Invisibly returns `x`.
