# print Method for ontology object

Print a ontology object.

## Arguments

- x:

  An object of class `ontology`, see [`ontology()`](ontology.md).

- ...:

  Additional arguments (currently not used).

## Value

Invisibly returns `x`.
